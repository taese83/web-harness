# Protected Core — 하네스가 지켜야 하는 것 (판단 계층의 canonical)

형식 게이트(validate-harness)는 **필요조건**이다. 이 문서는 그 위의 판단 계층이 보호하는
불변식을 서열화하고, 변경 클래스별 판단 질문과 증거 형식을 고정한다. 소비자: 루트 `CLAUDE.md`
판단 게이트(모든 AI 세션에 로드), `harness-change-reviewer` 에이전트, `validate-contract-hygiene.mjs`
(기계 앵커). 이 문서 자체의 부재는 validate가 fail한다.

## 0. 원칙 — 판단은 기계화되지 않는다, 그러나 강제된다

"과적합인가", "2번째 서비스에도 성립하는가"를 grep으로 판정할 수 없다. 기계가 강제하는 것은
**판단이 실제로 일어났고, 반증 가능한 형태로 기록됐는가**다. 진실성 검증은 fixture·CI 증명이
따라와야 완성된다(주장≠증명 — I1).

## 1. 보호 불변식 (서열 — 충돌 시 위가 이긴다)

- **I1 · 증거의 진실성.** green은 실제 실행 근거가 있을 때만 green이다. 로컬에서 서명 증거를
  위조하지 않는다. 증명 없는 `certified`를 주장하지 않는다(tier 정직). "닫았다"와 "닫는 시늉"을
  구분해 보고한다.
- **I2 · 게이트 강도.** 통과율을 위해 검증을 약화하지 않는다. 골든/생성물이 게이트에 막히면
  그 지점이 곧 결함이다 — 게이트가 아니라 모델링을 고친다.
- **I3 · 일반화.** 계약은 부류(class) 역량이다. 특정 서비스의 이름·백엔드·고정 수치·사고모델을
  인코딩하지 않는다. 기준: **서로 다른 서비스 형태 2개 이상**에 성립함을 명시하고, 참조 서비스는
  eval fixture로만 등장한다.
- **I4 · 고정 비용 예산.** always-read·CLAUDE.md·플러그인 배포 사본 표면은 예산이다. 추가 전에
  제거를 먼저 검토한다(선례: ×3 미러는 2026-08-18 소비자 0 실증으로 제거 — M4 판정). 예산
  갱신은 의식적 행위(baseline 편집 + JUDGMENT 기록)여야 한다.
- **I5 · 프록시 ≠ 품질.** 게이트가 프록시(줄 수·개수·패턴)라면, 프록시를 우회해 "통과"만 만드는
  것은 통과가 아니라 위반이다. 알려진 프록시는 §4에 등록하고, 우회 발견 시 그 사례를 등록부에
  추가한다.
- **I6 · 안전 하한.** 접근성·보안·receipt 게이트는 fast-path에서도 생략 불가. 줄이는 것은
  세리머니뿐이다.

## 2. 변경 클래스별 판단 질문 (커밋 전 — 증거로 답한다)

**모든 변경 공통**: I1(주장/증명 구분), I2(게이트 약화 없음), I5(프록시 우회 없음).

| 클래스 | 추가 질문 | 요구 증거 |
|---|---|---|
| 계약 신설/확장 | I3: 서로 다른 서비스 형태 2개+에 성립하는가? 한 서비스를 역설계하지 않았는가? | 계약 내 `## 일반화 근거` 섹션(형태 2개+ 명명, fixture 검증 여부 명시) — 기계 강제 |
| always-read/SKILL 변경 | I4: 고정 로드가 늘었는가? 조건부로 강등 가능한가? | baseline 대비 증감. 성장 시 baseline 갱신 + JUDGMENT 사유 — 기계 강제 |
| 게이트/validator 변경 | I2: 무엇이 느슨해지는가? G2: 오탐이 정당한 기존 스킬을 잡는가? | 실제 트리에서 오탐 0 실행 로그 + 의도 회귀(seed) 탐지 로그 |
| 스킬/에이전트 신설 | I4: 고정 로드·플러그인 배포 표면 유지비 정당한가? 기존과 중복인가? | 기존 대안 검토 1줄 + README 인벤토리 갱신 |
| fast-path/세리머니 축소 | I6: 안전 하한이 남는가? | 유지되는 게이트 목록 명시 |
| tier/supportLevel 변경 | I1: 라벨이 증거와 일치하는가? | 근거 receipt/golden 링크 |

**기록 형식**: 커밋 본문에 `JUDGMENT:` 블록으로 해당 질문의 답을 1–3줄로 남긴다. 실질 변경
(계약·게이트·스킬/에이전트)은 read-only `harness-change-reviewer`를 먼저 실행하고 결과를 반영한다.

## 3. 예산 (기계 강제 — contract-hygiene)

- 루트 `CLAUDE.md` ≤ 60줄(판단 게이트 마커 `harness-judgment-gate` 필수·삭제 시 fail).
- 스킬별 always-read: `contract-hygiene-baseline.json`의 실측이 상한(ratchet — 성장만 fail).
  미등록 신규 스킬 기본 상한 8.
- baseline 밖 신규 reference 계약: `## 일반화 근거` + 형태 2개+ 필수.

## 4. 알려진 프록시 등록부 (I5 — 정직한 한계 명세)

| 프록시 | 한계 | 우회 사례(실제) | 대응 |
|---|---|---|---|
| 스크립트 400줄 제한 | 줄 수 ≠ 복잡도 | import 세미콜론-병합으로 399줄 맞춤(기존 관행 + 2026-08 세션도 동일 우회) | 병합으로 줄이지 말 것. 실질 해소는 validator 모듈 추출 — **첫 실질 추출 발생(2026-08-18)**: UI 레인 검사 추가로 한도 초과 시 병합이 아니라 섹션-리더 블록을 `validate-section-readers.mjs`로 추출해 해소. 잔여 인라인 블록의 추출은 계속 TODO |
| always-read 카운터 | `항상…읽는다` 한국어 마커의 첫 문장만 집계 — 영어 서술("Read X before…")·조건부 읽기는 미집계 | 앵커 드리프트 실측(2026-08-18 리뷰): web-orchestrator SKILL.md에서 정규식 첫 매칭이 실제 always-read 헤더(16행, "항상" 없음)가 아니라 무관한 129행("plan-reviewer를 항상 실행하고…")에 걸려 **실제 목록(18~25행)은 전혀 미집계** — 우연히 카운트가 baseline 2와 일치해 self-consistent하게 보였다. 즉 이 표면의 성장은 현재 기계로 감시되지 않는다 | **해소(2026-08-20)**: web-orchestrator SKILL.md의 실제 목록을 `<!-- always-read -->` 앵커로 감싸고 **앵커가 산문 휴리스틱을 이기도록** 스코프 결정을 바꿨다 — 오집계(129행 무관 문장, refs 2)가 실측(refs 6)으로 정정됐고 baseline도 의식적으로 갱신했다. **총 로드비용도 이제 측정한다**: `alwaysReadBytes` 차원 신설(참조 수가 그대로여도 계약 파일이 커지면 FAIL — +3KB 주입으로 실효 검증), 숫자는 README `<!-- inventory:entry-cost -->`로 공표되고 실측과 어긋나면 FAIL한다. **남는 한계**: 바이트는 실제 토큰이 아니라 근사(bytes/3 표기), 앵커를 배치하지 않은 29개 스킬은 여전히 산문 매칭이며 alwaysReadBytes 미등록(소급 fail 금지 관례) |
| 일반화 근거 섹션 | 존재·형태만 검사, **진실은 미검증** | — | fixture/2번째 서비스 검증 전까지 해당 계약은 "명명 수준" — 계약에 자기 표기 |
| 골든 5/7 로컬 green | e2e·서명 attestation 없는 로컬 증명은 폐곡선 아님 | **라벨-증거 언바인딩 실측(2026-08-18 M3 정찰)**: 어떤 기계 검사도 certified 라벨을 증거에 묶지 않아 adapter.json 편집만으로 라벨이 성립했고, 실제로 유일 certified(react-vite-spa: 골든 validator 미커버·locked profile 없음·receipt 0)가 compatible인 hybrid(골든 완비·QA 7/7·T1 validator)보다 증거가 약한 **라벨 역전** 상태였다 | **2026-08-18 해소**: `validate-certified-evidence`가 certified→증거(골든 존재·locked certified 일치·`t1-summary.json ISOLATED_VERIFIED`·QA 전부 PASS)를 기계 강제 + 매 실행 seed 4종으로 무장 증명(certified 0개 vacuous 상태에서도). react-vite-spa는 증거 정렬로 compatible 강등(기능 무손실). **남는 한계**: t1-summary 자체의 진위(위조 JSON)는 이 게이트 밖 — validate-isolated-cohort + T2 서명 attestation 몫. compatible 어댑터의 스킬-내러티브 검사는 스킬 부재 시 여전히 skip(react-vite-spa는 전용 스킬 없음 — 미해결 TODO). **리뷰 실측 2건(2026-08-18) 폐쇄**: QA 리포트 0건 vacuous 통과 → `CERTIFIED_WITHOUT_QA`로 명시 실패 + seed 고정, seed 4종이 에러코드 6개 중 3개 미커버(해당 검사 삭제에도 "armed" green) → 코드별 음성 seed 8종으로 전면 커버. 이 게이트는 T1 **하한**만 강제 — 8/8 production-grade는 별도(§ production-hardening-plan) |
| UI_LANE 일치 검사 | `validate-ui-lane.mjs`는 선언 레인(overlay 우선→tech-stack)과 `src/`(ts·tsx·js·jsx)의 **모듈 지정자 접두어**만 대조한다 — 정적 import·재수출(`export…from`)·side-effect import·dynamic `import()`·`require()`를 매칭하되, 레인의 정의적 시그널(mui: tailwind 생태계 4종 / tailwind-shadcn: `@mui/`·`@emotion/`)만 잡는다. **미검출**: 문자열 조립 지정자, import 없는 우회(전역 CSS로 Material 모방 등), 의미 수준 이탈. code-reviewer의 vendored-a11y 검사는 산문 지시(정적 grep 절차)이지 스크립트가 아니다 | tier (a) 시점(2026-08-18 리뷰): 기계 검사 0 + templates.md 레인 무관 MUI 스캐폴드 → tier (b)로 해소. tier (b) 리뷰가 재수출·dynamic import 미탐(설계 대상 R1의 우회)과 미지 overlay 레인의 조용한 폴백을 실측 → **동일 커밋에서 폐쇄**(정규식 확장 + UNKNOWN_LANE_DECLARATION loud 실패, 회귀 11종) | 잔여: eval 시나리오 2종은 계약 등재 상태이며 **실행 전**(시나리오 실행이 최종 진실 — maturity 라벨은 contract-only 유지). validator의 의미 수준 한계는 좌측 명세대로 |
| 재진입 로드 맵(reentry-map) | 로드 인덱스는 원본(SKILL.md·execution-contract 등)이 바뀌어도 스스로 fail하지 않는다 — stale한 맵을 따라도 기계는 침묵한다(강제 층은 로드 독립이라 게이트는 안 뚫리지만, 안내 층이 낡은 지시를 재생산) | — | 산문 완화만 존재: SKILL.md 우선 + 발견자 수리 책임 + 애매하면 전체 진입 폴백(reentry-map.md 사용 규칙). 재진입 훅·마커 층이 얹히면 사람이 이 파일을 덜 열게 되므로, 로드 지시 대조 기계 검사는 미해결 TODO — day-2 파일럿에서 stale 발생 여부를 관찰해 우선순위 결정 |
| SessionStart 훅 주입 채널 | always-read baseline ratchet(`countAlwaysReadRefs`)은 마크다운 마커만 계측한다 — 런타임 훅의 stdout 주입(`detect-harness-project.mjs`, `_workspace/` 프로젝트에서만 ~4줄)은 이 채널 밖이라 **커져도 기계가 못 잡는다**(2026-08-18 리뷰 등록) | — | 현 주입은 조건부 소량이며 안내 층 한정. 이 채널로 주입을 늘리는 변경은 리뷰 필수 + 이 행 갱신. 훅 주입량 기계 계측(스크립트 stdout 상한 검사)은 미해결 TODO |
| 생성 프로젝트 CLAUDE.md 마커 채널 | 위 행과 같은 클래스의 두 번째 채널(2026-08-18 리뷰 등록): 생성 프로젝트의 always-read 표면(~6줄 마커)을 늘리는데 이 repo의 60줄 예산·ratchet은 생성물에 미적용. 강제는 **경로 수준뿐**(ownership 훅은 내용을 읽지 않음) — 템플릿 그대로 생성·재실행 중복 방지·기존 내용 보존은 산문 규칙(`environment-scaffolder`의 재진입 마커 규칙, 구 package-scaffolder 규칙 18)의 self-attestation. **blast radius 비대칭**: toolchain pin 오류는 CI가 자기 검출하지만 CLAUDE.md 마커 오손은 어떤 CI도 못 잡고 미래 세션을 조용히 오도한다 | — | 마커는 고정 블록·변형 금지·append-only를 규칙에 명시, 위반은 리뷰 몫. 이 채널로 생성 CLAUDE.md 주입을 늘리는 변경은 리뷰 필수 + 이 행 갱신. 마커 content-level golden/scenario 검증은 백로그(미해결 TODO) |
| 스타일 타일 자기채점 대비 검사 | 발산 후보의 WCAG 대비 하한을 후보 디렉터리로 **복사된 사본 안의 내장 JS**가 자체 채점한다 — "사본 수정 금지"는 산문 규칙뿐이고, 사본의 검사기를 고쳐 FAIL을 PASS로 위장해도 잡는 독립 기계가 없다(선례인 wh-overlay도 존재 검사만 있음). 오케스트레이터의 렌더 판정도 사본 무결성을 재검증하지 않으면 self-report를 읽는 것이다(2026-08-18 리뷰 등록) | — | 발산 단계 안내·조기 탈락용 게이트로 한정 — 릴리스 하한의 정본은 여전히 Phase 4 접근성 QA(중복 채점이지 대체 아님). 렌더 판정 시 사본과 assets/ 원본의 digest 대조는 미해결 TODO. 실측 receipt: docs/efficacy/receipts/style-tile-selfcheck.md |
| 시안 구현 축별 대조표(IMPLEMENTATION-VERDICT) | 시안 적용 완결성의 검증표는 **순수 자기 기록**이다 — 존재·형식을 검사하는 validator가 없고(전수 검색: 계약 1곳 언급뿐), 실제 산출 이력도 아직 없다(2026-08-19 등록 시점). 색만 확인하고 5축 통과로 기재하는 부실 기재를 잡는 독립 기계 없음 | 등록 계기 실측: motor-lab v4에서 색상만 적용된 리컬러가 시안 구현으로 완료 선언·릴리스까지 통과 — 사용자가 발견(receipts/implementation-completeness-incident.md) | 계약이 대조 항목 최소 5종(폰트·radius·spacing·그림자·액센트)과 위치를 고정. 시스템-우선 왕복 규율(누락 원소의 정본 경유)도 self-attestation이며 이 대조표가 탐지망이다. computed-style 자동 대조(브라우저 스크립트)는 미해결 TODO — 첫 산출 사례가 나오면 형식 검사 승격 검토 |
| SELECTED_CANDIDATE 마커(시안 선정) | 판정 기록(RENDER-VERDICT.md)의 고정 줄은 오케스트레이터 self-attestation — 소비자(콘솔 인덱서)는 후보 목록 소속만 검증하고 **마커 값이 판정 산문의 실제 결정과 일치하는지는 기계 미검증**(산문 "1순위 b"에 마커 candidate-a여도 콘솔은 a를 현재 디자인으로 노출) | — | 후보 목록 바운딩(목록 밖 값 무시) + 마커 부재 시 "선정 기록 없음" 정직 표시(추정 금지)가 코드 방어. 마커-산문 일치는 리뷰 몫. 차순위 교체 시 마커 갱신 의무는 계약 몫(2026-08-19 리뷰 등록) |
| maturity의 eval-언급 검사 | 시나리오 파일 내 스킬명 **언급**은 커버의 필요조건일 뿐 — 형식적 시나리오로 라벨만 승격 가능 | — | 승격용 시나리오는 신설 계약의 실질 assertion을 담아야 하며, 최종 진실은 시나리오 *실행* 결과다 |
| ~~anchorReceipt(live-delta 승인)~~ — **2026-08-28 제거**(라이브 델타 폐지로 기제 자체가 사라졌다. 대체 검증이 생겨서가 아니므로 행을 지우지 않고 이력으로 남긴다) | 존재·형식(한 줄 ≤300자)만 검사했다 — 자유 텍스트 self-attestation이며 라이브 서버·앵커 실검증을 자동 보장하지 않음 | — | receipt에 매칭 수·확인 URL·시점을 기록할 의무는 계약 몫. 실검증 자동화(브라우저 앵커 카운트 수집)는 미해결 TODO |
| ~~live-delta target 신원 대조(identity.titleIncludes)~~ — **2026-08-28 제거**(프록시로 승인 표면을 덮을 때 "지금 덮는 앱이 맞는가"를 증명하던 기제. 덮을 승인이 사라져 대조 대상도 사라졌다) | (2026-08-20 분리: 선언 정본은 `preview/live.json`, 레거시 manifest는 하위 호환 — 깨진 live.json은 침묵 폴백 없이 `INVALID_LIVE_CONFIG` loud fail, console 회귀 고정) manifest/live.json의 리터럴 부분 문자열을 응답 HTML `<title>`과 대조하는 **표시 시점 검사**다 — 제목이 같거나 generic한 앱은 구분 불가, 비-HTML 경로(asset·API·WS 패스스루)는 미검사(Content-Type이 text/html이 아니면 실제 HTML이어도 미검사 — 헤더 오지정 서브케이스 포함), 미선언 킷은 경고만(하위호환 — 소급 하드 실패는 승인 킷 일괄 무효화라 채택 안 함), 선언 자체는 repo 콘텐츠의 자기선언이라 옳은 제목을 골랐는지 미검증. **"신규 킷 필수" 요건도 기계 강제가 아니다**(리뷰 지적 2026-08-20) — design-preview-status-lib/validate-design-preview는 identity 필드를 보지 않으므로 산문 규칙 + 콘솔 "IDENTITY 미검증" 경고가 유일한 탐지망 | 실측 오표시(2026-08-19, 이 행 등록 계기): tart-web delta target 8080을 tamiya-motor-lab vite dev server가 점유 → 콘솔이 다른 앱을 tart-web 라이브 프리뷰로 표시(사용자 발견) | 프록시가 HTML 응답마다 대조·불일치 fail-closed(`LIVE_TARGET_IDENTITY_MISMATCH` 502, 바탕 앱 미표시·bootstrap 미주입), 선언 형식 오류는 미선언 강등 없이 loud(`INVALID_LIVE_IDENTITY`), 헬스체크·콘솔 카드가 신원 상태 노출, 미선언은 "IDENTITY 미검증" 상시 경고 + `unverified` 헤더. 회귀 고정: console 테스트(오표시 재현→차단·복구·미선언 패스스루). identity 부재·형식의 validator 승격(신규 킷 한정 — 승인 이력 있는 기존 킷 소급 제외, 오탐 0 실행 로그 필요)은 미해결 TODO. 앵커 수준 실검증은 여전히 anchorReceipt(위 행) 몫 — 이 검사는 오표시 차단이지 라이브 실검증 대체가 아니다 |
| preview render-source 마커 스캔 | 파일 내 **문자열 포함**만 검사 — 해당 파일이 실제 로드·실행되는지 미보장(decoy 파일로 우회 가능) | — | .mjs 포함은 2026-08-28 제거된 delta 모드의 잔재였고 함께 걷었다(현재 스캔은 html·js). 실질 진실은 브라우저에서의 앵커 렌더 확인(TC 검증) 몫 |
| 스냅샷 바탕 앵커 검사(anchor-map) | 캡처 시점의 미매칭·복수매칭·고아 앵커 검사 3종은 **anchor-map이 있을 때만 의미가 있다** — `--anchor-map`을 주지 않으면 세 검사가 전부 공허하게 통과한다 | 적대 리뷰(2026-08-28) 등록 계기: anchor-map 없이 캡처하면 배지 0개 바탕이 만들어지는데 그 위에서 승인이 그대로 진행됐다 | **2026-08-28 해소** — 강제 지점을 캡처(우회 가능: 플래그를 안 주면 그만)가 아니라 **승인 판정**으로 옮겼다. `readBaseSnapshot`이 ① 바탕 전체에 `data-wh-anchor`가 하나도 없으면 INVALID, ② 앵커를 **meta가 아니라 HTML에서** 읽어 메타 주장으로는 통과 불가, ③ traceability에 없는 앵커는 오버레이가 배지하지 않으므로 loud, ④ 앵커가 있는데 부트스트랩이 없으면 무력하므로 loud. 반증 4건 등록. **남는 한계**: 앵커가 **옳은 요소**에 붙었는지는 여전히 미검증(셀렉터 유일성만 캡처가 확인) — 앵커 1개짜리 형식적 바탕은 이 게이트를 통과한다. 개수 하한은 두지 않았다(맥락 route가 정상이라 프록시가 된다) |
| 스냅샷 바탕 문자열 치환 | 치환은 **텍스트 노드 + 속성 5종**(`value`·`title`·`alt`·`placeholder`·`aria-label`)만 덮는다. 미커버: 그 밖의 속성(`data-*`·`href`·`srcset`), HTML 주석, 수집된 CSS의 `content:` 문구, 이미지·canvas 안의 픽셀. 보존 어휘는 소스 파일의 리터럴·템플릿 텍스트에서만 오므로 서버에서만 오는 문구는 어휘에 없다(치환되는 쪽 — 안전 방향). 부분 일치 보존 하한은 6자이며 그보다 짧은 어휘는 전체 일치로만 보존된다 | 적대 리뷰(2026-08-28) 실측 3건 — ① 부분 일치가 문자열 **전체**를 보존해 `Signed in as jane@corp.com`이 통째로 남던 fail-open, ② `title`이 meta.json에 원문으로 기록, ③ `[가-힣]`·ASCII 밖 문자(일본어·키릴·전각 숫자)가 identity로 통과하면서 substitutedCount만 증가 | 셋 다 폐쇄(구간 단위 보존·title 마스킹·유니코드 속성 기반 치환) + 회귀 고정. **남는 한계는 좌측 명세대로이며 meta.json `limits`에 산출물마다 기록된다.** 근본 방어는 여전히 **시드 데이터로만 캡처**다 — 치환은 안전망이지 면허가 아니다 |
| preview 소스 입력 `(.md\|/)` 그룹 필수 검사 | `existsSync` **존재-only** — 빈 디렉토리·빈 파일도 required를 충족하고 digest에 아무 것도 안 실린다. 결함 8호 정합화(2026-08-19)로 required 그룹이 3종→5종으로 확장되며 이 프록시 표면도 함께 커짐 | — | 내용의 진실은 preview 구조 검사(TC 커버리지·앵커)와 sharding validator(INDEX↔절 파일 양방향) 몫 — 이 검사는 입력 존재·digest 고정만 담당. 빈 그룹 감지(파일 0개 시 경고)는 미해결 TODO |
| ingestion ancestor 스캔의 사촌 제외 마커 | `_workspace/01_plan` **디렉토리 존재**로 "독립 하니스 release root"를 판정(결함 11호 정합화, 2026-08-19) — 이름 기반 휴리스틱이라 그 구조까지 위조한 decoy는 ancestor 스캔을 여전히 회피 가능. 기존 `SOURCE_SCAN_EXCLUDED_DIRECTORIES`(dist·test 등 이름 제외)와 같은 클래스의 한계 | 빈 `_workspace/` 위장(리뷰 실측 지적) → 실질 마커(01_plan)로 강화 + 우회 seed 회귀 고정(2026-08-19) | detector는 bounded consistency check이며 난독화 부재의 증명이 아니다(web-profile-contract 기존 명세) — 외부 수집 여부는 intake·review에서 명시 선언이 정본. 구조 위조 decoy 감지 강화는 미해결 TODO |
| 콘솔 frame-src loopback 와일드카드 | 열거식 CSP는 동적 프록시 포트가 문서 로드 후 배정되는 구조적 경합이 있어 `127.0.0.1:*`로 완화(2026-08-10) — DOM-XSS에 대한 심층방어 폭이 loopback 전 포트로 넓어짐 | — | iframe src는 서버 계산 값만, 신뢰 경계는 postMessage origin 엄격 검증 + 프록시 대상은 launch.json 포트 allowlist. script-src 'self'·frame-ancestors 'none' 유지 |
| targetless 승인의 구조화 범위 self-report | 대상 없는 CR(기획 초안) 승인의 affectedFeatureIds는 apply 결과의 자기보고 — "그 FEAT를 정말 이번 apply가 신설했는가"는 미검증(승격 후 canonical 존재만 확인) | 파일럿(2026-08-11): apply가 기존 plan 재작성으로 승인 TC 파괴 — 존재 검사로는 미탐 | **2026-08-12 부분 해소**: `validate-plan-delta.mjs`가 변경 전 안정 ID 인벤토리를 기계로 뜨고(자기선언 아님) 적용 후 선언과 대조해 `UNDECLARED_REMOVAL`로 잡는다 — 실제 파일럿 데이터에서 TC 3건 소멸을 재현·검출 확인. `modified` 선언으로 소멸을 덮을 수 없다. 순서 우회(사고 후 스냅샷으로 before 오염)는 승인 레코드의 `affectedTestCaseIds`를 바닥값으로 삼아 `LATE_SNAPSHOT`으로 차단하고, **delta 파일 삭제 후 재스냅샷**은 delta 바깥 append-only 원장(`.plan-snapshots.jsonl`)의 최초 digest와 대조해 `RESNAPSHOT`으로 거부(exit 2) — 둘 다 실측 확인. 이 우회는 resume-manifest planLock에서 이미 겪은 것과 **같은 클래스**였고(리뷰 지적), 같은 해법(증거를 변경 대상 바깥으로)을 재적용했다. **I3 미충족이 드러낸 구멍(2026-08-12)**: ID 규율을 쓰지 않는 형태(라이브러리형 계획)에서는 안정 ID가 0개라 **계획 문서를 통째로 비워도 PASS**가 났다 — 예약형 SPA 단일 실증이라 보이지 않던 vacuous PASS. `NO_STABLE_IDS`로 닫았고(무산출 가드와 같은 관용구, `--allow-no-ids` 명시 opt-in은 자기진술), 오탐 0도 확인. **남는 한계**: 원장까지 지우면 초기화된다(tamper-evident이지 tamper-proof 아님 — 실해소는 commit-anchor 몫, 미해결 TODO). **2026-08-23: 이 원장의 안전 append·판독은 공유 `evidence-log-lib.mjs`(`appendEvidenceLine`·`readEvidenceLog`)가 정본 관용구다** — `.plan-snapshots`는 그 writer/reader로 이행됐고 최초-digest 가드는 공유 `detectRebind`로 매핑됐다(관용구 재발명 종식). **스냅샷이 정말 변경 *전*에 찍혔는지는 기계가 알 수 없다** — mtime·git 경계 어느 것도 검증하지 않으며, 승인 레코드가 있는 경우에만 `LATE_SNAPSHOT` 바닥값이 작동한다. 즉 이 게이트는 선언 *내용*의 자기진술은 풀었으나 선언 *시점*의 자기진술은 부분적으로만 풀었다. 선언 자체가 옳은지는 미검증이고, ID가 유지된 채 **내용만** 파괴되는 경우는 못 잡는다 — 그건 여전히 인간의 절 보존 확인 몫(runbook 2단계). 라인 diff 렌더(후속 8)로 확인 비용 축소 예정 |
| 구현 검증 증거(implementation receipt) | 존재·형식(승인 TC 부분집합·한 줄 ≤300자)만 검사 — 자유 텍스트 self-attestation이며 테스트가 실제로 실행·통과했는지 자동 보장하지 않음 | — | 증거에 명령·통과 요약·시점 기록 의무는 계약 몫(runbook). 테스트 러너 연동 자동 수집은 미해결 TODO |
| 완결성 게이트 무산출 가드 | "owned 범위에 파일 0개=미완성"은 실제 신호이나, "파일 있음=완성"은 truncation/의미결함까지 보장 못 함. `--allow-no-output`은 self-attestation opt-in(오케스트레이터가 오탐 회피용으로 남용 가능). **truncation 스캐너 실측 한계(2026-08-18 합성 리플레이, docs/efficacy/receipts/synthetic-replay.md)**: 줄 경계 절단 recall 77~93%(미탐 = 마침 완결된 지점의 절단 — 구문 검사의 원리적 한계, Layer 1·3이 보완). 완전 파일 오탐 2/126(1.6%)도 실측됐으나 — 정규식 리터럴 안 괄호 문자를 열림으로 오인(fail-safe 방향이나 재시도 토큰 낭비) — **2026-08-18 해소**(아래 대응란) | 초기 구현이 scannable(.ts/.js) 확장자만 세어 비-code 산출물(.md/.json/.yml) 스폰을 오탐 FAIL — anyFilePresent(확장자 무관)로 수정(2026-08-11). 합성 리플레이가 잡은 정규식-리터럴 오탐 2건: 원인은 (a) word 버퍼가 '/' 판정 전에 flush되지 않아 키워드-선행 정규식(`return /re/`)이 코드로 읽힘, (b) 화살표 `=>` 미추적으로 본문 위치 정규식(`rule => /re/`) 미인식 | 무산출은 anyFilePresent로 판정, truncation은 별도 스캔, 의미결함은 typecheck 몫. --allow-no-output 남용은 리뷰에서 확인. 정규식-리터럴 진입 판정은 2026-08-18 정밀화(word flush 순서 교정 + `=>` 합성 토큰, bare `>`는 JSX 보호로 계속 제외) — 재측정 오탐 0/126·recall 비열화(line-cut 50% 89.2→90.0%, 나머지 동일), 실측 2건 원문을 회귀로 고정(test-spawn-completion.mjs) |
| tech-advisor 버전 pin | 이전엔 install 전까지 자기진술 프록시(§ 위 "일반화 근거"와 성격 유사) — "웹 리서치 교차확인" 주장이 실재 registry와 불일치 가능 | seminar-booking 실증: TS 6.0.0(존재하지 않는 버전) + typescript-eslint↔TS7 peer 비호환(install/lockfile은 WARN만) | **기계검사로 승격(2026-08-11)**: `validate-dependency-pins.mjs`가 존재성+peer 호환을 install 전 검사(Tessl 착안). 단 registry 없는 환경이면 self-attestation으로 강등(install 시점 검증에 위임), 파싱 불가 범위는 미검사 |
| 재개 매니페스트 outputs 자기선언 | `resume-manifest.mjs`의 remaining은 "선언된 outputs의 존재+비-truncation"만 본다 — **선언 자체가 완결 범위를 다 담았는지는 미보장**. 2026-08-12 부분 해소: **사후 축소**(빌더 사망 후 매니페스트를 실제 쓰인 파일에 맞춰 줄여 COMPLETE 위조)는 계획 스팩 확정으로 차단됐고, **선언 밖 산출물**은 owned 교차검증으로 보고된다. **그러나 처음부터 적게 선언한 경우(초기 과소 선언)는 여전히 미탐** — "무엇이 필요했는가"의 진실은 스펙에 있지 파일시스템에도 digest에도 없다 | 실측 재현(2026-08-12): 스팩 확정 전에는 outputs 3→1 축소가 `COMPLETE exit=0`으로 통과, 스팩 확정 후 `INVALID exit=1`. **스팩을 매니페스트 안에만 두자 두 우회가 실측으로 뚫림** — `planLock` 삭제(→unlocked, exit 0) · 축소 후 재확정(→새 digest, exit 0). 증거를 매니페스트 바깥 append-only 원장으로 옮겨 둘 다 차단(최초 항목과 대조, 재확정은 `relocked`로 노출) | `validate-spawn-plan --lock`이 FITS일 때만 계획 digest를 박고(REFUSE된 계획은 잠그지 않음), `resume-manifest`가 대조해 TAMPERED면 remaining이 비어도 exit 1(fail-closed). 스팩 확정 없으면 "검증되지 않은 자기선언"으로 정직 보고. 증거는 매니페스트 바깥 append-only 원장(`.plan-locks.jsonl`)에 두어 파일 내 스팩의 두 우회를 차단하고, `--lock`은 다른 digest의 스팩이 있으면 **재확정을 거부**(exit 2, 사전 차단). 단 **원장과 planLock을 둘 다 지운 뒤 재확정하면 위조가 성립하며, 그 결과는 정직한 `unlocked`가 아니라 위조된 `locked`다**(증거 전부 파기 시 최초 스팩과 기계적 구분 불가 — 리뷰 지적으로 정밀화). tamper-evident이지 tamper-proof 아님; 이 경로는 CLAUDE.md 비협상 규칙이 금지한다(실해소는 commit-anchor 몫, 미해결 TODO). **2026-08-23: `.plan-locks` 원장의 안전 append(`appendEvidenceLine`)와 판독(`resume-manifest.readLockLedger`→`readEvidenceLog`)은 공유 `evidence-log-lib.mjs`가 정본이다** — 두 사이트(writer·reader)가 이제 O_NOFOLLOW(POSIX)·크기 상한을 함께 갖는다. "전부 반환" 충돌 판정(`conflictingLockDigests`)은 최초-vs-현재보다 강한 superset 의미라 공유 `detectRebind`에 욱여넣지 않고 로컬 유지(재확정 거부 약화 방지). **초기 과소 선언은 계약 몫으로 남음**. 의미결함은 여전히 typecheck 몫 |
| 요구사항 의무 진술 검사 | `validate-requirements-notation.mjs`는 REQ 층의 **의무 진술 존재**와 AC 층의 **EARS 구조 적합**(응답 명시, 조건·트리거 분류)을 본다 — 진술·AC가 **옳은지**·요구사항을 빠뜨리지 않았는지는 여전히 미검증이다. EARS 패턴 분류는 **정규식 기반 추정**이며, 특히 `unwanted`는 한국어에서 구문이 event와 같아 **의미 키워드**(실패·오류·초과·차단 등)로 잡는다 — 어휘 목록 밖의 표현은 놓친다. Must 범위 판정(`### Must`)도 문서 구조에 의존한다 | 개발 중 실측에서 **게이트 자신이 세 번 틀렸다**: 줄끝 마커로 `$` 앵커 실패(100% 오탐) · AC 줄을 헤드라인으로 오인해 상위 REQ의 본문 강탈 · 종결 어미 뒤 여는 괄호 미허용. 셋 다 "문서가 나쁘다"가 아니라 "검사가 틀렸다"였고 회귀로 고정 | 실제 파일럿에서 **16/16 PASS·오탐 0** 확인 후 채택. REQ 블록을 하나도 못 찾으면 통과가 아니라 `NO_REQUIREMENTS`(검사 미수행)로 보고. 진술의 **품질**은 여전히 plan-reviewer와 사람 몫 |
| 산출물 언어 검사 | `validate-output-language.mjs`는 **마크다운 헤딩만** 본다 — 본문·표·코드펜스는 검사하지 않는다(샘플 데이터·고유명사로 한글이 정당하게 들어가 오탐이 크기 때문). 즉 헤딩만 영어이고 본문이 한국어여도 PASS가 난다. 검사는 **단방향**(en 선언에 한글 헤딩 금지)이라 ko 선언의 품질은 보장하지 않는다. `outputLanguage` 자체는 오케스트레이터의 자기선언이며, 요청 언어를 옳게 판별했는지는 미검증 | 실측(2026-08-12): seminar-booking을 `outputLanguage: en`으로 선언하면 한글 헤딩 **236건** 검출 — 게이트가 실제 산출물에서 작동함을 확인 | **미선언은 통과가 아니라 `UNDECLARED`(검사 미수행)로 보고**해 "영어 지원"이 조용한 통과로 성립하지 않게 한다. 헤딩 밖 본문 언어까지 보는 것은 오탐 비용이 커서 미해결 TODO — 실질 품질은 산출물을 읽는 사람 몫 |
| 계약 마커의 자연어 의존 | validator 다수가 **한국어 문자열 자체**를 마커로 썼다(`항상…읽는다`, `## 일반화 근거`, `미구현`, `대상 화면/기능` 등). 본문을 영어로 옮기면 매칭 실패 → 마커 0건 → **조용히 통과**로 게이트가 장식이 된다(번역이 게이트를 끄는데 CI는 green) | 실측(2026-08-12): 한국어는 SOV라 `항상 … 읽는다`가 always-read 목록을 감싸 조건부 읽기와 구분되지만, 영어는 SVO라 그 경계가 없다 — seed에서 lazy 매칭은 참조 0건(MARKER_LOST 오탐), 전방 윈도우는 조건부 참조까지 삼켜 과대계수(3 > baseline 2) | 한국어·영어·중립 앵커를 모두 인식하되, **baseline이 마커 존재를 전제하는데 사라지면 MARKER_LOST로 FAIL**(vacuous pass 차단). 번역 시 정확한 경계가 필요한 곳은 자연어가 아니라 중립 앵커 `<!-- always-read --> … <!-- /always-read -->`를 **필수**로 쓴다. 영어 자연어 매칭은 best-effort이며 틀릴 때 과대계수(FAIL) 쪽으로 틀리므로 저자가 앵커로 옮기게 된다 |
| A/B 암 토글의 self-attestation | `run-eval-executor.mjs --arm off`는 게이트를 **코드로 끄지 않고 프롬프트로 지시**한다(하드 정책이 bypass env를 막으므로 실제 제어점이 오케스트레이터의 호출 여부다). 따라서 ①게이트 미호출 ②telemetry `run` 라벨의 `+gatesOff` 부착이 **둘 다 헤드리스 실행기의 지시 순응에 의존**한다 — 순응하지 않으면 암이 조용히 오염되고, 그 데이터로 낸 결론은 거짓이 된다 | (Stage 1 미착수 — 실측 오염 사례 없음) | 봉쇄 3중: 기본값 `on`·오타 exit 2 / `global-bash-policy-lib.mjs` `evalExecutorContract`가 **서브에이전트·샌드박스 경로에서 `--arm` 자체를 차단**(`--scenario <id> [--dry-run]`만 허용 — 사람 세션에서만 실행 가능) / 대상은 `eval-runs/*/fixture` 격리 디렉터리 한정. 탐지: `checkArmCompliance()`가 executor.log의 3게이트 호출 흔적을 세어 OFF run이 0이 아니면 폐기 판정(문자열 매칭이라 다른 경로 호출은 미탐 — 오염 탐지 하한이지 준수의 증명이 아니다). **Stage 1 receipt 필수 항목**으로 격상(계획서 §5) |
| 마커 무결성 카운트 | `validate-marker-integrity.mjs`는 등록 마커의 **출현 수**만 센다(current<baseline→FAIL) — decoy 삽입으로 실제 배선을 지우고도 카운트를 유지해 통과할 수 있다. 즉 손실은 잡지만 **의미 보존은 미보장** | 문자열 grep 인벤토리의 사각(2026-08-18 리뷰 실증): `component-designer.md`가 열 이름(`주 소비자`) 없이 같은 프로토콜을 참조해 "26곳 전부" 집계에서 누락 — baseline이 누락을 반영한 채 self-consistent하게 보였다 | 마커 탈스팩 확정(marker-delock-plan §5)의 안전망으로 도입(2026-08-18 리뷰에서 등록). **③ 완료로 부분 대체**: 생성물 쪽 소비자-열 검사는 validate-artifact-sharding이 구조 식별(절 행 = 2열 백틱 파일, EOL 앵커) + HARD FAIL + **절 행 커버리지**(백틱 생략·5열 표 부분 이탈 우회를 리뷰가 실증 → 폐쇄, 회귀 고정)로 의미 수준 검증. 소스 쪽은 앵커 `consumer-read-protocol`(28곳 = 27 agents + 계약) 카운트로 전환 — 앵커 decoy 삽입 우회는 여전히 가능하나 앵커는 산문과 달리 우발 삭제·번역 소실이 없어 잔여 위험이 좁다. **④에서 존재-류 2종 추가**(`timeseries-historical-only`·`timeseries-realtime-build-order`, 각 baseline 1): 구 인라인 검사는 의미 문장 자체를 매칭했으므로 "산문 삭제·앵커 존치" decoy에선 신 검사가 구 검사보다 느슨하다 — 이 완화는 본 항목의 일반 한계(의미 보존 미보장)에 포섭되며, 의미 수준 진실은 리뷰어·eval 몫. baseline 갱신(--update)은 before/after diff 출력. **⑤ 파일 필터의 침묵 공백(2026-08-20 실측 등록·부분 수정)**: `collectMarkdown`이 레지스트리에 **명시 등록된 파일**도 `.md`가 아니면 조용히 건너뛰어, 스크립트에 배치한 앵커(`immediate-write-contract`의 게이트 리마인더)가 2곳 중 1곳만 집계됐다 — 미보호 상태로 baseline이 self-consistent하게 통과했다(consumer 열 누락 사례와 같은 클래스의 '세는 대상이 실제 배선과 어긋남'). 명시 파일은 확장자 무관 스캔으로 수정하고 회귀 고정. **남는 공백**: 디렉터리로 등록된 경로의 재귀는 여전히 `.md`만이라, 디렉터리 하위 비-.md 앵커는 미집계다(의도적 범위 제한 — 스크립트 앵커는 파일 단위 명시 등록이 필수라는 규약으로 대응, 코드 주석에 명시) |
| fit 게이트 임계 완화 옵션 | `--max-outputs`/`--max-read-tokens`는 게이트 자신을 느슨하게 만드는 통로다 — 계약은 "완화는 의식적 행위, 사유를 남긴다"고 요구하지만 **사유 기록을 기계로 강제하지 않는다**(산문 의무) | 초기 등록 시 상한이 없어 `--max-outputs 9999999`로 게이트를 사실상 무력화 가능했음 — 리뷰 지적으로 bash 정책에 상한 도입(2026-08-11) | bash 정책이 상한 강제(outputs ≤32, read ≤200k tokens)로 **완전 무력화는 차단**. 그 아래 구간의 완화는 여전히 자기진술 — 남용은 리뷰에서 확인. 매니페스트 `overrideReason` 필드 강제는 미해결 TODO |
| fit 게이트 readMode(injected) | `"readMode": "injected"`는 "발췌를 주입했고 재독을 금지했다"는 **자기진술**이다 — 실제로 주입했는지 게이트가 확인하지 않는다. injected면 reads를 문자 그대로 재므로 측정치가 8배 가까이 작아진다(seminar-booking 실측: 23.4k→2.8k) — 즉 이 한 필드로 게이트를 크게 느슨하게 만들 수 있다. 또한 이 하네스에서 **최초로 CLI 인자가 아닌 파일 내용에 사는 self-attestation**이라, 호출 명령줄만으로는 injected 여부가 안 보인다(실행 출력에는 `readMode=`로 항상 노출됨) | 재구성 실험(2026-08-11, seminar-booking): 좁은 선언 4건 중 1건만 REFUSE / 넓은 선언 4건 전부 REFUSE — **선언 방식이 효능을 지배함이 실측됨** | 미지정·오타는 `browse`로 fail-safe(느슨한 쪽으로 기울지 않음, 테스트로 고정). browse는 파일 선언을 상위 디렉터리로 전개해 측정을 약 8배 조인다. **단 그 조인 값도 임계 미만이라 실측 4건은 여전히 미탐** — 아래 행의 결과 수준 진실 참조. injected 사용 시 발췌 근거 포인터(주입 절 제목·해시)를 매니페스트에 요구하는 강화는 `overrideReason`과 묶어 미해결 TODO |
| 스폰 사전 fit 게이트 reads 선언 | `validate-spawn-plan.mjs`는 **선언된 `reads` 범위**의 바이트만 잰다 — 선언에서 누락한 스펙은 계산되지 않고, 빌더가 선언 밖을 읽으면 FITS를 받고도 runaway가 난다. 토큰 수는 바이트 기반 **추정**(ASCII/4 + 비-ASCII/3)이지 토크나이저 실측이 아니다. 임계(8 outputs · 60k tokens)는 단일 서비스 실측에서 뽑은 값이라 형태가 다른 서비스에서 재교정 필요 | — | 산문 규칙(자기진술) 대비 강화점: 선언이 아티팩트로 남고, `reads`에 디렉터리를 적으면 하위 전체가 전개돼 과소 신고가 어렵고, 사후 `resume-manifest`와 **같은 매니페스트**를 공유해 교차 확인이 가능하다. 교정 근거: 실측 폭주(seminar-booking client-domain-state-builder)를 넣으면 162k 추정 → REFUSE(실측 소비 168k, telemetry 원본). **미실증: 이 게이트가 실제 runaway 발생률을 낮추는지는 Phase 3 재실행으로 측정 전이다** — 지금 있는 것은 교정 증거이지 효능 증거가 아니다. **결과 수준 진실(2026-08-11 재구성 실측 / 빈도는 2026-08-12 telemetry로 정정 — 실제 미완은 22스폰 중 3건, 토큰 15%): 재구성한 베이스라인 계획 6건 중 선언과 무관하게 걸리는 것은 OUTPUT_FANOUT 2건뿐이고, 나머지 4건은 browse 기본값 적용 후에도 10.9k~23.4k로 임계 60k 미만이라 미탐이다** — 잡으려면 사람이 정직하게 넓은 reads를 선언해야 한다. 오탐은 반대로 확인됨(정당하게 좁은 스폰 5건 전부 FITS, 오탐 0). 또한 browse의 상위 디렉터리 전개는 **산출물 디렉터리 단위**라 artifact-sharding의 소비자 경계와 어긋날 수 있다(단일 aggregate만 필요한 빌더도 같은 산출물의 다른 샤드까지 합산) — 현 규모에선 오탐 0이나 샤드가 훨씬 큰 프로젝트에선 재확인 필요 |
| TC 실행 exit code 판정(콘솔 QA 탭) | 콘솔 `/api/qa/tc-run`은 프로젝트가 선언한 `test:tc` 스크립트를 TC ID 인자로 실행하고 **exit code를 그대로** 기록·표시한다(2026-08-20, 저자 지시로 실행 기반 판정 도입) — "그 TC의 단언이 실제로 실행됐는가"는 러너 필터 구성(스크립트 내용) 몫이며, `--passWithNoTests` 류 러너 설정이면 매칭 0건이 exit 0으로 통과할 수 있다(vitest 기본값은 매칭 0건 = 비0 exit로 fail-closed). 재테스트 필요 표기는 프로젝트 서브트리 git 스탬프(경로 스코프 마지막 커밋+dirty 다이제스트, **`_workspace/04_qa`만 제외** — 실행 기록 tc-runs.jsonl·QA receipt가 여기 쓰이는 출력이라, 포함하면 '실행→기록이 트리 dirty→즉시 재테스트'의 자기 무효화가 난다; 2026-08-20 motor-lab 실증 발견. 제외를 `_workspace` 전체로 넓히면 01_plan/feature-plan.md(TC given/when/then 정본)까지 빠져 **CR 미경유 스펙 직접 수정을 놓친다** — 리뷰 지적으로 04_qa로 국한, 01_plan 변경은 스탬프에 잡힘을 회귀 고정. 남는 한계: 03_dev 등 04_qa 밖 _workspace 메타데이터 쓰기(CR 파일·스냅샷)는 스탬프를 흔들 수 있으나 보수적(과다 재테스트) 방향이라 무해) 불일치·실행 이후 승인 CR라는 **파생 신호**다 — 스탬프를 못 얻거나(비-git) **커밋 베이스라인이 없으면**(상위 repo에서 gitignore된 경로 등 — dirtyDigest만으로는 변화를 못 잡는다) '변경 감지 불가'로 정직 표기하고 지어내지 않는다(2026-08-20 라이브 테스트에서 gitignore 경로의 침묵 오도를 발견·수정) | 직전 구현(같은 날)의 정적 토큰-발견 뱃지를 저자가 프록시로 기각 — 토큰 발견은 "테스트 존재"조차 보장하지 못했다(프로덕션 소스의 앵커 토큰이 테스트로 오인될 뻔한 실측: TC-001-1) → 실행 기반으로 교체 | 실행 채널은 사전 선언 명령만(package.json `test:tc`, 미선언 fail-closed 404, 프로젝트당 동시 1개, 300s 타임아웃) — live-base start와 같은 로컬 신뢰 모델(origin+intent 검증). 기록은 append-only `_workspace/04_qa/tc-runs.jsonl`(1MB 상한, O_NOFOLLOW)에 사실만(exit·출력 꼬리·소스 스탬프). 스위트-수준 릴리스 판정과 무관함을 UI 문구로 명시. `test:tc`의 no-match 성공 처리 방지 기계 검사는 미해결 TODO(예시 3종 러너의 fail-closed 기본값은 소스 지식 기반 주장이며 실러너 fixture 실행 증거는 아직 없음 — 리뷰 2026-08-20). '선언됐으나 이름 태그 없음 → -t 0건 매칭'은 시끄러운 FAIL(비0 exit) 방향이라 안전하나 회귀 고정은 미완 |
| 티켓 식별자 원장(팀 워크플로우) | 티켓 통합의 식별자 원장(`_workspace`/repo의 append-only JSONL, `ledger.mjs`)을 설계가 "왕복 정본·1차 멱등 가드"로 격상했다(runner가 재청구를 원장 우선으로 차단, GitHub 색인 지연 우회). **이는 이미 §4에 등록된 "재개 매니페스트 outputs 자기선언"·"재개 매니페스트/planLock" 항목과 같은 tamper 클래스** — append-only 원장을 삭제 후 재구성하면 청구 이력을 위조할 수 있다(tamper-evident이지 tamper-proof 아님). | 라이브 실증(2026-08-21)에서 원장-우선 가드가 색인 지연 중복 발행을 막음을 확인 — 정본 격상의 실효는 있으나 tamper 대응은 미구현 | **부분 배선(2026-08-23 갱신)**: 안전 append writer(`ledger-writer.appendLedgerRecord`)는 존재하며 이제 공유 `evidence-log-lib.mjs`(`appendEvidenceLine` + 도메인 `validate` 훅)로 이행됐다 — O_NOFOLLOW(POSIX)·0o600·1MB cap을 다른 세 원장과 **동일 관용구**로 공유한다(재발명 종식). **최초-digest 가드 배선 완료(2026-08-24, executor CLI가 첫 writer 소비자)**: 청구(create) 경로는 `appendClaimRecord`가 공유 `detectRebind`로 같은 FEAT의 최초 청구 contentHash와 다른 재청구를 `LEDGER_REBIND_REFUSED`로 loud 거부한다(within-file, 회귀 고정). 정당한 계획 갱신·prUrl 링크는 비가드 `appendLedgerRecord` 경로(emit update/멱등 판정 경유 — 경로 분리가 계약). 파일 통삭제 재구성은 여전히 못 잡음(tamper-evident 한계, 외부 앵커 없음 — 명시적 리스크 인수 유지). 크로스-머신 원장 분기(개발자별 로컬 원장)는 트래커 라벨을 사후 dedup 기계 키로 삼는 게 정본 — 미구현 |
| change-scope fenced JSON self-attestation(팀 워크플로우 link STALE 게이트) | `cli.mjs runLink`의 STALE 완료 차단은 `_workspace/03_dev/change-scope.md`의 fenced JSON(픽업이 발급)을 되읽어 원장 청구 해시와 대조한다(2026-08-24 등록, 리뷰 HIGH). **파일은 개발자가 편집 가능한 self-attestation이다** — sourceDigest를 현재 계획 해시로 고쳐 쓰면 STALE 차단을 우회할 수 있고, 파일을 지우면 대조 자체가 불가능해진다 | — (실측 우회 아직 없음 — 예방 등록) | fail-open 금지: 대조 미수행(파일 부재·훼손·타 FEAT)은 침묵 스킵이 아니라 `staleCheck: not-performed`로 정직 표기하고 **confirm 경로는 차단**(`stale-check-unavailable`), 통과는 `--accept-unverified-scope` 명시 인수뿐(회귀 고정). 조작 우회는 남는다 — 이 게이트는 성실 경로의 STALE 방어이지 위조 방어가 아니며, 위조는 CLAUDE.md 비협상 규칙+PR 리뷰(사람) 몫. 픽업의 활성 change-scope 덮어쓰기는 `--replace-scope` 명시 없이 차단(STALE 앵커 침묵 소실 방지) |
| 브랜치 레지스트리(마커·라벨) — 팀 워크플로우 §4-1 | 작업 브랜치 발견의 정본은 이슈 본문 왕복 마커의 `branch=` 필드, 라벨 `branch:*`는 근사 인덱스다(2026-08-24 등록, 소비자 `overview.discoverBranchRegistry` 생김). **둘 다 프록시다**: (a) 마커는 이슈 본문 self-declaration이라 브랜치 rename 후 stale해도 기계가 못 잡고, (b) 라벨은 50자 초과 생략·스탬프 도입 전 구세대 무라벨·triage 탈부착 가변으로 마커의 부분집합이며, (c) `origin/<br>` 존재 판정은 마지막 fetch 시점 스냅샷이라 소실/신생 브랜치를 오판할 수 있다 | — (실측 우회 아직 없음 — 등록은 예방적) | union 집계(마커-only·라벨-only 각각 등록 + 소스 태그)로 침묵 누락을 막고, 마커↔라벨 불일치는 조용히 한쪽을 고르지 않고 `mismatches`로 노출(회귀 고정: test-ticket-overview). phantom 후보(불일치 이슈의 라벨측 브랜치)의 경고 표시는 소비자 계약으로 명세. fetch 신선도 전제는 코드 주석·설계 §4-1에 명시 — 배선 커밋에서 fetch --prune 선행 여부 결정. rename-stale 마커의 기계 검증은 미해결 TODO(브랜치 존재 대조가 부분 방어) |
| 픽업 시 self-assign 소유권 판정(청구≠픽업) | `assign.mjs`의 `computeAssignmentPlan`은 호출 시점 스냅샷(`issue.assignees`)만 보는 **순수** 판정이다(2026-08-21 등록, 리뷰 지적). 순차 경합(이미 남이 배정)은 `taken`으로 차단하나, **동시(TOCTOU) 경합은 못 막는다** — 두 개발자가 같은 미배정 이슈를 동시 조회하면 둘 다 `assignable`을 받고, 실행부 `assignArgs`(`gh issue edit --add-assignee`)는 additive(compare-and-swap 아님)라 둘 다 성공해 다중 배정이 남는다. 발행 경합(claim-race)의 원장-우선 가드와 **같은 클래스**다 | — (실행부 미배선 — core-only, 실행 blast radius 0) | **완화 구현(2026-08-24, executor CLI 배선)**: `cli.mjs runPickup`이 조건 두 개를 이행 — assign **직전 재조회+재판정**(선점됐으면 양보, assign 미도달 회귀 고정)과 **사후 다중배정 감지**(2인 이상이면 `multi-assign-detected`로 되돌림 — 선착 판정은 자동화하지 않고 사람 조율, 정직). 잔여: 재판정~assign 사이 미시 창(gh에 CAS 없음 — 사후 감지가 그 창의 방어). 순수 코어 주석·설계 doc §3에 한계 공시 완료. 근본적으로 gh add-assignee가 CAS를 제공하지 않는 한 원장-우선 가드(로컬 sync-consistent)가 1차, 트래커 배정이 2차인 claim-race와 동일 관용구를 재적용한다 |
| 티켓 본문 인젝션 정규식 스캔(B 픽업) | `pickup.mjs`의 `scanUntrustedBody`는 비신뢰 이슈 본문에서 제어토큰(`ALLOWED_PATHS` 등)·하네스 내부경로·오버라이드 지시·파괴/실행을 **키워드+40자 윈도우 정규식** 4종으로 잡아 `INJECTION_SUSPECT`로 플래그하고, `pickupTicket`이 이를 개발 진입 fail-closed 차단으로 소비한다(2026-08-21 B단계 등록). **정규식은 프록시다** — 동의어·줄바꿈 분산·유니코드 치환·패턴 밖 표현은 미탐(false negative) 가능하다. 반대로 정상 스펙이 우연히 키워드를 포함하면 오탐(false positive)으로 정당한 티켓을 되돌릴 수 있다 | — (실측 우회 아직 없음 — 등록은 예방적) | **차단은 최종 방어선이 아니라 사람 확인의 보조 신호**로 명세(코드 주석·설계 doc §B). 이중 방어: (1) suspect면 change-scope 미발급(개발 진입 차단), (2) 통과한 본문도 change-scope의 `TARGET_BEHAVIOR`에 raw가 아니라 fence+"지시 아님" 라벨의 격리 발췌로 실어 dev agent가 지시로 해석하지 않게 함(untrusted-content-quarantine Rule 2). 회귀 고정: clean 왕복+본문 인젝션 조합이 `injection-suspect`로 차단됨을 test-ticket-pickup이 실행. 패턴 밖 인젝션의 의미 탐지 승격은 미해결 TODO — 이 스캔은 표면 신호이지 의미 판정이 아니다. 에이전트 마커 사슬(`validate-content-policy`)과는 별개 층(그쪽은 `.md` 에이전트 producer/consumer 강제, 이쪽은 스크립트 코드 경로 강제) |
| 타일 방향 게이트의 skip 예외 | `design-approval-contract.md` 0단계는 "레퍼런스·시안 기지정 또는 기존 디자인 시스템 승계"면 후보 타일을 건너뛰게 하는데, **그 판단이 정당한지 검사하는 validator가 없다**(2026-08-20 적대 리뷰 등록). 오케스트레이터가 매 라운드 자기선언으로 스킵할 수 있고, 의무인 decision-log 기재 여부조차 기계가 확인하지 않는다. 같은 파일의 기존 "사용자가 명시 skip하면 프리뷰 루프 생략"과 동일한 honor-system 클래스 | — | 게이트 신설 취지는 비용(방향 기각을 프리뷰에서 받으면 630k~681k vs 타일 132k)이지 안전 하한이 아니므로 스킵의 blast radius는 예산 한정. decision-log 기재 존재의 기계 검사는 미해결 TODO — 스킵 사례가 실제로 관측되면 승격 검토 |
| 델타 라운드의 앵커 드리프트 | `design-preview-builder` 규칙 12(개정 라운드 = 델타 수정)에서, 델타로 DOM 앵커를 추가·삭제·개명하고 `traceability.json`을 갱신하지 않으면 **어떤 기계도 못 잡는다** — `design-preview-status-lib.mjs`는 traceability의 내부 정합성(스키마·selector 형식 `[data-wh-anchor="…"]`·featureId 참조)만 검사하고 **선언된 앵커가 실제 프리뷰 HTML/JS에 존재하는지는 대조하지 않으며**, `--write-source-snapshot`은 입력 스펙 digest만 고정한다(출력 DOM 아님). 즉 R3/R4의 "앵커 21/21 무결"은 builder self-report였다(2026-08-20 적대 리뷰가 미확인으로 제기 → 코드 확인으로 확정) | 사후 수동 대조(2026-08-20): search-portal 선언 앵커 21종 전부가 프리뷰 소스에 존재 — 이번 델타 2라운드에서는 드리프트 **미발생**. 다만 이 확인은 사람의 grep이지 게이트가 아니다 | 규칙 12가 `traceability.json`을 "안 건드리는 파일" 예시에서 빼고 **앵커 변경 시 갱신 의무 + 기계 미대조 사실**을 명시(2026-08-20). 앵커↔DOM 구조 대조의 validator 승격은 미해결 TODO — 델타가 기본이 된 만큼 드리프트 노출면이 커졌다 |

| T1 receipt(t1-summary.json)의 트리 결속 | `validate-certified-evidence`는 receipt의 `status` 문자열만 본다 — `declaredRevision`·`sourceFingerprint`가 **커밋된 골든 트리·승격 커밋과 일치하는지 어떤 기계도 대조하지 않는다**. 설계상(runbook B-6→C-1) receipt는 승격 *전* 트리(compatible 스팩 확정)에서 나오고 승격 커밋이 locked profile을 재확정하므로, 첫 승격 커밋의 receipt는 항상 stale fingerprint를 담는다. 커밋 바이트의 provenance(GitHub artifact ↔ 러너 디스크 사본)도 self-report다 | 첫 실전 승격(hybrid, 2026-08-23, 리뷰 지적): receipt는 48b96b3(compatible 스팩 확정)에서 산출, 승격 커밋은 adapter sha·profile sha 재확정 → fingerprint `d7ec0014…` 불일치 상태로 게이트 green | 승격 SHA로 `hybrid-t1` 재dispatch → receipt 교체(declaredRevision = certified 스팩 확정 트리)를 승격 직후 의무로 runbook에 명시. artifact sha256 대조 기록은 JUDGMENT/runbook 몫 — hybrid 첫 receipt는 사후 대조 완료(artifact zip 내 sha256 `b3c24f72…` = 커밋본, 2026-08-23), **B-7 재실행(run 32636870973)으로 receipt를 승격 후 트리(97936f1) 기준으로 교체**해 stale 해소(artifact에서 직접 채취, sha256 `bccfd5fb…`). 단 "일치"는 사람이 수행·기록한 것이며 커밋 트리 fingerprint·revision 대조 validator 승격은 미해결 TODO |
| eval receipt 커밋 규약 | web 카탈로그(`scenarios.json`) receipt는 실행 중 인라인 검증(run-eval-executor의 verifyResult — runDirectory 컨텍스트 필요)에 의존한다. **커밋된 receipt만으로 사후 독립 재검증하는 표준 명령이 없어**, 커밋 후의 web receipt 내용은 self-report다(ai 카탈로그는 `run-ai-evals.mjs --verify-result`로 JSON 단독 재검증 가능 — 비대칭) | — (규약 신설 시 리뷰 지적으로 예방 등록, 2026-08-23 — receipt 0건 상태) | receipt에 실행 메타·검증 로그 요약 포함 의무는 규약 몫(evals/README). web receipt 독립 재검증 CLI 승격은 미해결 TODO — 첫 web receipt 배치 전 구현이 이상적. maturity 라벨과 receipt의 기계 결속도 §"maturity의 eval-언급 검사" 행의 승격 조건으로 연동 |
| CI 워크플로 미러 동기화 검사 | `CI_MIRROR_DRIFT`(validate-workflows-and-evals)는 `.claude/ci/*.yml` 중 `.github/workflows/<name>` **활성 미러가 존재하는 파일만** 바이트 대조한다 — 미러가 0개면(또는 `.claude/ci`가 비면) 아무 것도 검사하지 않고 pass가 나는 vacuous 조건이 있다. 제안본 자체의 내용 품질은 워크플로 보안 검사(inspectWorkflowSecurity) 몫 | — (도입 시 리뷰 지적으로 예방 등록, 2026-08-23) | 활성화 절차(ci-activation-runbook)가 미러를 배치하는 순간부터 드리프트가 기계 강제된다. pass 메시지가 검사한 미러 개수를 노출하므로(`N activated … mirror(s)`) 0개 상태가 침묵 통과로 보이지 않는다 |

| source/deployed 판별자(`detectSourceRepository`) | `.claude/deployment.json` 존재 **또는** settings 2종 byte 동일이라는 **위조·실수 가능한 마커**로 source-only 검사군을 일괄 게이트한다. 2026-08-25 배선 확대로 blast radius가 2개 validator → 5개 검사(adapter-hygiene source 검사·contract-hygiene 판단 계층·console CI 배선·certified 실트리·agent reachability)로 커졌고, 이 중 contract-hygiene·certified는 I1 anti-tamper 게이트다. source repo에서 마커가 성립하면 **5개가 침묵 속에 꺼진 채 CI green**이 된다 — 카운터 불변식이 없다 | — (실측 우회 없음 — 적대 리뷰 지적으로 예방 등록, 2026-08-25) | **미해결 TODO 3종**: (1) 모순 fail-closed — settings byte 동일 && `deployment.json` 부재 → FAIL, deployed 판정 && `golden/`·`docs/protected-core.md` 실존 → FAIL(deploy는 이들을 절대 복사하지 않는다). (2) 배포 후 라벨 변조 탐지 — `deployment.json`에 배포 시점 supportLevel 스냅샷을 실어 배포본 재검증이 대조. 배포본의 seed 무장은 검사기 로직 증명이지 라벨-증거 결속이 아니다. (3) agent reachability 과다 범위 — 스킬은 배포되므로 스킬-참조는 배포본에서도 검사 가능하다. `deployment.json`에 운영자-전용 소비 에이전트 목록을 실어 그 목록만 면제 |

| A/B outcome 자기보고 3분류(`analyze-ab`) | `outcome`은 완결성 게이트의 판정 결과라(`execution-budget-contract.md`) **OFF 암은 계약상 outcome을 생략한다** — 주 엔드포인트가 telemetry 경로로는 ON 암에서만 측정되는 **차등 측정**이다. 미상 제외는 MAR 가정이며, crash한 스폰일수록 미기록 개연성이 높으면(MNAR) 미완율을 **과소평가**한다(게이트에 유리한 방향). `UNKNOWN_RATIO_INVALID=0.2`는 **미교정 임의 임계**이고 검사가 run 단위라 임계 이하 미상이 여러 run에 누적돼도 `interpretable: true`로 남는다 | 실측(2026-08-26): OFF run 스폰 3개 전부 `outcome: undefined` — 초판 분석기는 이를 완주로 세어 "OFF 미완율 0%"가 나올 뻔했다(fail-open) | 미상 3분류·분자분모 제외·`DATA_INVALID` 표시로 1차 차단(회귀 반증 테스트 고정). **미해결 TODO**: 실해소는 executor.log·산출물 실존에서 **암 독립 outcome 도출**(grader와 같은 관용구). 그 전까지 telemetry `outcome`을 주 엔드포인트로 쓰는 Stage 1은 착수 금지 |
| A/B 암 준수 검증(`checkArmCompliance`) | executor.log 문자열 매칭이라 게이트를 다른 경로로 호출하면 놓친다 — 오염 탐지 하한이지 준수의 증명이 아니다. 2026-08-26 적대 리뷰로 **구조적 오탐**도 발견: 로그 첫 줄 `# command:`가 프롬프트 전문을 에코하는데 OFF 지시가 3게이트 이름을 명시해, 헤더를 세면 **모든 준수 OFF run이 자동 폐기 판정**을 받았다 | 실측(2026-08-26): aborted ON run 로그 헤더에서 프롬프트 에코 확인(스폰 0 run이라 본문 출처가 아님) | 헤더 블록 제외 + 이름 언급이 아닌 **실호출 형태**(`node <경로><gate>.mjs`)만 집계로 수정. 현실적 OFF 로그 fixture로 회귀 고정(3종) |

| 에이전트 model/effort 프런트매터 계층 | 99종의 모델 티어 배정(sonnet 72/opus 21/haiku 4/fable 2)과 opus 티어 `effort: xhigh`는 **repo 실측 실패 데이터가 아니라 모델 포지셔닝 기반 판단**이다 — 실측 지지는 `client-domain-state-builder` runaway 1건뿐이다. `validate-harness`는 `model` 필드의 **존재만** 검사하고 값도 `effort`도 읽지 않으므로 오타·미지원 값·드리프트가 조용히 통과한다. `effort`는 런타임 반영 자체가 미관측이다(**설정 ≠ 효과**) — 스폰이 뜨는 것만 확인했다 | 실측(2026-08-26): 같은 날 사용자 지시로 38종을 fable로 상향했다가 포지셔닝 근거로 반전 — 배정이 증거가 아니라 판단·비용 인센티브에 흔들림을 자기 실증 | 배정 변경은 적대 리뷰 필수 + JUDGMENT에 근거 서열 명기. **미해결 TODO**: (1) `effort` 실적용 관측(스폰 텔레메트리), (2) `model`·`effort` 값 자체의 기계 검증. 오배정의 최종 진실은 파일럿 결과·완결성 게이트 텔레메트리이며, 티어 하향 에이전트(haiku 4종)의 산출 결함은 이 행에 추가 등록한다 |

| 프로필 감지의 요구 패키지 근거(declared ∪ workspace) | 워크스페이스 루트 선언을 요구 패키지 충족 근거로 인정한다. **멤버십 glob을 확인하지 않는다** — `pnpm-workspace.yaml`의 `packages:` 패턴에 해당 프로젝트가 실제로 속하는지 검사 없이 상위 manifest를 채택한다. `.git` 보유 디렉터리에서 탐색을 멈추지만 그 안에서는 최대 6단계를 오른다 | 실측(2026-08-26): 모노레포 패키지가 `vite`를 루트에 선언해 감지 실패 → workspace 근거로 해소. **lockfile 근거는 적대 리뷰에서 기각** — 설치 증거를 채택 증거로 오귀속해 webpack+vitest 앱이 react-vite-spa로 조용히 오탐된다(AMBIGUOUS는 2개+ 매칭에서만 발화). 기각된 설계는 음성 테스트로 고정 | evidence 문자열의 근거 출처 표기(`package:vite@workspace`)가 사후 진단 채널. forbidden도 declared ∪ workspace로 대칭 유지(호이스트 우회 차단). **미해결 TODO**: 워크스페이스 멤버십 glob 확인 |

| solution-design 자기보고 표면(Stage 0) | `_workspace/02_design/solution-design.md`는 **소비 validator 0개**인 관측 산출물이다 — `source: measured\|proposed` 구분(추론값을 measured로 위장해도 미탐), §1 경계(구현 절차 금지 — 산문 파이프라인을 이 파일로 옮겨도 미탐), `STAGE: 0` 라벨, 기계 판독 블록의 "형식 임의 변경 금지"가 **전부 self-attestation**이다 | — (실측 우회 없음 — Stage 0 신설 시 예방 등록, 2026-08-26) | Stage 0 동안 소비자 0으로 한정하는 것이 §8 계약(게이트·소유권·프로필·빌더 순서 불변). **Stage 1 승격 전제조건**: (1) `measured` 선언의 경로 실존 대조 validator, (2) 기계 판독 블록 형식 검사, (3) `source-artifact-ingestor`의 `02_design` 광역 소유권 carve-out — 스팩 확정 아티팩트 덮어쓰기 벡터다. 관측 결과는 receipt로 남긴다 |
| spec-lock (Stage 1 — 스팩 확정 아티팩트) | `_workspace/03_dev/spec.json`은 생성·형식 검증되나 **소비 게이트가 0개**다 — Stage 1에서 "스팩 확정"은 명칭이지 강제가 아니다(계약 §6 정직 표기). `source: "measured"`·`layerMap` 경로·`moduleBoundaries` scope의 **실존이 대조되지 않는다**. 전량 `assumed` 스팩은 사용자에게 실제 제시됐는지가 self-attestation이다. digest는 flat 8경로 고정이라 sharded 입력 변형을 포섭하지 못해 stale 판정이 fail-open 방향이다. 소유권 차단은 Edit/Write 채널 한정(default-deny)이고 스팩 확정 **입력**은 `source-artifact-ingestor`의 광역 소유권 안에 있다. `constitution.substrate`의 source 3분류도 같은 자기보고 클래스이며 더 강하다 — `measured`는 실존 대조 없이 **하네스 기본값을 이긴다**(위장 시 기본 substrate가 스팩 기록에서 조용히 대체). `SUBSTRATE_DEFAULT_MISMATCH`는 default 라벨의 정직한 오기만 잡는 **정합성 검사**이지 measured·declared 위장은 못 잡는다. `.claude/substrate-defaults.json`은 `tooling-scaffolder`·`validate-toolchain`·`ts-conventions`와 값을 공유하지 않는 **4번째 사본**이라 드리프트해도 기계가 침묵한다 | — (신설 시 적대 리뷰 등록, 2026-08-26. 리뷰 실측으로 스팩 확정 명령이 bash 정책에서 DENY 상태였음이 드러나 계약 등록으로 해소) | **Stage 0 행의 승격 전제조건 재판정**: (2) 형식 검사는 확정 시점 `lock-spec`이 수행 — **이행**. (3) ingestor carve-out은 lock을 03_dev에 배치해 **부분 완화**(입력 carve-out은 미해결). (1) `measured` 실존 대조는 **미이행 — Stage 2 게이트 전환의 전제조건으로 이월**. 1차 방어로 verifiable↔feature-plan 실존 결박·스키마-출력 결속 테스트·정책 fixture 3종을 넣었다. `isSpecStale` 배선과 ref ID 실존 대조는 **2026-08-28 해소**(ACCEPTANCE_REF_NOT_FOUND) — 적은 ID가 feature-plan에 실제로 있어야 확정된다. 나아가 verifiable 스팩은 그 TC가 **테스트에서 인용되는지**까지 대조한다(acceptanceCoverage, 선언된 testLayers를 읽는다). **남은 프록시**: 텍스트에 ID 문자열이 있는지만 보므로 주석만 달아도 통과한다 — 그 테스트가 실제로 그 기준을 검증하는지는 code-reviewer의 몫이다. unverifiable은 여전히 막지 않되 릴리스 매니페스트의 acceptance 요약에 무엇이 검증되지 않았는지 남는다. **substrate 전제조건**: 이를 읽는 **첫 소비자**(게이트 전환·scaffolder 통합) 이전에 (a) `measured` 실존 대조(lockfile·설정 실측), (b) defaults ↔ `validate-toolchain` pin 정합 테스트가 선행돼야 한다 — Stage 2 이월. 현재는 소비 게이트 0이라 blast radius가 스팩 파일 기록에 한정된다(미통합이 역설적으로 폭을 좁힌다). 미지 키 `source: default` 위조 채널은 폐쇄 완료. **Stage 2a 재판정(2026-08-26)**: `validate-spec-conformance.mjs`가 layerMap 실존·루트 이탈, libraries·substrate의 `measured` 대조, staleness 소비를 기계화했다 — `isSpecStale` 배선과 layerMap 실존은 **이행**, `measured` 대조도 이행이나 **대조 불가 형태는 unverifiable로 보고**된다(도구명이 npm 패키지명과 다르고 aliases 미등록인 경우). substrate ↔ toolchain 정합은 **7키 중 packageManager 1키만** 대조하므로 **부분 이행**이며 나머지 6키는 pin과 결속되지 않는다 — 기준값은 리터럴이 아니라 `substrate-defaults`에서 파생하고, 두 파일의 드리프트는 텍스트 결속 테스트가 잡는다. `SUBSTRATE_EVIDENCE`는 **수기 근거표**라 미등록 도구명은 오탐 FAIL(fail-closed 방향) 또는 unverifiable이 된다. **소비자는 산문뿐**이다 — Phase 3 전 계약 경유이며 release gate·validate-harness가 호출하지 않는다. 게이트가 아니며 기계 배선은 Stage 2b 몫이다. **형태 대조(2026-08-26 조사 반영)**: `targetShapes`는 배열이며(라이브러리+CLI 조합이 정상 패턴) `bin`·`exports`/`main`·`private` 신호와 대조된다 — 대조 없이 형태가 게이트를 고르면 형태 자기보고 하나로 검증 세트 전체를 회피할 수 있다. 다만 `web-app` 등 규칙 미등록 형태는 unverifiable이며, `cli`·`library` 두 형태만 기계 근거가 있다. **형태 → 요구 검증(Stage 2b)**: `.claude/shape-checks.json`이 형태별 요구 검증을 정의하고 정합 검사가 evidence receipt 커버리지를 판정한다 — static 3종(`pack.publish-metadata`·`lib.public-api`·`cli.bin-entrypoint`)은 `validate-shape-checks.mjs`가 매니페스트·파일 실존만으로 판정해 receipt를 낸다. **runtime 3종(`pack.contents`·`cli.exit-codes`·`cli.stderr-errors`)은 실행 관측이 필요해 미구현**이며 quality runner 바인딩이 없다 — 이들은 `required`가 아니라 `unimplementedChecks`로 분리 보고된다. **하네스가 못 하는 것을 프로젝트 실패로 보고하지 않는다**는 규율이며, 반대로 미구현을 통과로 만들지도 않는다. 정적 검사가 runtime을 흉내내지 않는 것도 같은 규율이다(정적 근사 PASS는 프록시). **Stage 2b 릴리스 배선(2026-08-26)**: `validateReleaseGate`가 `inspectSpecConformance`를 호출해 스팩 확정 존재 시 FAIL을 릴리스 errors로 올린다. 스팩은 opt-in이라 `NO_SPEC`는 무발화이며(기존 프로젝트 무영향, 회귀 고정) unverifiable은 실패로도 통과로도 바꾸지 않는다. 손상된 스팩은 `INVALID_SPEC`으로 loud fail한다 — 초안은 파싱 실패를 부재로 강등해 **파일 한 바이트만 깨뜨리면 결박이 꺼졌고 보고까지 거짓이었다**(적대 리뷰가 잡음, `live.json` INVALID 판례 재적용). **남는 한계 3종**: (a) **해소(2026-08-26)**: append-only 원장(`spec-ledger.jsonl`)에 스팩 확정 자신의 해시를 기록해 사후 수정(`SPEC_TAMPERED`)과 삭제(`SPEC_DELETED`)를 잡는다. **잔여 한계**: 원장도 파일이라 스팩과 함께 지우면 탐지되지 않는다 — 로컬 신뢰 모델의 명시적 리스크 인수이며(티켓 원장과 같은 판단) 실질 방어는 원장이 git에 커밋되어 삭제가 히스토리에 남는 것이다. 원장 부재는 실패가 아니라 결박 부재로 보고된다. (b) **해소(2026-08-26)**: `golden/vite-serverless-hybrid`를 `system-architect` 실측 설계로 실제 확정했다 — 첫 실사용 스팩이며 이 배선이 fixture 밖에서 처음 발화한다(대조 중 스팩 확정 임시 삭제에 `SPEC_DELETED` 실발화). **실사용에서 드러난 것**: 형태 **생략** 우회 — `checkTargetShapes`는 "없는 형태 주장"만 FAIL로 잡고 "있는 형태 미선언"은 note였다. 합집합 규칙은 형태를 **더하는** 것만 안전하게 만들지 **빼는** 것을 막지 않는다. `cli.bin-entrypoint`·`pack.publish-metadata`·`lib.public-api`는 형태 층이 **유일 기계 경로**임을 전수 확인해(어댑터 DAG에 없음) 배포 패키지(`private !== true`)에 한해 FAIL로 승격했다. **예외 2종(등록)**: ① `private:true`의 `bin`·진입점은 note로 강등한다 — 배포 차단이 실기전이라 우회하면 배포 능력을 스스로 잃지만, **비레지스트리 배포(git URL·tarball)는 private과 무관**하므로 이 강등이 포섭하지 못하는 채널이 남는다. ② 초안은 private+진입점이 **완전 침묵**이었다(적대 리뷰가 잡음) — 워크스페이스 형제 패키지가 소비하면 실소비자가 있다. 대칭 note로 해소. **오탐 후보(미관측)**: `npm init` 기본 스캐폴드(`main:"index.js"`, private 미설정)를 확정하면 library 미선언 FAIL이 난다 — 스팩이 opt-in이라 폭은 좁고, 실사용 스팩 확정 2호에서 관측되면 재판정한다. **되돌린 설계**: "선언 형태가 전부 카탈로그 밖이면 FAIL"을 만들었다가 되돌렸다 — 프로필 실행계획이 `vite.build`·`vite.browser`를 **독립 요구**함을 실측했고(형태 층은 가산이지 유일 경로가 아님), 하네스는 지어낸 `banana`와 아직 모르는 정당한 `browser-extension`을 구별할 수 없어 FAIL이 하네스 공백을 프로젝트 실패로 보고하게 된다. 되돌림 근거를 회귀로 고정했다. **실사용 스팩 확정 2호 시도(2026-08-26, 사내 SDK 모노레포 패키지)**: 스팩은 **미완**이나 형태 층의 나머지 절반(library static 검사)이 처음 돌며 결함 2건이 드러나 해소했다. ① `lib.public-api`는 `kind:"static"`인데 **대상이 빌드 산출물**이라 컴파일해 배포하는 정상 라이브러리가 빌드 전에 반드시 FAIL했다 — 미판정을 PASS로 승격하지 않는 규율의 **반대 방향**(미판정을 FAIL로 강등)이 뚫려 있었다. 진입점의 최상위 디렉토리가 통째로 없으면 receipt를 쓰지 않고 미빌드로 보고한다. **프록시 표기**: "최상위 디렉토리 부재 = 미빌드"는 근사이며, `dist/`가 있는데 파일만 빠지면 여전히 FAIL이다. 실판정은 runtime `pack.contents`(미구현)의 몫이다. ② `checkPublishMetadata`의 `.npmignore` 검사만 상대 경로라 **하네스 cwd**를 봤다 — 외부 project-root 검사에서 대상과 무관하게 판정이 갈렸다(오탐·누락 양방향). **정정(2026-08-26)**: 앞서 이 자리에 "브라운필드 실측이 에이전트 경로로는 구조적으로 불가능"을 등록했으나 **과장이었다**. `DENY_PATH_OUTSIDE`는 `CLAUDE_PROJECT_DIR` 밖을 막는 것이 맞고, 브라운필드는 **대상 repo에서 하네스를 돌리는 것**이 정상 사용이다. 2호 시도가 막힌 것은 web-harness 세션에서 외부 repo를 겨눈 **운영자 설정 오류**였지 하네스 결함이 아니다. 하네스 결함으로 기록했던 것을 철회한다. **추가 등록**: 배포 상태가 `private:true`/공개 이진법이라 **사내 registry 배포**라는 제3 상태가 없고, 그 위에 `license` 강제가 얹혀 오탐을 낸다. 모노레포 substrate 귀속은 dependency 근거가 union인데 config 근거는 package-only라 **비대칭**이며, 루트 도구를 패키지 substrate로 적어도 기계가 잡지 않는다. `acceptanceSource: "absent"`는 **미확인과 구분되지 않는다**(`measured` vs `measured-absent`와 같은 문제인데 여기엔 그 구분이 없다). **배포 메타 제거(2026-08-26, 트렌드 조사 근거)**: `profileBinding` 11필드 중 배포 메타 넷(`deploymentProvider`·`deploymentTarget`·`releaseTarget`·`selectedCapabilities`) 검증을 뺐다. 근거 — **Score·OAM**은 워크로드 스펙과 배포를 분리하며 배포 결정은 플랫폼이 실행하지 개발자 스펙에 담지 않는다. **Vercel·Netlify**는 2026년 배포 대상을 선언이 아니라 감지로 옮기고 있다. **SLSA build provenance**가 묶는 것은 `builder.id`·`buildType`·`externalParameters`이며 배포 대상은 그 필드가 아니다. 검증을 포기한 것이 아니라 관심사 혼입을 제거한 것이다. **정정(같은 날)**: 대체물로 `executionBinding`(shapeChecks·graph·spec 해시)을 만들어 커밋했다가 **되돌렸다**. 이유: `sourceFingerprint`의 제외 목록이 `04_qa`·`RELEASE`뿐이라 `_workspace/03_dev/spec.json`이 이미 그 안에 있고("확정하면 receipt가 stale이 된다"의 근거가 바로 그것), 검사 카탈로그가 늘면 그 receipt가 없어서 어차피 잡힌다 — **새 결속이 덮는 것이 기존 결속과 겹쳤다.** 되돌리면서 그 과정에서 드러난 것 하나는 남긴다: 배선을 시험하는 회귀가 없으면 배선은 조용히 끊긴다(검증 호출을 지워도 CI exit 0이었다 — lib 테스트가 lib을 직접 부르고 있어 배선을 안 지나갔다). **잔여**: `if (expectedProfile)` 조건부 스킵은 그대로다 — 어댑터를 지우면 남은 7필드도 통째로 미검증이 된다. 어댑터 삭제 전에 닫아야 한다. **어댑터 해체 진행(2026-08-26, ② 안 — 그래프만 남기고 나머지를 걷어낸다)**: 어댑터가 담던 넷 중 둘의 대체물이 **등가성 실측으로 증명**됐다. (1) `commands` → `resolve-commands.mjs`: 45개 중 42개가 `pnpm run <script>`였고 골든 2종에서 12+건이 문자열까지 일치한다. (2) `tasks` 그래프 → `derive-execution-graph.mjs`: 공통 8개 중 7개가 프로필 간 완전 동일했고 나머지 하나(`release.assemble`)는 evidence 합집합이라 자동 도출된다. **프로필마다 다른 것은 "어느 검사가 빌드 산출물을 쓰는가" 한 비트뿐**이며 그것을 `shape-checks.json`의 `needsArtifact`가 선언한다 — 빌드 산출물을 쓰는지는 **검사의 성질이지 스택의 성질이 아니므로** 프로필이 아니라 형태 카탈로그가 소유한다. 도출 규칙 넷: 정적 검사는 `dependencies.installed`, 빌드는 그 위에 `evidence.typecheck`, 산출물 소비 검사는 `artifact.built`, release는 evidence 합집합. **도출 초안의 실제 버그**: 접두만 떼자 `api.unit`과 `quality.unit`이 둘 다 `evidence.unit`으로 합쳐졌다 — 조용히 합쳐지면 하나만 돌아도 release가 통과한다. receipt 이름 별칭 + 충돌 loud fail로 고쳤고 회귀로 고정했다. **함께 이관**: `vite.production-mock-boundary`(어댑터 profile-specific → web-app 형태), `api.unit`·`api.guards`(→ `serverless-functions` 형태 신설). 2호에서 `unknownShapes`로 보고되던 형태가 이로써 실체를 갖는다. **미해결 2종**: (a) `ingestion.validate`는 `requires: ["external-ingestion"]` **capability 조건부**라 형태로 표현되지 않는다 — `shape-checks`에 조건 축이 없다. (b) receipt 메타(`profileId`·`adapterVersion`·`adapterSha256`)를 `receipt-validation-lib`과 릴리스 게이트가 검증한다 — 어댑터를 지우려면 이 결속의 대체가 선행돼야 한다. **아직 지우지 않았다** — `profile-policy-lib` importer 11개, `web-core/` 15파일, `validate-release-fixtures` 1420줄(단언 97건)이 남아 있고 등가성만 증명한 상태다. **정합 검사 축소(2026-08-26)**: `measured` 대조(libraries·substrate)와 `SUBSTRATE_EVIDENCE` 수기 근거표를 걷어냈다(454→338줄). **빌드가 돌면 vite가 있는 것이고 테스트가 통과하면 그 라이브러리가 있는 것이다** — 실행이 증명하는 것을 정적으로 흉내내면 중복이고, 흉내가 어긋나면 오탐이다. 이로써 §4에 등록돼 있던 두 항목이 **원인 제거로 해소**된다: (a) `SUBSTRATE_EVIDENCE` 오탐이 릴리스 블록으로 격상된 문제, (b) 모노레포 dep union / config package-only 비대칭(G1). 둘 다 정적 흉내의 부작용이었다. **남긴 것은 실행이 증명하지 못하는 것뿐이다** — `layerMap` 실존·겹침(소유권 공급원이며 아무도 실행하지 않는다), staleness·원장(기록 무결성), 형태 → 요구 검증 커버리지(TC는 "옳은 검사를 돌렸는가"를 자기 자신에 대해 말할 수 없다), `checkToolchainAlignment`(실측 대조가 아니라 스팩↔하네스 정책의 모순 검출). **대체 경로**: 걷어낸 "결정을 따랐는가"는 `code-reviewer`가 `spec.json`을 읽어 대조한다 — libraries·moduleBoundaries·nonGoals·layerMap 어휘·architecture. 정적 표가 못 하던 판단("zustand로 정했는데 useState로 흩어놨다")을 코드를 읽는 쪽이 한다. 검증이 셋으로 갈린다 — **TC=행위 · 정합=기록 · 코드리뷰=결정 준수**. **잔여**: 코드리뷰는 에이전트 판단이라 기계 재현성이 없다. 정적 대조를 없앤 대가이며, 스팩↔코드 불일치 시 어느 쪽이 옳은지 판정하지 않고 둘 다 보고하도록 계약에 못 박았다. **진입 경로·근거 티어(2026-08-26)**: 스팩 확정을 별도 의식에서 **작업 진입에 결박**했다 — 그린필드는 요청에서 추론(`inferred`), 브라운필드 첫 작업은 기능 작업 전 실측(`measured`), 기능 추가는 갱신, 단순 수정은 기획·디자인만 건너뛰고 스팩은 없으면 만든다(계약 §0-1). `source` 티어에 `inferred`·`confirmed`를 넣어 **실측 → 추론 → 질의** 순서를 표현한다 — 이전에는 사실상 "실측 아니면 거부"라 읽지 못하면 추측·중단 둘뿐이었다. **질의 왕복**: 설계자는 서브에이전트라 직접 묻지 못하므로 `openDecisions`를 `status:"open"`으로 남기고 멈추고, 오케스트레이터가 물어 `confirmed`(답함)·`assumed`(보류)로 닫는다. **실사용 확정 1호의 미결정 3건이 전부 `assumed`였던 것은 질문이 사소해서가 아니라 비대화형 서브에이전트에서 구조적으로 물을 수 없었기 때문**이다 — `spec.mjs`가 `open` 잔존 시 확정을 거부하므로 왕복은 기계로 강제되나, **묻지 않고 `assumed`로 적는 경로는 여전히 자기보고다**(에이전트 정의로만 막혀 있다). **줄 수 게이트는 프록시(신규 등록)**: `validate-modularity`의 참조 한도를 300→400줄로 상향했다(사용자 결정). 줄 수는 읽기 비용의 대리 지표이고 실제 비용은 바이트다 — **줄바꿈을 없애 같은 내용을 긴 줄로 밀어넣으면 게이트는 통과하는데 비용은 줄지 않는다**(이번 작업에서 실제로 그렇게 압축했다). `always-read`는 이미 contract-hygiene이 바이트로 재므로 참조도 바이트 계측으로 옮기는 것이 실해소다. SKILL.md·에이전트 한도는 300줄로 남겼다. **실사용 스팩 확정 2호 완주(2026-08-26, 사내 SDK 모노레포 `@kakao/ai-chatkit`)**: 배포되는 library × pnpm 모노레포라 형태 층의 나머지 절반이 처음 돌았다. 미결정 3건이 **전부 `confirmed`**다 — 1호가 전부 `assumed`였던 것과 대비되며 질의 왕복이 실제로 작동함을 보인다. **신규 공백 5종**: (G1) substrate 근거의 **dep/config 비대칭**이 모노레포에서 검증 공백을 만든다 — `collectDeclaredPackages`는 dep을 union하는데 `checkSubstrate`의 config 근거는 `--project-root` 하나만 본다. 대상에서 `eslint`·`prettier`·`typescript`가 패키지에 선언도 설정도 없이 루트에만 있는데 `measured`가 통과했다 — **union 한 줄에 걸려 있고 "모노레포 어딘가에 있다"까지만 증명한다**. 반대로 패키지에 설정만 있고 dep이 루트에 있으면 `lint`엔 `configs` 규칙이 없어 정당한 실측이 FAIL한다. (G2) **형태에 검증 적용 범위 개념이 없다** — 대상 `type-check`가 `include:["src"]`라 `playwright/` 14파일이 타입 검사에서 빠지는데 receipt는 exit 0이면 PASS다. 커버리지를 담지 않는 구조적 한계(I5 계열). (G3) `communication`·`concurrency`에 **`source` 티어가 없다** — `audio-worklet` 실측과 `web-worker` 부재 확인을 기계 블록에 남길 자리가 없다. `libraries`가 `measured-absent`를 둔 논리가 이 두 필드엔 적용돼 있지 않다. (G4) 모노레포에서 **어느 패키지에도 속하지 않는 코드를 아무도 보지 않는다** — 워크스페이스 `scripts/` 9파일 중 `testReactPackage.js`는 배포 직전 검증인데 `withinRoot`가 거부한다. project-root를 어디에 두든 남는다. (G5) **프로젝트가 하네스보다 앞서 있다** — `scripts/testReactPackage.js` + 소비자 픽스처가 미구현 `pack.contents`가 해야 할 소비자 계약 검증을 이미 하고 있다. 형태 층이 배울 패턴이 실물로 있다. **운영 실측**: `.git/info/exclude`로 대상 repo 추적 파일을 건드리지 않고 확정했다 — 대가로 원장이 git에 남지 않아 "삭제가 히스토리에 남는다"는 방어가 이 프로젝트엔 성립하지 않는다. **판정 계열 완결성 게이트(2026-08-26 해소)**: `maxTurns`에 걸린 서브에이전트는 **에러가 아니라 빈 보고로 끝나고 정상 종료와 반환 형태가 같다**(같은 날 3회 관측). Layer 1이 산문이라 오케스트레이터 자기보고였던 것을 `verify-spawn-completion --return`으로 기계화했다 — SPAWN_RESULT 마커 부재·조기 종료 문구·"무엇을 하겠다"로 끝나는 꼬리를 잡는다. `system-architect`·`harness-change-reviewer`의 `maxTurns`를 25→45로 올렸으나 그것은 증상 완화이며 **실해소는 절단이 자동으로 잡히는 것**이다. **잔여**: 마커는 에이전트가 스스로 붙이므로 마커를 붙이고 내용이 빈 경우는 못 잡는다(자기보고 잔존). 꼬리 판정은 정규식 프록시다. **잔여**: 스팩은 `_workspace/02_design`·`03_dev`에 놓이는데 이 경로가 `computeSourceFingerprint` 범위 안이라 **스팩 확정 행위 자체가 그 프로젝트 receipt를 stale로 만든다** — 스팩 확정 후 QA 재실행이 강제된다. 제외 목록 확대는 fingerprint를 약화시키므로 하지 않았고 미해결로 남긴다. (c) `SUBSTRATE_EVIDENCE` 오탐 FAIL이 산문 소비를 넘어 **릴리스 블록으로 격상**됐다 — aliases 미등록 도구를 measured로 확정하면 릴리스가 막힌다  **sharded 입력 해소(2026-08-30)**: 확정 입력의 flat 8경로 고정을 걷어냈다 — `x.md`가 없고 `x/`에 `.md`가 있으면 파일명 정렬로 샤드를 잇는다(`ticket/cli.mjs`의 `resolvePlanLocation`과 같은 규칙). 샤드는 **경로와 해시를 함께** 다이제스트해 파일을 쪼개거나 합치는 것도 변경으로 잡는다. track 실측이 계기다: 8개 중 7개가 `present:false`로 잠겨 (a) 기획·설계 변경이 staleness에 안 잡히고 (b) 수용 기준 85개가 실재하는데도 `ACCEPTANCE_SOURCE_WITHOUT_PLAN`으로 `acceptanceSource: feature-plan`이 거부돼 specTier가 unverifiable로 밀렸다 — **축이 없다는 이유로 검증이 면제되는** 형태였다. flat 프로젝트의 `combined`는 변경 전후 동일하다(기존 스팩·receipt를 stale로 만들지 않는다). **적대 리뷰 반영(2026-08-30)**: ① sharded 레코드가 `spec.schema.json`의 `additionalProperties:false`를 위반해 **동기 사례 자체가 스키마 밖 아티팩트를 만들 뻔했다** — `kind`·`shards`를 스키마에 넣고 깊은 키까지 보는 결속 테스트를 붙였다(top-level만 보던 기존 테스트가 못 잡던 클래스다). ② flat 불변은 분석 논증에서 **실행 증거**로 올렸다 — 변경 전 구현 스냅샷과 같은 combined가 나오는 것을 대조했고 그 값을 회귀에 pin했다. ③ 샤드 결합을 `path:hash` 개행 join에서 JSON 배열 직렬화로 바꿨다(파일명이 콜론·개행을 담을 수 있어 서로 다른 샤드 집합이 같은 문자열을 만드는 인코딩 모호성). ④ 기록·해시 경로를 NFC 정규화한다 — macOS(NFD)와 Linux(NFC)의 readdir 차이로 한글 샤드명이 팀 간 거짓 stale을 냈다. 읽기는 원본 이름으로 한다. **잔여**: 디렉터리 1단계만 본다(중첩 샤드 미포섭). `.json` 입력은 샤딩하지 않는다. 샤드 해소는 `LOCK_INPUTS`의 8개 논리 입력에만 걸리므로 그 목록 밖의 기획·설계 문서는 여전히 다이제스트 밖이다. **flat과 디렉터리 공존** 시 flat이 이기고 샤드 편집은 다이제스트 밖이 되는데, 그 방어는 lockSpec이 아니라 `validate-artifact-sharding`의 동명 공존 ERROR에 있다 — sharding 검사를 안 거친 확정 경로에서는 성립하지 않는 **교차 의존**이다. **심볼릭 링크**: `LOCK_INPUT_ESCAPES_ROOT`는 논리 경로 8개에만 걸리는 어휘 검사라 샤드 디렉터리·개별 샤드가 루트 밖을 가리키면 그 내용이 다이제스트와 수용 기준 색인에 든다 — flat 시절과 같은 클래스지만 대상이 고정 1파일에서 런타임 발견 N파일로 넓어졌다. **골든 stale**(이 변경과 무관한 기존 상태): `golden/vite-serverless-hybrid`의 커밋된 spec.json은 lock 커밋 이후 project-profile.json이 바뀌어 이미 stale이며 재확정되지 않았다  **중첩 프로젝트 root 비대칭(2026-08-30 실측·해소)**: `enforce-agent-ownership`이 `workspace/<project>/` 중첩에서 **쓰기 경로만 접두를 벗기고 스팩·범위는 하네스 root에서 읽었다.** 하네스 root에는 `_workspace/`가 없으므로 중첩 프로젝트는 스팩을 확정해도 `developer`가 `SPEC_MISSING`으로 영원히 막혔다 — 사용자에게 보인 증상은 '구현 스폰이 디스크 변경 0건으로 반려'였고, 계약 문구를 고쳐도 이 기제 때문에 풀리지 않았다. ALLOWED_PATHS도 같은 root에서 읽혀 **통째로 미적용**됐는데 그쪽은 조용히 **넓어지는** 방향이라 더 나쁘다. `ownershipRoot`를 한 번 도출해 셋 다 같은 root에서 읽는다. **함께 드러난 죽은 폴백**: JSON 배열 ALLOWED_PATHS 폴백 정규식이 `\*?\*?`만 허용해 따옴표 붙은 키(`"ALLOWED_PATHS":`)를 못 읽었다 — **배열을 읽으려고 만든 폴백이 배열에서만 죽어 있었다.** **근본 원인은 둘 다 같다**: `agent-registry`의 순수 함수에는 회귀 24건이 있는데 훅 **스크립트**를 실행하는 회귀가 0건이었다(§4에 이미 등록된 '배선을 시험하는 회귀가 없으면 배선은 조용히 끊긴다'의 재발). `test-ownership-hook.mjs`를 신설해 훅을 실제 프로세스로 돌린다(9건). **적대 리뷰 반영(2026-08-30)**: ① ALLOWED_PATHS 추출이 "문서에서 처음 나오는 곳"을 잡아 **승자가 편집 순서에 좌우**됐다(실물은 산문·JSON 펜스·마크다운 줄 3회 담는다) — ```json change-scope 펜스를 정본 앵커로 삼았다. ② 펜스 JSON 파싱 실패가 `[]`로 떨어졌고 빈 배열은 `intersectWithScope`에서 "제한 없음"이라 **파싱 실패가 범위 확대로 이어졌다**(fail-open) — 판정 불가면 block한다. ③ 줄 표기가 별표를 요구하고 폴백은 허용해 `ALLOWED_PATHS: a, b` 민무늬 표기가 어느 쪽에도 안 걸려 범위가 사라졌다 — 표기를 통일했다. ④ `ORCHESTRATOR_AUTHORED_ARTIFACTS`를 훅의 **강제 deny**로 승격했다: layerMap은 `_workspace/`를 금지하지 않고 레이어 패턴이 선행 세그먼트를 허용하므로, 스팩에 그 경로를 적으면 developer가 spec.json을 고쳐 **자기 소유권을 스스로 넓힐** 수 있었다(등록부 부재로만 표현되던 "아무도 소유하지 않는다"를 코드로 옮겼다). ⑤ 중첩 판정을 이름이 아니라 `workspace/<x>/_workspace/` **실존**으로 조건화했다 — 자기 소스에 `workspace/`를 가진 평면 프로젝트가 중첩으로 오인돼 자기 스팩이 무시되는 오탐을 막는다. **회귀 14건이 수정 전 코드에서 9건 실패함을 실행으로 확인했다**(동어반복이 아님의 조건). **잔여**: 중첩 판정은 `workspace/<한 단계>/`만 본다. `readSpecLock`의 `statSync().isFile()`은 O_NOFOLLOW가 아니라, `workspace/<x>`가 루트 안 다른 지점을 가리키는 심볼릭 링크면 쓰기는 통과하면서 스팩·범위는 링크 대상에서 읽힌다(Edit/Write로 링크를 만들 수 없고 bash 정책이 `ln`류를 막아 실효 벡터는 미확인)  **매니페스트 ↔ 스팩 결속(2026-08-30 실측·해소)**: 스폰 계획(build-manifest)이 스팩보다 **76분 앞서 잠겼는데 아무것도 그 순서를 보지 않았다.** 매니페스트는 `lib/parse-track-string.ts`(평면)를, 나중에 확정된 스팩의 OD-001은 `lib/parse/`(하위 디렉터리)를 말했고, 두 아티팩트가 어긋난 채 구현에 도달해 **개발 세션이 사용자에게 "어느 쪽이 정본이냐"를 물었다** — 사용자가 잠금 시각을 알 도리가 없으니 답할 근거가 없는 질문이다(판정 책임만 넘기고 근거는 주지 않는 형태). 셋을 넣었다: ① `SPEC_REQUIRED` — 소스에 쓰는 계획(`_workspace/` 밖 outputs, **자기선언이 아니라 outputs에서 도출**)은 spec.json을 전제조건으로 갖는다. Phase 2 설계 스폰은 산출물이 전부 `_workspace/` 안이라 걸리지 않는다. ② `PLAN_OUTSIDE_SPEC_OWNERSHIP`·`PLAN_OUTSIDE_MODULE_BOUNDARY` — 계획 경로를 layerMap·testLayers·moduleBoundaries와 **세그먼트 단위**로 대조한다(문자열 접두로 보면 `lib/parse`가 `lib/parse-track-string.ts`를 포섭한다 — 실측 결함이 정확히 그 형태). ③ planLock·원장에 `specDigest`를 함께 찍고, 스팩이 그 뒤로 바뀌면 `resume-manifest`가 `PLAN_LOCK_SPEC_STALE`로 fail-closed한다(sourceFingerprint↔receipt와 같은 규율). **계약 쪽**: `interaction-contract`에 "묻지 않는 것" 절을 신설해 정본 우선순위를 못박고, 판정 불가는 질문이 아니라 `BLOCKED`임을 규정했다 — always-read가 45,494→46,221B로 늘어 **baseline을 의식적으로 갱신**했다(사유: 이 규칙은 질문이 튀어나오는 그 순간에 있어야 발화한다. 기계 게이트는 한 사례만 막고 규칙이 클래스를 막는다). **적대 리뷰가 BLOCK을 냈고 반영했다(2026-08-30)**: ⓐ **결속 증거가 위조 대상 파일 안에만 소비됐다** — 원장에 `specDigest`를 쓰면서 읽지는 않아, `planDigest`가 `planLock`을 제외하는 성질을 이용해 **매니페스트에서 `specDigest` 키 한 줄만 지우면** 판정이 STALE→UNBOUND(경고)로 강등됐다. planLock을 매니페스트 안에만 두었다가 두 우회로 뚫렸던 판례의 재발이다 — 원장을 우선 증거로 읽는다. ⓑ **`NO_SPEC` 침묵 통과** — 결속이 있는데 spec.json을 지우거나 한 바이트 깨뜨리면 부재로 강등돼 exit 0이 났다(§4 `INVALID_SPEC` 판례와 같은 클래스). `SPEC_GONE`으로 fail-closed한다. ⓒ **훅과 판정이 갈렸다** — 자체 `withinScope`가 모노레포 선행 세그먼트를 못 덮고(정당한 계획을 REFUSE) 점이 든 디렉터리는 반대로 통과시켰다. **판정이 둘이면 "계획은 통과, 훅은 차단"이 나고 그것이 이 커밋이 고치려던 문제다** — `agent-registry`의 `layerPattern`을 export해 한 곳에서만 판정한다. ⓓ **비-developer 스폰의 구조적 오탐** — `environment-scaffolder`가 소유한 `package.json`·`vite.config.ts`·`docs/`는 layerMap에 있을 수 없는데(루트를 레이어로 못 만든다) 훅은 허용한다. 게이트가 그것을 막으면 오케스트레이터가 "이 스폰엔 게이트를 건너뛴다"를 학습해 게이트가 침식된다 — 등록부 소유권을 합집합으로 인정하고, 경계 검사는 **스팩 소유 영역 안에서만** 잰다. ⓔ `_workspace/../src/x.ts` 표기로 구현 분류가 꺼지던 우회를 정규화로 닫았다. ⓕ `specDigestOf`가 `spec.mjs`의 `specDigest`를 재구현하고 있었다 — 정본을 import한다(한쪽이 정규화를 도입하면 조용히 갈린다). ⓖ 배선 회귀 0건 지적에 `test-plan-spec-binding-wiring.mjs` 신설(두 CLI를 실제 프로세스로 8건, **수정 전 코드에서 7건 실패 확인**). **잔여**: `moduleBoundaries` 미선언 프로젝트는 경계 검사가 **미수행**이다(통과가 아니라 미검사로 보고). `UNBOUND`는 경고일 뿐 실패가 아니라, 결속 도입 **이전에** 잠긴 매니페스트는 여전히 스팩과 어긋난 채 통과할 수 있다. 설계 계획(산출물이 전부 `_workspace/`)도 `--lock`이 specDigest를 찍으므로 스팩 재확정 후 재개하면 STALE이 난다 — fail-closed 방향이라 남긴다. `validate-spawn-plan.mjs`가 400줄 관행을 넘었다(다음 확장 전 모듈 추출 검토) |
| 청구 범위 판정의 배선 부재(2026-08-30 실측·해소) | `claim-scope`의 의존 강등(`dependsOn`)·충돌 강등(`paths`)·계층 분류(`layer`)가 **파서가 만든 적 없는 필드를 읽고 있었다** — `plan-units.mjs`는 `{featureId,title,body,testCaseIds,type}`만 낸다. 세 판정이 처음부터 한 번도 발화하지 않았다(track 14단위 전부 `paths: undefined` → 충돌 검사 0건, `dependsOn: undefined` → 의존 강등 0건). 그 사이 설계 문서는 "FEAT-006이 FEAT-004/005를 필요로 한다"·"FEAT-006/007/008/009/011이 track-canvas를 공유한다"를 **산문으로** 적어뒀다. 순수 함수에는 회귀 다수, 그것을 먹이는 배선에는 0건 — §4에 두 번 등록된 그 클래스의 세 번째. | 해소: ① 파서가 `<!-- web-harness:unit feat=… dependsOn=… paths=… -->` 마커를 읽는다. ② **미선언과 "없음"을 구분한다** — 속성이 없으면 필드를 만들지 않고, `claimScopeReadiness`가 `deps-undeclared`로 막는다. 종전 `dependsOn ?? []`는 미선언을 곧바로 "의존 없음"으로 읽어 **선행 기능이 안 끝났는데도 착수 가능**으로 보였다(11건 pickupable, 실제 4건). 명시적 없음은 `dependsOn=none`. ③ `pickup`이 그 판정을 **강제**한다 — 종전에는 board만 강등하고 pickup은 보지 않아 강등이 표시일 뿐 게이트가 아니었다. ④ 충돌 검사 미수행을 `uncheckedForCollision`로 보고한다("충돌 없음"과 "검사 못 함"을 가른다). ⑤ `feature-planner`와 team-flow 계약에 선언 의무를 넣었다. | **한계**: ⓐ 선언은 **자기보고**다 — 계획이 적은 `paths`가 실제 쓰기와 같은지는 아무도 대조하지 않는다(스팩 `moduleBoundaries`·change-scope와의 3자 정합은 미구현). ⓑ `paths` 미선언은 **막지 않는다**(미수행 표기만) — 막으면 기존 계획이 전부 봉쇄되고, 의존 쪽이 더 높은 신호라 우선했다. 그 결과 track-canvas 5중 공유 같은 충돌은 여전히 조용히 통과한다. ⓒ 마커는 FEAT 섹션 본문에서 찾으므로 표(feature-list 테이블) 형식 계획에는 붙일 자리가 없다 — 그 형식은 애초에 unit 0개가 나오는 기존 한계와 같은 자리다. ⓓ 기존 계획은 전부 `deps-undeclared`가 되어 **한 번은 손으로 선언을 넣어야 한다**(마이그레이션 비용, track 14건). 그 비용은 한 줄이 아니다 — 마커를 넣으면 섹션 body가 바뀌어 `unitContentHash`가 변하고, **이미 청구된 티켓이 전부 shape-stale로 튕긴다**(선언 + stale 왕복). 또 `resolveMergedFeatures`는 원장에 `prUrl`이 링크된 레코드만 머지로 세므로, team-flow 도입 **전에** 이미 머지된 선행 FEAT는 영원히 merged에 안 잡혀 `deps-incomplete`가 풀리지 않는다 — 소급 claim+link 외에 해제 수단이 없다. **적대 리뷰 반영(2026-08-30)**: ⓔ 값 정규식이 공백에서 끊겨 `dependsOn=FEAT-004, FEAT-005`(쉼표 뒤 공백 — 사람이 가장 자연스럽게 쓰는 형태)에서 **두 번째 의존이 조용히 사라졌다** — 마커를 썼는데도 미머지 선행 위에서 착수 가능해지는 fail-open이다. 쉼표 뒤 공백을 허용하고, `key=value`로 읽히지 않은 잔여 토큰이 있으면 미선언으로 강등하며 사유를 싣는다. ⓕ `layer=foundation`은 `claimScopeReadiness`에서 **무조건 pickupable**이라, 막힌 팀의 가장 싼 우회가 "전 유닛 foundation 선언"이었다 — **마커에서 `layer`를 받지 않는다**(계층은 운영자의 `--foundation-roots`와 실제 경로에서 도출한다). ⓖ 마커가 둘이면 첫 매칭이 조용히 이겨 낡은 선언이 정정을 눌렀다 — 미선언으로 강등한다. ⓗ 선언된 `paths`를 ALLOWED_PATHS seed로 흘려 단방향 정합을 만들었다(플래그가 있으면 플래그가 이긴다) — 실제 쓰기와의 대조는 여전히 없다 |
| 개발 착수 관문(Gate 0, 2026-08-30 신설) | **왜 신설했나**: 하네스에 `planning-readiness`(Phase 1 진입)·`design-readiness`(Phase 2 진입)는 있는데 **개발 진입에는 관문이 없었다.** 대신 Gate A·B·C가 개발 *도중에* 터졌고, 기획·디자인을 다 승인한 사용자가 개발 중에 계속 막혔다. 2026-08-30 하루에 난 블록 8건을 "개발 전에 알 수 있었나"로 대보면 **전부 예**였다(스팩 부재·분할 기획 미포섭·중첩 root 소유권·계획↔스팩 충돌·lint 부재·브랜치 낙후·확정 결정 미설치·티켓 자산 미설치). 늦게 물어본 것뿐이다. | 원칙: **개발 단계에서 사용자에게 가는 질문은 없다.** 문서에서 도출되면 도출해 진행하고, 도출되지 않으면 선택지가 아니라 `BLOCKED`이며, 그 BLOCKED는 첫 줄 이전에 난다. **자기 개선 규칙**: 개발 중 BLOCKED는 이 관문의 버그 리포트로 취급해 항목을 추가한다 — 마찰이 단조 감소하는 것이 성공 기준이다. | **함께 고친 순서 결함**: `project-profile.json`·`integration-overlay.json`이 둘 다 `LOCK_INPUTS`인데 Phase 3에서 만들어졌다 — **확정하는 순간 낡도록 설계돼 있었다**(실측: 확정 11:48, 프로필 생성 12:53 → 즉시 stale, 모든 프로젝트에서 반드시 발생). 둘 다 스팩을 읽지 않으므로 확정 **앞으로** 당겼다(오버레이는 애초에 `system-architect`의 선언된 입력이다). stale 보고도 "입력이 바뀌었다"에서 **무엇이 어떻게**(새로 생김·사라짐·내용 바뀜)로 바꿨다 — 원인 없는 판정은 처방을 낼 수 없다. **한계(정직)**: ① 관문은 **검사 목록이지 완결성 증명이 아니다** — 여기 없는 원인으로는 여전히 개발 중에 막힌다(그때 항목을 추가하는 것이 규칙이다). ② 소유권 예행은 레이어당 **대표 경로 하나**를 찔러본다. 레이어 안에서 경로별로 갈리는 판정 (ALLOWED_PATHS가 레이어 일부만 허용)은 잡지 못한다. ③ `checkDecisionsApplied`는 `choice` 문자열에서 패키지명처럼 보이는 토큰을 뽑는 **프록시**다 — 서술형 choice("수기 variant 함수")는 검사되지 않고, 반대로 패키지명처럼 보이는 서술은 오탐한다. ④ 브랜치 낙후는 아직 항목에 없다(git 실행이 필요해 순수 판정에서 분리 중). ⑤ "분할 기획 미포섭"은 별도 축이 아니라 spec 신선도로만 잡힌다 — 스팩이 sharded 입력을 다이제스트하므로(0.7.6) 목록이 바뀌면 stale로 나오지만, 그것을 분할 문제로 명명하지는 않는다. **적대 리뷰 반영(2026-08-30, BLOCKED 판정 후 해소)**: ⓐ **티켓 흐름 구조적 오탐** — 훅은 developer 판정에서 layerMap을 change-scope의 ALLOWED_PATHS로 좁히는데 예행은 layerMap 전체를 찔러, 픽업 뒤에는 범위 밖 레이어가 전부 차단으로 나와 **정당한 티켓 개발이 진입 봉쇄**됐다. 실효 범위(범위가 있으면 교집합, 없으면 layerMap)를 예행한다. ⓑ **greenfield 시퀀싱** — `package.json`은 Phase 3의 scaffolder가 만드는데 진입 시점에 FAIL을 내면 "아직 돈 적 없는 단계로 되돌리라"는 처방이 됐다. 미수행으로 표기하고 scaffold 뒤 재실행한다. ⓒ **예행 배선 fail-open** — 훅은 `tool_name`·`agent_type`이 어긋나면 조용히 exit 0(허용)이라 스키마 드리프트 시 전 레이어 "쓰기 가능"이 됐다. 음성 컨트롤(반드시 차단돼야 하는 `spec.json`)을 넣어 "전부 허용"과 "배선 사망"을 구분하고, 주입 없이 실제 훅을 도는 배선 회귀를 넣었다(종전 12건은 전부 스텁이었다). ⓓ **자기 개선 규칙이 무한정이라 I2 침식 벡터**였다 — "개발 중 BLOCKED = 관문 버그"를 Gate A·B·C에 적용하면 정당한 차단을 탈정당화한다. **착수 전에 알 수 있었던 원인**으로 한정했다. ⓔ 신규 validator가 bash 정책에 미등록이라 에이전트 경로에서 `DENY_VALIDATION_COMMAND`로 막혔다(같은 diff가 그 규범을 스스로 적어놓고 어겼다) — 등록하고 `--fix`의 인자 계약을 좁혔다. ⓕ `choice: "none"`이 `none`이라는 패키지를 요구하던 자체 발견 오탐과 런타임 내장 선택의 탈출구(`provided: "built-in"`, 자기진술)를 넣었다. ⓖ 입력 **목록 자체**가 바뀌면 delta가 비어 "전부 새로 생김"이라는 반대 처방이 나가던 것을 분기했다. **잔여**: Gate A 2단계의 profile 재생성·Gate L의 outputLanguage 병합이 `project-profile.json`을 다시 쓰면 또 stale이 된다 — 계약에 재확정 의무를 적었으나 기계로 강제하지는 않는다. 모노레포 app-root 예행은 미실증 |

| 보조 skill 감지 → 스팩 유래(2026-08-26) | intake에서 7종 flag(`HYBRID_SERVERLESS`·`SERVER_DB`·`API_CONTRACT`·`MOCK_SERVICE`·`OAUTH_SERVER`·`I18N`·`OBSERVABILITY`)를 미리 감지했으나 **소비 지점이 전부 Phase 3(SKILL.md 199~206행)이고 그때는 이미 스팩이 있다.** 스팩이 더 정확하게 답한다 — `libraries.mock`은 선택뿐 아니라 대안과 근거 티어를 담고(2호 실측: `{choice:"none", alternatives:["msw","nock"], source:"measured-absent"}`), `targetShapes`가 hybrid 여부를, `communication`이 계약 형식을 담는다 | 실측(2026-08-26): 모드 16종 중 7종이 스팩 필드와 1:1 대응, 3종은 부분 대응, 3종(`AI_MODE`·`ANALYTICS_BUILDER`·`VISUAL_QA`)은 스팩에 자리가 없다 | 판단표를 `shape-routing-contract.md` §2-1로 이관하고 `companion-skill-detection.md`(8,644B)를 삭제했다 — 참조가 사라져 고아가 됐다. **스팩이 없으면 이 판단을 하지 않는다** — 감지로 되돌아가지 않고 스팩부터 세운다. **미해결**: `AI_MODE`는 라이브러리가 아니라 **위협 모델·승인 정책·비용 예산**을 바꾸는데 스팩에 그 자리가 없다. `ANALYTICS_BUILDER_MODE`·`VISUAL_QA_MODE`도 같다 — 이 셋은 감지가 정당하며 남는다. 부분 대응 3종(`LOCAL_DOMAIN_STATE`·`EXTERNAL_DATA_INGESTION`·`TIMESERIES`)의 이관 가능성은 미판정 | 진입 비용 66,363 → 66,285B(−78B) **→ 2026-08-27 정정**: 이 행이 "감지가 정당하다"고 남긴 flag 중 `AI_MODE`와 5개 submode는 AI 표면 전면 제거로 함께 사라졌다. 아래 "AI 표면 전면 제거" 행이 그 부분을 대체한다. |
| 진입 비용 계측 누락(2026-08-26 발견·수정) | `measureAlwaysReadBytes`가 **선행 로드 참조만** 세고 `SKILL.md` 본문을 세지 않았다. web-orchestrator에서 SKILL.md가 38,066B로 참조 합계(28,297B)보다 크므로 **공표된 진입 비용이 실제의 43%**였다 — I4 예산 게이트가 가장 큰 항목을 보지 않고 있었다. 오늘 하루 "진입 비용 28,297B"로 여러 번 보고한 것이 전부 그 절반 이하의 수치였다 | 실측(2026-08-26): 28,297 → 66,363B(2.3배) | SKILL.md 본문을 합산하도록 고치고 README 공표·baseline을 진실에 맞췄다. **발견 경위**: "AGENTS.md 권장 100~300단어 vs 우리 4,700단어" 대조에서 중복분을 실측하려다 잡았다 — **중복은 거의 없었다**(파일 간 동일 문장 1건 53B, 게이트 메시지 429개 중 산문과 일치 0건). 찾던 것은 없고 계측 자체가 틀려 있었다. **성격 구분**: AGENTS.md는 저장소 관례(컨벤션)이고 web-orchestrator 진입은 6단계 오케스트레이션(프로토콜)이라 같은 기준으로 잴 대상이 아니다 — 다만 ETH 연구가 겨눈 "중요한 제약이 소음에 묻힌다"는 분량 위험은 그대로다. 11,000단어 중 몇이 실제로 판단을 바꾸는지는 미측정 | 반증 등록부 8건째 |
| 서브에이전트 Bash 경로 탈출(2026-08-26 발견·수정) | "argv-only이므로 안전하다"가 참인지 실측했더니 **경로 탈출 두 종**이 있었다. (a) `--dir`·`-C`가 명시적으로 허용되는데 값이 프로젝트 안인지 보지 않아 `pnpm --dir /etc run lint`·`pnpm --dir ../../.. run lint`가 통과했다 — 그 디렉토리의 `package.json` script가 실행되므로 **프로젝트 밖 코드 실행 경로**다. (b) script 뒤 인자가 통째로 무검증이라 `--reporter ../../../etc/passwd`·`--outDir /tmp/evil`이 통과했다 — 러너가 그 경로를 파일로 읽거나 모듈로 로드한다 | 실측(2026-08-26): 우회 시도 10종 중 9종은 이미 차단(파이프·체이닝·셸 재진입·치환·네트워크·쓰기 도구), 2종이 통과 | `assertInsideProject`로 절대 경로·상위 탈출·홈 상대 경로를 막는다. **오탐을 경계했다** — 과하게 막으면 정당한 검증이 깨지고 그러면 우회 유인이 생긴다. 정당 명령 7종(`--dir packages/app`·`--filter @scope/pkg`·`--reporter=verbose`·`--outDir dist` 등) 통과를 회귀로 함께 고정했다. **프록시 표기**: 경로처럼 보이는 값만 검사한다 — 경로가 아닌 값은 통과시키므로 러너가 비-경로 인자로 위험한 동작을 하면 잡지 못한다. **적용 범위**: 이 정책은 서브에이전트에만 걸린다(main session은 사용자 감독하에 면제, 2026-07-27 승인) — 즉 오케스트레이터 Bash는 이 통제 밖이며 그것은 기존 등록 사항이다 | 회귀 5종, 반증 등록부 7건째 |
| 서명 provenance — 설계는 SLSA L2, 실증 0(2026-08-26 조사) | 업계 대조에서 "빌드 provenance 뒤짐"으로 적었으나 **부정확했다**. `quality-attestation-lib`이 keyId·서명·신뢰 attester·보호된 CI 정체성 결속을 갖추고 있고, `verifyQualityAttestation`은 `environment !== 'isolated-ci'`와 `executionContext !== 'isolated-ci-declared'`를 거부한다 — **호스트 receipt는 릴리스 증명 대상이 아니다**. 보호 컨텍스트 6종(`WEB_HARNESS_EXPECTED_TRUST_CONFIG_SHA256`·`REPOSITORY_ID`·`REVISION`·`WORKFLOW_REF`·`CI_ISSUER`·`CI_RUN_ID`)은 CI가 주입하며 빌드 스크립트가 위조할 수 없다 — SLSA L2의 "provenance는 빌드 스크립트가 아니라 플랫폼이 생성한다"와 같은 축이다 | 실측(2026-08-26): 보호 컨텍스트 부재 시 릴리스 final이 3건으로 **fail-closed** — 열려 있지 않다 | **실제 공백은 배선이다**: (a) `.github/workflows/hybrid-t1.yml`이 `WEB_HARNESS_ISOLATED_EXECUTION`만 설정하고 보호 컨텍스트 6종을 **하나도 주입하지 않는다**, (b) `.claude/quality-attesters.json`이 없다, (c) 실제 프로젝트에 서명 증명이 0건이다. 즉 이 경로는 **fixture로만 검증되고 fixture 밖에서 발화한 적이 없다** — 아침의 스팩 확정과 정확히 같은 상태이며 처방도 같다(실사용 1호). **로컬에서 서명 증명을 만들 수 없다**는 것은 결함이 아니라 설계다(CLAUDE.md 비협상: 로컬 서명 증거 위조 금지) — 무장은 CI 쪽 작업이다. **SLSA 자체 한계(2026-05 SLSA 블로그)**: 공격자 코드가 빌드 플랫폼 안에서 돌면 관측은 정확하지만 빌드는 이미 오염됐다. L3의 실행 격리 요구가 그래서 있으며, 이 저장소의 격리는 `argv-only` 수준이라 그 층위는 미해결이다 |
| layerMap 파일 항목(2026-08-26 발견·수정) | `normalizeLayerPath`가 **무조건 `/`를 붙여** `src/ChatKit.ts` → `src/ChatKit.ts/`를 만들었고 그 파일 자신과는 영원히 맞지 않았다. 실사용 확정 2호의 layerMap 17개 중 **6개가 파일**이라(`src/` 아래가 평면이라 루트 파일이 각각 한 레이어) **실사용 스팩의 3분의 1이 무소유**였다 | 실측(2026-08-26): 2호 17경로 중 6개 차단 → 수정 후 17/17 | 파일은 완전 일치(`$` 앵커), 디렉토리는 접두 일치로 가른다. 앵커가 없으면 `src/ChatKit.ts`가 `src/ChatKit.tsx`까지 잡아 스팩 밖으로 샌다. **프록시 표기**: 마지막 세그먼트의 점 유무로 파일·디렉토리를 가른다 — `src/v1.2` 같은 디렉토리는 파일로, `Makefile` 같은 확장자 없는 파일은 디렉토리로 오인된다. 두 오판 방향을 회귀로 고정했다. **발견 경위**: 단계별 산출물 소유 확인 중 "src/anything.ts 소유자 없음"이 나와 파고들다 잡았다 — 정적 등록부만 보면 안 보이고 **실사용 스팩으로 재야** 보인다 | 반증 등록부 6건째 |
| lockfile 출처 판정 오탐(2026-08-27) | 판정이 lockfile **텍스트 전체**에서 URL 모양을 찾아 사람이 읽는 `deprecated:` 안내문 속 URL을 dependency source로 오인했다. **eslint를 쓰는 어떤 프로젝트도 install할 수 없었다** — 첫 eval receipt의 executor가 이 벽에 갇혔고, 유일한 탈출구가 `eslint-plugin-jsx-a11y` 제거였는데 I6 하한이라며 거부하고 BLOCKER로 남겼다 | 실측(2026-08-27): 이웃 repo lockfile의 걸린 URL이 `https://eslint.org/version-support`·`https://nextjs.org/blog/CVE-2025-66478`·`security-update-2025-12-11` — **셋 중 둘이 보안 권고다**. CVE 안내문이 적혔다고 install을 막는 것은 정확히 거꾸로다 | `lockfile-source-lib.mjs`로 순수 로직을 뽑고(CLI라 테스트 불가였다) 산문 필드 값을 제외한 뒤 판정한다. **denylist 방향**: 새 *출처* 필드는 여전히 검사(fail-closed), 새 *산문* 필드는 시끄러운 오탐(조용한 통과 아님). allowlist였다면 반대다. **적대 검토가 HIGH를 잡았다**: 첫 구현이 `>`/`|` 리터럴만 봐서 접힌 plain·따옴표 다중행·들여쓰기 지시자(`|2`·`>4`)를 놓쳤고, **고치려던 오탐이 긴 통지에서 재발**할 수 있었다. 형태별 특수 처리를 버리고 **들여쓰기 규칙 하나**로 통일했다 — 키보다 깊게 들여쓴 줄은 전부 값이다. 네 형태가 한 규칙에 덮인다. **과다 제거가 아닌 근거**: 더 깊게 들여쓴 줄은 YAML도 스칼라 내용으로 읽으므로 거기 `resolution:`을 숨겨도 pnpm이 fetch하지 않는다 — 지우는 범위와 pnpm이 무시하는 범위가 같은 의미론이다. 같은 들여쓰기 줄은 값을 끝내고 검사된다(회귀로 고정). **필드 축소**: 처음 8개를 넣었다가 pnpm-lock v9에 실재하는 `deprecated` 하나로 줄였다 — 넓히면 이득 없이 "값을 지우는 키" 표면만 커지고 나중에 pnpm이 그 키로 fetch 가능한 값을 흘리면 스스로 눈을 가린다(적대 검토 지적). **호출 계약**: anchor/alias 모호성 거부(`rejectAmbiguousYaml`)는 호출자가 **원문에** 선행해야 한다 — strip은 alias를 해석하지 않는다. lib 주석에 못박았다. **모노레포 차단 해소(같은 날 후속)**: pnpm은 `workspace:<name>`을 lockfile에 `link:<상대경로>`로 적으므로 **모든 워크스페이스 lockfile은 필연적으로 `link:`를 포함한다** — 프로토콜만 보고 막으면 모노레포 전체가 install 불가였다. 원래 `link:`를 막은 이유는 **프로젝트 밖 코드**를 install script와 함께 끌어오는 것이었으므로, 기준을 프로토콜에서 **대상이 루트 안인가**로 바꿨다. `importers:` 절을 훑어 importer 문맥을 추적하고 그 디렉터리 기준으로 해석한다 — 같은 `link:../common`도 importer마다 대상이 다르다. **문맥을 모르면 루트 기준**(가장 보수적, `../` 하나로 밖). `workspace:<name>`은 경로가 아니라 이름이고 pnpm이 선언된 워크스페이스 안에서만 해석하므로 허용한다. **작업 중 낸 사고**: 상대경로를 `./`·`../` 접두로만 인식해 pnpm이 루트 importer에 쓰는 **bare 형태(`link:packages/common`)를 놓쳤다** — 회귀가 잡았다. `/`나 `~`로 시작하지 않으면 상대로 고쳤다. **남은 것**: `patch:` 프로토콜은 여전히 차단이다 — pnpm patch는 프로젝트 안 `patches/`를 가리키지만 형태가 달라 경로 판정 대상이 아니다. 별도 판단으로 남긴다 | 회귀 11종, 반증 등록부 13·14호, **실제 lockfile 4종 전부 통과** · 탈출 시도 5종(과다 상승·루트 상승·절대 file:·홈 경로·외부 tarball) 전부 차단 |
| 첫 eval receipt(2026-08-27) | 효능 축이 사흘째 0이었다 — 시나리오 46건, 커밋된 receipt 0건. `complete-harness-packaging`을 `run-eval-executor`로 완주해 **첫 receipt를 확보**했다(PASS 3/3, 기계 evidence·스키마 검증 통과) | 실측: run 20분 · `harness@d64533d` · grader read-only 채점 · `--verify-result` 통과 | **receipt보다 결함이 값졌다 — 5건**: ① `deploy-harness`가 `shape-checks.json`을 배포하지 않아 **배포된 하네스가 어떤 프로필도 로드 못 함**(오늘 그 파일을 실행 그래프 정본으로 만든 결과. Claude를 띄우기도 전 1단계에서 죽었다) ② `APP_PACKAGE_JSON`의 죽은 pin 3건(`@tanstack/react-query@5.90.0`·`typescript@6.0.0` 미존재, `typescript-eslint@8.57.0` peer 위반) ③ broker `validateLockfileSource()`가 `deprecated:` 안내 URL을 dependency source로 오인 — **현재 레지스트리에서 eslint 쓰는 모든 프로젝트가 install 불가** ④ `deploy-harness`가 target `README.md`를 요구하는데 만들지도 알리지도 않음(1차 배포 FAIL→롤백, telemetry에 이력이 남았다) ⑤ 체인 배포(배포본에서 다시 배포) 시 스캔이 `deployment.json`을 집어 중복 — **오늘 열거를 스캔으로 바꾼 내 변경의 부작용**. **가장 중요한 관찰**: executor가 게이트를 통과할 **유일한 경로**를 찾았고 거부했다 — `eslint-plugin-jsx-a11y` 제거가 유일한 통과였는데 "정적 접근성 lint는 I6 안전 하한"이라며 BLOCKER로 남겼다. **비대화 자율 실행에서 검증 약화로 폐곡선을 닫지 않았다** — CLAUDE.md 비협상 조항이 전이된 첫 증거다. **프록시 표기**: 이 receipt는 `/project-init`(scaffold) 경로다. `/web-orchestrator` 전 구간(기획·설계·QA 게이트)은 여전히 receipt 0건이며, 이 1건으로 효능이 측정됐다고 말할 수 없다 — **결함 발견 능력**의 증거이지 **결과 품질**의 증거가 아니다. ②③은 미수정(제어면 실질 변경이라 별도 리뷰 필요) | receipt 0→1건, `deploy-harness` 배포 카탈로그를 열거→스캔 강제, 체인 배포 중복 수정 |
| 프로젝트 밖 읽기 + ingestion 감지 게이트(2026-08-27) | **둘 다 "경계 두 개가 한 규칙에 뭉쳐" 있던 것이다.** ① `evaluateSensitiveAccess`가 `.ssh/id_rsa`와 이웃 repo의 `package.json`을 **같은 코드·같은 이유**(`DENY_PATH_OUTSIDE`)로 막았다 — 워크스페이스가 repo 하나로 끝나지 않는데(프론트 옆 API 서버) 계약 설계가 추측이 됐다. ② ingestion **감지**가 capability를 강제 선택해, 배포 알림 `fetch(url,{method:'POST'})` 하나로 프로필 식별과 quality runner가 통째로 죽었다 | 실측(2026-08-27, 이웃 repo 6개 코퍼스): 읽기 5/5 차단 → 선언 후 열림·비밀 여전히 차단 · 프로필 해석 5개 중 4개 `INGESTION_CONTRACT_MISSING`/`INVALID` → 개정 후 4/4 해석(5번째는 Java라 `PROFILE_NOT_DETECTED`가 정답) | **읽기**: `.claude/workspace-roots.json`에 **사용자가 선언한 루트만** 읽기 전용으로 연다. 기본은 닫힘 유지(파일 없으면 종전 동작), 모델이 런타임에 못 넓힘(그 파일 쓰기는 소유권 훅이 막는다), 비밀 검사는 **연 루트 안에서도 그대로**(`.env`·`.git/config`·키), 심볼릭 링크 탈출 차단, 홈·`/`·프로젝트 조상은 선언해도 안 열림. 쓰기·실행은 무관하다. **ingestion**: 감지 강도는 **전혀 낮추지 않았다** — 좁히면 진짜 수집을 놓친다(오탐 4건을 응답소비·메서드로 가르려 했으나 `upload.cjs`의 OIDC 토큰 GET에서 무너졌다). 대신 기준을 감지 → **선언**으로 옮겼다. 선언하면 `ingestion.validate` receipt·`qa-data-quality.md`·Vercel static 릴리스 차단이 **전부 그대로** 발화한다. **약화 방향(I2, 숨기지 않음)**: 진짜로 수집하는데 선언하지 않으면 ingestion QA가 돌지 않는다. 그 대가로 감지 사실을 프로필 `ingestion` 보고에 남기고(evidence 포함) intake에서 되묻게 했다 — **조용히 사라지지 않는다**를 테스트로 고정했다. **작업 중 낸 사고**: `ingestionReadiness.blocksImplementation`을 만들었다가 걷어냈다 — 아무도 안 읽는데 게이트처럼 보이는 필드는 가짜 게이트다(사용자 "게이트 위치를 어디로??" 지적). 또 프로필에 `ingestion` 키를 넣자 `additionalProperties:false` 스키마에 걸렸고, required에는 넣지 않아 기존 잠금은 소급 fail하지 않는다. **프록시 표기**: `workspace-roots.json`은 수기 선언이다. 선언하지 않은 형제 repo는 여전히 안 보인다 — 조용한 실패가 아니라 `DENY_PATH_OUTSIDE`로 시끄럽게 막힌다 | 회귀 5종 갱신(옛 "감지=진실" 단언을 새 계약으로 이전), 반증 등록부 12호, `web-profile-contract.md` 개정 |
| 카탈로그 키 엄격성 누락(2026-08-27 발견·수정) | 실행 명세를 어댑터 → `shape-checks.json`으로 옮기면서 어댑터에 있던 **미지 키 차단(`assertKnownKeys`)이 따라오지 않았다.** 오늘 추가한 필드 6종 중 3종(`gatesRelease`·`needsArtifact`·`evidenceName`)의 오타가 조용히 통과했다 | 실측(2026-08-27, 재평가 중 발견): 오타 6종 주입 → 3종만 FAIL(receiptKind·requires·scriptCandidates), 3종 통과 → 키 엄격성 추가 후 6/6 FAIL | **왜 약화인가**: 오타는 필드 부재로 읽혀 **기본값으로 퇴화**한다 — `gateRelease`로 잘못 적으면 `gatesRelease`가 undefined가 되어 smoke가 릴리스를 막고, `needArtifact`면 산출물 의존이 사라져 빌드 전에 검사가 돈다. 값이 틀린 것보다 조용하다. **앞선 커밋에서 같은 클래스를 하나는 잡고 하나는 놓쳤다**: 검사 kind 어휘의 schema-parity는 "이동은 검사 삭제의 이유가 아니다"라며 옮겼는데, 키 엄격성은 같은 이동에서 빠뜨렸다 — 옮길 목록을 **열거로** 만들었기 때문이다. **프록시 표기**: `ENTRY_KEYS`는 수기 목록이다. 새 필드를 추가하면서 목록에 넣지 않으면 그 필드가 전부 "알 수 없는 키"로 FAIL한다(fail-closed 방향이라 조용하지 않다) | 회귀 2종, 반증 등록부 11호 |
| 어댑터 실행 명세 제거(2026-08-27) | `commands`·`checks`·`tasks`를 어댑터에서 걷어냈다 — 3종 33,038B → 8,453B(**−74%**). 검사와 그래프는 `shape-checks.json`에서, 명령은 프로젝트 `package.json` script에서 도출한다. **등가 게이트는 삭제와 함께 없앴다** — 비교 대상이 사라지면 게이트도 죽는 것이 맞다. 발판을 계약으로 승격시키면 중복을 영구화하면서 감시 비용까지 얹는다(사용자 지적) | 실측(2026-08-27): 삭제 전 3종 노드·간선 전부 일치 확인 → 삭제 → `pnpm run ci` green · 반증 10건 발화 | **"지우고 전체가 도는지 본다"가 실제로 다섯 건을 드러냈다**(전부 게이트가 잡았다): ① `ingestion.validate`가 **어댑터에만** 있고 형태 카탈로그엔 없어 통째로 사라질 뻔했다 — `test-web-core`가 잡았고 카탈로그로 옮겼다. ② 옮기자마자 형태 conformance가 **전 프로젝트에 ingestion receipt를 요구**했다 — 형태(무엇을 만드는가)와 능력(무엇을 켰는가)은 다른 축이라 조건부 검사를 형태만으로 요구할 수 없다. `capabilityGated`로 분류해 조용히 사라지지도 않게 했다. ③ `resolveProfileCommands`가 `missing`을 버려 script 없는 검사의 **receipt 자체가 안 나왔다** — 침묵은 BLOCKED보다 나쁘다. 이름 붙인 기대를 돌려주도록 고쳤다. ④ 어댑터가 명령을 선언하지 않으니 "선언에 shell이 섞이는" 공격 표면이 사라졌는데, 표면이 사라진 것과 **보호가 사라진 것은 구분되지 않는다** — 같은 단언(실행 파일 allowlist·argv 배열·shell 메타문자·pnpm 하위 명령 경계)을 `resolve-commands`에 다시 걸었다. ⑤ 검사 kind 어휘의 schema-parity가 어댑터 스키마를 보고 있었다 — 어휘가 `shape-checks.json`으로 옮겨갔으므로 parity 대상도 옮겼다. **이동은 검사 삭제의 이유가 아니다.** **golden 재-잠금**: `vite-serverless-hybrid` 잠금이 stale이 돼 재생성했다. 앞서 "certified T1이 격리 검증 안 된 어댑터를 증명하게 된다"고 미뤘으나, 삭제 **전에** 노드·간선 등가를 확인했으므로 같은 그래프에 대한 증거다. 재-잠금에서 사전 드리프트 2건도 드러났다(`frontend.router`가 어댑터와 불일치, evidence 표기 형식). **프록시 표기**: 어댑터 잔여 8,453B(`detection`·`trust`·`profileDefaults`·`deployment*`·`artifacts`·`shapes`)에는 교체본이 없다. `trust`는 안전 하한이라 이관 대상이 아니다(I6) | 어댑터 −74%, `shape-checks.json`에 `ssr-web-app` 22항목 + `ingestion.validate` + `receiptKind`·`scriptCandidates`·`requires`·`gatesRelease`·`evidenceName` 기구, 등가 게이트·회귀·반증 11호 제거(임무 완료), 회귀 6종 갱신 |
| 어댑터 삭제 — 전제가 틀렸다(2026-08-27) | 이전 기록은 남은 일이 "`validate-release-fixtures` 1,422줄 중 단언 74/97 재작성"이라고 적었다. **틀렸다.** 실측하니 교체본 `resolve-commands.mjs`·`derive-execution-graph.mjs`가 **소비처 0**이었다 — 등가를 증명해 놓고 **배선한 적이 없었다**. 실행 경로는 여전히 `compile-execution-plan` → `dag-lib` → `adapter.tasks`다. 스캐폴딩이 막고 있던 것이 아니라 교체가 시작조차 안 됐다 | 실측(2026-08-27): 두 스크립트 소비처 0 · 어댑터 33,038B 중 commands+tasks+checks 59% · `react-vite-spa`/`vite-serverless-hybrid` 도출≡선언 · `next-app-fullstack` adapter 32 tasks vs 도출 12, **교집합 7** | ~~**Next는 형태 어휘 밖이다**~~ → **같은 날 정정(사용자 지적)**: 이 판단이 틀렸다. `next.*` 22종의 실행 명령을 재보니 **전부 `pnpm run <script>`**였다 — 하네스 고유 명령 0건. 프레임워크 지식이 아니라 receipt 이름과 script 이름의 이름표이며, `vite.build`가 이미 카탈로그에 있으니 "프레임워크 접두"라는 논거도 자기모순이었다. 능력 조건(`rsc`·`app-router`·`ssr`·`auth`)과 배포 타깃도 **스팩이 확정하는 것**이지 어댑터 고유 지식이 아니다(`selectedCapabilities`는 이미 호출자가 넘기고 어댑터는 기본값만 준다). 없던 것은 어휘가 아니라 **조건 기구**였다 — `shape-checks.json`에 `requires`가 아예 없었다. 도출 규칙 4개(능력 게이팅 · 이름 있는 2차 산출물 · 배포 타깃별 릴리스 노드 · `gatesRelease`)를 넣자 **3종 전부 노드와 간선까지 일치**했다. **`trust`는 이관 대상이 아니다**: `argv-only`·실행 파일 allowlist·환경 allowlist는 모델이 판단하면 안 되는 안전 하한이다(I6). 어댑터 잔여 41%(detection·trust·profileDefaults·deployment*)에는 교체본이 없다. **삭제가 아니라 결박을 먼저 한 이유**: `vite-serverless-hybrid/adapter.json`을 건드리면 golden 잠금이 stale이 되고(실측: version 한 글자만 바꿔도 `validate-golden-profiles` FAIL), 다시 잠그면 이 프로필의 `certified` T1 receipt(격리 run 32614388125)가 **실제로 격리 검증된 적 없는 어댑터를 증명한다고 주장**하게 된다 — I1 위반이다. 재-잠금에는 새 격리 CI 실행이 필요하고 로컬에서 만들 수 없다. 그래서 순서를 뒤집었다: **먼저 CI에서 등가를 결박하고, 삭제는 격리 실행이 가능할 때.** 먼저 지우면 대조 기준이 사라진다. **작업 중 낸 사고**: 등가 게이트 첫 구현이 `resolveCommands`의 반환 형상을 `{checkId: command}`로 오해해 명령 대조 루프가 **공회전**했다 — 위반 0건이 나왔지만 아무것도 대조하지 않았다. 해석 0건이면 FAIL하도록 고치고 회귀로 고정했다(공허한 PASS는 이 repo가 남에게서 잡던 결함이다). **프록시 표기**: 명령 대조는 이 repo의 golden 2종에서만 이뤄진다 — golden이 쓰는 script 이름이 곧 실세계 이름이라는 가정이다 | `shape-checks.json`에 `receiptKind` 16항목 + `ssr-web-app` 형태 22항목 신설, 도출 규칙 4개 일반화, 등가 게이트 신설(검사 집합·receiptKind·그래프 **노드와 간선**·명령 argv 14건 실대조), 회귀 8종, 반증 등록부 11호. **등가 게이트 자체가 두 번 과장을 잡았다**: ① 첫 구현이 `resolveCommands` 반환 형상을 오해해 명령 대조가 **공회전**했다(위반 0건인데 아무것도 대조하지 않았다) ② 노드 집합만 비교해 "Vite 2종 등가"라고 보고했는데 간선까지 재니 `release.assemble`의 requires/provides가 달랐다 — 노드가 같아도 **무엇이 릴리스를 막는가**가 다르면 등가가 아니다. 둘 다 회귀로 고정했다. **남은 미판정**: `next-app-fullstack`은 golden이 없어 명령 해석 대조를 못 한다 — 통과가 아니라 `미판정`으로 이름을 남긴다 |
| Phase 본문 시점 로드 강등(2026-08-27) | `web-orchestrator/SKILL.md`의 Phase 2·3·4 본문을 `references/phase-{2,3,4}-*.md`로 내렸다. **표적을 다시 재서 바꿨다** — 전날 분석은 "진입 비용을 줄이려면 Phase 계약을 시점 로드로"라고 적었으나 그 계약들은 이미 앵커 밖이었다. 다시 재니 SKILL.md **본문 하나가 35,423B로 진입 비용의 56%**였고 참조 6종 합계(28,230B)보다 컸다 | 실측(2026-08-27): 본문 35,423B / 참조 28,230B / 합계 63,653B → 강등 후 17,282 + 28,230 = 45,512B(−28%) | **절감의 성질**: 전체 그린필드 1회 실행의 총 컨텍스트는 줄지 않는다 — 뒤로 미뤄질 뿐이다. 진짜로 줄어드는 것은 ① 모든 실행이 **아무것도 하기 전에** 내는 고정 비용 ② 짧게 끝나는 경로(Iterate·Resume·verification-only·Phase 1 체크포인트 기각). 그 구분을 흐리면 프록시 게이밍이다. **강등이 만든 새 표면**: 시점 로드는 모델이 실제로 읽어야 발화한다 — 산문 계약이 한 층 더 "읽지 않으면 없는 것"이 됐다. 기계 게이트(receipt·release gate·훅)는 이 층에 의존하지 않으므로 하한은 그대로다. **작업 중 드러난 것**: 강등하자마자 검사 15건이 FAIL했다 — `web-orchestrator/SKILL.md` **경로를 하드코딩한 검사가 8곳**이었고 본문이 옮겨가자 전부 눈이 멀었다. 경로를 다시 하드코딩하면 다음 강등에서 반복되므로 `validators/orchestration-surface.mjs`에 **glob 정의**(SKILL.md + `references/phase-*.md`)를 두고 8곳이 그것을 쓰게 했다. **프록시 표기**: 표면 검사는 "오케스트레이터가 이 문장을 읽는다"가 아니라 "표면에 이 문장이 있다"만 본다. **grandfather 3건**: 세 Phase 파일을 contract-hygiene `references` allowlist에 넣었다 — 내용이 SKILL.md에서 **축자 이동**한 것이라 신규 계약의 `## 일반화 근거` 요구 대상이 아니다. 새 경로이되 새 계약이 아니다 | 진입 비용 63,653→45,512B(−28%), 참조 baseline 86→89, 반증 등록부 10호, 회귀 2종 |
| AI 표면 전면 제거(2026-08-27) | AI 앱 생성 표면 전체를 걷어냈다 — 에이전트 10종(`ai-*` 5·`data-governance-architect`·`tool-contract-designer`·`agent-trace-verifier`·`cost-latency-verifier`·`ai-eval-runner`), 스킬 8종, `ai-harness.json`, `ai-scenarios.json`(31 시나리오), 러너/검증 스크립트 3종, `AI_MODE`와 5개 submode. **근거는 겹침이 아니라 소비자 부재였다**: `ai-harness.json`의 `services`는 전날 지운 도메인 특화 5종과 정확히 같은 집합이었고, 10종이 소유하던 산출물(`ai-architecture.md`·`ai-threat-model.md`·`eval-plan.md`·`tool-contracts.md`·`data-governance.md`·`ai-requirements.md`)을 **요구하는 웹 계약이 하나도 없었다**(grep 0건) | 실측(2026-08-27): `ai-harness.json` services 5 = 삭제된 도메인 에이전트 5 · AI 산출물 요구 웹 계약 0건 | **삭제가 아니라 이관한 것 3가지**: ① `INJECTION_SUSPECT` **소비** 의무 — `ai-security-reviewer`가 유일한 두 번째 소비자였다. 그냥 지우면 소비자가 `data-quality-verifier` 하나로 줄어 ingestion 밖 경로(사용자 제공 외부 파일·원격 문서)가 무검증이 된다. `security-reviewer`로 옮겼다 — **의무는 AI가 아니라 외부 콘텐츠의 성질에 걸린다**. ② `data-access-verifier` — tenant 격리·row-level 인가는 AI 전용이 아니다. 입력을 `data-governance.md`에서 확정 스팩·`api-schema.md`로 재조준하고, release 리포트 조건을 `isAiProject`에서 `migrations/` 존재(= 서버가 데이터를 소유)로 바꿨다. ③ `untrusted-content-quarantine.md`의 "AI runtime 경계" 규칙 → "읽기와 행동의 분리"로 일반화. **프록시 표기**: `migrations/` 존재는 서버 소유 데이터의 프록시다 — migration 없이 서버 인가만 있는 형태(BaaS·외부 DB)는 잡지 못한다. **적대 검토가 잡은 사각(같은 날 수정)**: 처음엔 최상위 `migrations/`만 봤는데 `/server-db-migration`은 `client/migrations/`를 **기존 관습**으로 문서화하고 `agent-registry`의 소유권 정규식은 임의 깊이를 인정한다 — 하네스가 스스로 권장한 관습을 따른 프로젝트가 조용히 요구에서 빠졌다. 1단계 하위까지 보도록 고쳤고 회귀 4건 + 반증 등록부 9호로 고정했다. **남은 사각**: 2단계 이상(`apps/api/db/migrations/`)은 여전히 미탐이다. **또 하나 잡힌 것**: `run-eval-executor`의 인라인 검증이 구 `--verify-result`보다 약했다(versions 필수·assertion status 유효성·critical BLOCKED 3건 누락) — 삭제 전에는 AI 카탈로그만 강한 경로를 쓰고 있었다. 3건 전부 인라인으로 복원했다. **작업 중 낸 사고**: `run-eval-executor`가 `run-ai-evals.mjs --verify-result`로 스키마 검증을 **재사용**하고 있었다 — AI 카탈로그 전용 스크립트인 줄 알았는데 web 카탈로그 executor의 검증 경로 절반이 거기 있었다. 지우기 전에 인라인 검증으로 합쳤다. 이름이 `ai-`로 시작한다고 AI 전용이 아니다 | 에이전트 56→46, 스킬 32→24, 스크립트 107→104, 진입 비용 66,285→63,653B(−2,632), 참조 baseline 101→87 |
| 5범주 밖 26종 제거(2026-08-26) | 사용자 정의 5범주(기획·디자인/설계·스팩·개발·QA/검증) 밖의 에이전트를 제거하고 소유를 둘로 흡수했다 — 소스(`src/**`) 11종 → `developer`, 설정·배포·문서 15종 → `environment-scaffolder`. `_workspace` 산출물 3종(`design-preview-builder`·`release-manager`·`visual-baseline-manager`)은 디자인·QA 범주라 남겼다 | 실측: 소유 경로 분류 | **흡수하지 않은 것**: `packages/agent-runtime/`·`apps/agent-api/`·`workers/agent-jobs/` 등 **패키지 이름 처방**은 버렸다 — 도메인 특화 5종에서 지운 것과 같은 성질이며 소스이므로 `developer`가 layerMap으로 덮는다. **필드 소유권 철회**: 2시간 전 만든 `FIELD_OWNERSHIP`을 제거했다 — `package.json` 소유자가 5종→1종이 되면서 **가를 대상이 사라졌다**. 통합이 그 문제를 없앴으므로 죽은 기계를 남기지 않는다(반증 등록부에서도 제외, 6→5건). **제거된 검사**: `shared-foundation-builder`의 timestamp 소유 선언 검사와 `deploy-ci-writer`의 워크플로 하드닝 검사는 그 에이전트 **본문을 읽는** 검사였으므로 함께 사라졌다 — 계약 산문은 skills 쪽에 남는다. 경계 테스트 50건도 대상 에이전트가 없어져 제거됐다. **잔여 리스크 정정(같은 날)**: 앞서 "배포 하드닝 기계 검사가 사라졌다"고 적었으나 **과장이었다**. 사라진 것은 `deploy-ci-writer.md` **프롬프트 텍스트에 특정 문자열이 있는지** 보는 검사(`config:best-practices`·`WebFetch` 등)였고, 워크플로가 실제로 하드닝됐는지는 보지 않았다 — **프록시를 검사하는 프록시**였으며 지시가 있어도 따르지 않으면 아무것도 잡지 못했다. 생성된 워크플로 **실물**을 보는 `validate-workflows-and-evals`는 그대로 있다. 복구 불필요 | 에이전트 82→56(오늘 101→56), 마커 baseline 16→14, 진입 비용 28,291→28,297B |
| 조건부 구현 빌더 제거(2026-08-26) | 6종(`analytics-implementation`·`client-domain-state`·`form-state`·`lib-core`·`next-runtime`·`realtime-data-builder`) 제거. **첫 측정이 "겹침 0·무소유 0"으로 나와 유지를 권고했으나 그 측정이 틀렸다** — FSD 어휘를 전제한 fixture 경로로 FSD 어휘를 시험한 것이라 당연한 결과였다. 실사용 확정 2호(`@kakao/ai-chatkit`)의 실제 경로로 다시 재니 **9경로 중 6개가 무소유**였고, 소유된 3개 중 하나는 Next 앱이 아닌데 `next-runtime-builder`가 `components/` 패턴으로 잡은 오탐이었다 | 실측(2026-08-26): 깨끗한 fixture 9경로 겹침 0 → 실제 브라운필드 9경로 중 무소유 6 | `developer` + 스폰 범위로 이관. `live-mode` carve-out도 `src/features/live-mode/`라는 **이름을 쓰는 프로젝트에서만** 성립했다. **작업 중 낸 사고**: 다중행 regex 제거가 인접 항목을 세 번 먹었다(`planning-facilitator`, `lib-*` 3종, `human-approval`·`mock-api`·`model-gateway`·`next-app-scaffolder`). 전부 게이트가 잡았고 HEAD에서 블록을 되살려 줄 단위로 다시 지웠다 — **regex로 구조를 편집하지 말 것** | 에이전트 88→82(오늘 101→82), 마커 baseline 20→16 |
| 반증 게이트(2026-08-26 신설) | **게이트가 실제로 발화하는지**를 기계로 확인한다. 실측 2회: 검증 호출을 지워도 CI가 exit 0이었다 — 테스트가 lib을 직접 부르고 **배선 지점을 지나가지 않아서**다. 이 repo가 하루 종일 남의 코드에서 잡아낸 실패 클래스를 자기 테스트가 앓고 있었다 | 실측(2026-08-26): executionBinding 검증 제거 → exit 0, 환경 결속 검사 제거 → exit 0 | `falsification-registry.json`의 각 항목이 게이트를 무력화하는 **최소 변형**과 그 변형으로 반드시 실패해야 할 테스트를 짝짓는다. 통과하면 `NOT_FALSIFIED` — 반증되지 않는 게이트이며 언제든 조용히 끊길 수 있다. 변형 지점이 없거나 유일하지 않으면 `STALE`로 보고한다(조용히 통과시키지 않는다). 원본은 실패 경로에서도 복원한다. **손으로 하던 반증의 기계화**이며 오늘 손으로 15회쯤 했다 — 손으로 하면 잊는다. **프록시 표기**: 등록부는 수기다. 게이트를 새로 만들면서 등록하지 않으면 그 게이트는 여전히 반증되지 않는다 — 등록 강제는 미해결이다. **적용 범위**: 현재 14건(환경 결속·스팩 원장 TAMPERED·evidence 이름 충돌·형태 생략 FAIL·미빌드 강등·data-access 경로 모델 등) | CI 편입, 자기 회귀 4종 |
| 필드 소유권(2026-08-26 신설) | 경로 소유권으로는 **"이 필드를 소유한다"를 표현할 수 없다**. `package.json`은 5종이 소유하는데 각자 다른 필드를 다뤘고, `version-file-updater`가 `scripts`를 고쳐도 경로 훅은 통과시켰다 — `scripts`는 **무엇이 검사로 도는지**를 정하므로(`resolve-commands`가 거기서 읽는다) 게이트를 스스로 약화시키는 경로였다 | 실측(2026-08-26): `package.json` 소유자 5종. **통합 전부터 있던 겹침이며 내가 만든 것이 아니다**(앞선 보고에서 "방금 만들었다"고 한 것은 오기였고 커밋에서 정정) | `FIELD_OWNERSHIP` 표 + `changedTopLevelKeys` (Write는 content, Edit은 정확 문자열 치환을 재현해 diff). 겹침은 **금지가 아니라 명시**다 — `scripts`를 셋이 공유하지만 그것이 선언돼 있다. 합의된 공유와 아무도 눈치 못 챈 겹침은 다르다. **프록시 표기**: 최상위 키만 본다. `scripts.lint` 하나만 바꾸는 것과 `scripts` 전체 교체를 구분하지 못한다 — 중첩 필드 소유는 미해결이다. 판정 불가(JSON 아님·파싱 실패·배열)면 필드 판정을 건너뛰고 경로 소유권만 적용한다(미판정을 차단으로도 통과로도 만들지 않는다). **적용 범위**: `FIELD_OWNERSHIP`에 없는 파일은 기존 경로 소유권 그대로다 | 회귀 8종, 필드 검사 제거 시 exit 1 |
| 환경 구성 3종 통합(2026-08-26) | `package-scaffolder`·`tooling-scaffolder`·`test-scaffolder` → `environment-scaffolder` 1종. 셋이 `vitest.config.ts`·`playwright.config.ts`·`src/test/`를 **겹쳐 소유**해 경계가 성립하지 않았다(구조 지시 빌더 6종과 같은 결함) | 실측(2026-08-26) | **삭제가 아니라 통합인 이유**: 이 3종이 소유하는 것은 `layerMap`이 담는 범주가 아니다 — `layerMap`은 논리 소스 레이어이고 설정 파일은 거기 없다(2호 실제 layerMap 17개가 전부 소스). 그냥 지우면 **아무도 설정 파일을 못 쓴다**. **`developer`와 분리를 유지하는 이유**: `package.json`의 `scripts`가 무엇이 검사로 도는지를 정한다(`resolve-commands`가 거기서 읽는다). 구현자가 그 권한을 가지면 `"lint": "echo ok"`로 게이트를 스스로 약화시킬 수 있다 — **검사를 정의하는 것과 검사를 통과해야 하는 것은 분리한다.** **작업 중 낸 사고 2건**: 다중행 regex 제거가 `planning-facilitator` 항목까지 먹었고(게이트가 잡음), 일괄 치환이 "다른 에이전트는 막힌다"를 검증하는 경계 케이스까지 바꿔 그 단언을 무의미하게 만들었다(게이트가 잡음). 둘 다 복구했다 | 에이전트 90→88(오늘 101→88), 마커 baseline 22→20, 진입 비용 28,287→28,291B |
| 도메인 특화 빌더 제거(2026-08-26) | 5종(`analytics-agent`·`customer-support-agent`·`enterprise-search`·`browser-agent`·`code-review-agent-builder`) 제거. 소유 경로가 **패키지 이름까지 하드코딩**돼 있었다(`packages/analytics-agent/`·`apps/browser-runner/`·`workers/ingestion/`) — FSD 경로 처방보다 강한 처방이며 프로젝트가 그 이름을 쓸 것을 전제한다 | 실측: 5종 전부 소유 경로 2개가 모두 특정 패키지·앱 이름 | **가설이 틀렸던 지점**: 각 스킬의 `implementation-contract.md`가 처방일 것으로 보고 함께 지우려 했으나, 표본(browser-agent)을 읽어 보니 CAPTCHA 명시적 거부·ToS/robots 준수·rate·자격증명 운영 등 **안전 하한**이었다. 도메인 계약과 implementation 계약 **둘 다 남기고 에이전트만 제거**했다. **안전 계약 이관**: `validate-content-policy`의 `markerProducers`에 그 5종 중 3종이 있었다 — 비신뢰 콘텐츠 격리(`INJECTION_SUSPECT`) 의무다. `developer`로 이관했다. 근거: 이 의무는 **도메인이 아니라 구현의 성질**에 걸린다 — 외부 콘텐츠가 실행에 들어오면 누가 구현하든 진다. `ai-harness.json`의 `services[].builder` 5건도 `developer`로 바꿨다 | 에이전트 95→90, README 수치 갱신 |
| 구조 지시 빌더 제거(2026-08-26) | 6종(`app-shell`·`route`·`component`·`entity-query`·`feature-mutation`·`data-ui-binder`)을 제거하고 단일 `developer`로 대체했다. **제거 전에 그 소유권이 성립하는지 측정했고 성립하지 않았다** — `src/pages/**`를 셋이 겹쳐 갖고("병렬 작업이 서로 침범하지 않는다"는 명분이 이미 깨져 있었다), 비-FSD 어휘(`src/stores`·`src/hooks`·`src/components`)는 소유자가 아예 없었다(브라운필드에서 레이어 10개 중 5개가 막힌 원인). 그 6종이 공급한 것은 격리가 아니라 **FSD 경로 처방**이었다 | 실측(2026-08-26): 경로 9종 중 3종 3중 겹침 · 3종 무소유 | 소유권은 `layerMap`(프로젝트 자기 어휘), 격리는 스폰 범위(`ALLOWED_PATHS` = `moduleBoundaries`)가 공급한다. 둘은 **교집합**이라 범위가 소유권을 넓히지 못한다. `developer: []`로 **빈 소유권**을 등록해 스팩이 없으면 아무것도 못 쓴다 — FSD 기본을 폴백으로 주면 그 순간 다시 경로 처방이 된다. **잔여**: `feature-mutation-builder`의 `live-mode` carve-out이 그 6종의 유일한 정당한 기능이었고 평면 `layerMap`은 그것을 표현하지 못한다 — 아래 layerMap 행의 미해결과 같은 항목이다. 마커 baseline 28→22, 진입 비용 28,300→28,287B |
| 스팩 유래 소유권(layerMap) | 소유권 경계를 `spec-lock`의 `layerMap`에서 얻는다. **평면 map은 carve-out을 표현하지 못한다** — 실측(2026-08-26): FSD를 기본 layerMap으로 폴백에 쓰자 `validate-agent-boundaries`가 회귀를 잡았다. `AGENT_OWNERSHIP`은 레이어 이름 이상을 인코딩한다(`feature-mutation-builder`가 `live-mode`를 제외하는 등). 따라서 폴백은 여전히 등록부이며 **"FSD 기본값 제거"는 미달성**이다. 또 `layerMap`이 덮지 않는 소스 디렉토리는 아무 에이전트도 쓸 수 없는데 그 판정이 FAIL이 아니라 note다 | 실측(2026-08-26): 브라운필드 패키지의 레이어 10개 중 5개가 소유자 없음으로 훅에 막혔고, 브라운필드 계약(integration-overlay)의 "기존 설정 존중" 요구와 훅이 모순이었다 | 겹치는 레이어는 스팩을 신뢰하지 않는다(권한 확대 차단). 역할 매핑·레이어 미선언·스팩 부재는 전부 등록부 폴백(fail-closed 유지, 반증 고정). **미해결 TODO**: `layerMap`의 carve-out 표현, 커버리지 공백의 판정 강화 |
| 에이전트 reachability가 단방향이었다(2026-08-27 발견·수정) | `validate-harness`는 **정방향만** 봤다 — "각 에이전트가 어디선가 언급되는가". 역방향("스킬이 부르는 이름이 실존하는가")이 없어 삭제·개명 뒤 남은 죽은 스폰 지시가 조용히 통과했다. `legacyAgents`(수기 5개)는 2026-08-26에 제거된 55종을 담지 않았고, 그 목록은 애초에 **과거 서술과 실제 지시를 구분할 수 없어** 단순 추가도 불가능했다 | 실측: `visual-developer` — 596c92e의 `test-writer`→`developer` 일괄 치환이 `visual-test-writer`를 **만든 적 없는 이름**으로 바꿨고, `visual-design-verify`(maturity: eval-covered)와 `phase-3-development.md` 등 **6곳에 스폰 지시로 남은 채 harness-ci가 8커밋 동안 green**이었다. `VISUAL_QA_MODE` 레인이 그동안 스폰 불가였다. **같은 날 유일한 eval receipt(`complete-harness-packaging`)의 assertion a3가 정확히 이 속성을 검사하고 PASS를 냈다** — grader의 역할 접미사 목록에 `developer`가 없어 `visual-developer`를 **추출조차 하지 못했다**. 하드코딩 어휘가 프록시였다는 실증이며, "consumer 열 누락"·"collectMarkdown이 비-.md를 건너뜀"과 같은 클래스의 3번째 재발이다 | `validators/agent-reachability.mjs` 신설 — **역할 어휘를 실존 에이전트 이름의 마지막 하이픈 세그먼트에서 파생**한다 — 에이전트가 **늘면** 어휘가 함께 자라므로 "새 접미사를 목록에 넣는 것을 잊는" 사각(a3가 `developer`를 빠뜨린 그 사각)은 재발하지 않는다. **그러나 어휘 수축은 막지 못한다(적대 리뷰 지적, 2026-08-27 등록)**: 어떤 접미사를 **유일하게** 보유한 에이전트를 삭제하면 그 접미사가 어휘에서 사라지고, 바로 그 에이전트의 죽은 지시가 침묵 통과한다(예: `test-executor`를 지우면 `executor`가 소멸해 `foo-executor` 잔존 지시를 못 잡는다). **이 게이트가 겨누는 "삭제 후 잔존" 시나리오의 부분집합이 원리적으로 안 잡힌다** — 삭제 커밋에서 그 이름의 잔존 참조를 사람이 확인해야 한다(미해결 TODO: 삭제 대상 이름의 잔존 참조를 삭제 시점에 대조하는 검사). 배포본에서도 성립하므로 위 "source/deployed 판별자" 행의 reachability 과다 범위 TODO 절반을 닫는다. 회귀 11종(실제 트리 오탐 0 포함) + 반증 등록부 15호로 배선을 고정했다. **프록시 표기**: 서술과 지시를 **백틱 관례**로 가른다 — 백틱 없이 쓴 진짜 스폰 지시는 못 잡는다(fail-open). 수기 예외 목록을 두지 않으려는 의도적 교환이며, 반대 방향(서술에 백틱)은 loud FAIL이라 조용히 새지 않는다. 함께 드러난 **vacuous 검사**: `webOnlyAgent` 4개 중 3개가 삭제된 이름이라 영원히 발화하지 않았다 — 실존 대조를 붙였다 |
| 메인 가드가 Windows에서 통째로 no-op(2026-08-27 발견·수정) | 14개 CLI가 `import.meta.url === \`file://${process.argv[1]}\``를 썼다. POSIX에서만 맞고 Windows 경로(`D:\…`)에서는 **절대 일치하지 않아 스크립트가 아무 일도 하지 않고 exit 0**을 냈다 — 실패가 아니라 **조용한 성공**이다 | 실측(2026-08-27): `validate-falsification.mjs`가 그 중 하나였다. 로컬에서 돌리면 출력 0줄·exit 0이라 "반증 게이트 통과"로 보였는데 실제로는 **아무 항목도 반증하지 않았다**. 가드를 고치자 같은 명령이 15건을 실제로 반증했다. 게이트의 배선을 지키려고 만든 게이트 자신이 이 저장소의 저자 플랫폼에서 꺼져 있었다 | 14개 전부 `pathToFileURL(process.argv[1]).href`로 교체. **범위 주의**: CI는 Linux라 이 버그는 CI를 통과시킨 적이 없다 — 영향은 **로컬 실행의 거짓 안심**이고, 저자가 로컬에서 돌린 CLI 판정은 소급 신뢰할 수 없다. **미해결 TODO**: 신규 CLI가 옛 관용구를 다시 쓰는 것을 막는 기계 검사(현재는 관례) |
| 스캐폴드 템플릿 pin이 게이트 밖(2026-08-27 발견·수정) | `validate-dependency-pins.mjs`는 **생성된 프로젝트의 `package.json`**을 검사한다. 그런데 pin의 원본은 `project-init/assets/templates.md` 안의 마크다운 코드펜스라 **어떤 게이트도 읽지 않는다** — 죽은 pin이 템플릿에 머물면 매 신규 프로젝트가 install에서 죽고, 게이트는 사후에야 발화한다 | 실측(2026-08-27, 등록소 대조): `typescript@6.0.0`(안정 릴리스 부재 — 베타 전용)과 `@tanstack/react-query@5.90.0`(부재)이 박혀 있었다. 첫 eval receipt가 이미 이 둘을 결함 ②로 보고했으나 "제어면 실질 변경"이라며 미수정으로 남겨졌다. 추가로 `typescript-eslint@8.57.0`의 peer는 `typescript <6.0.0`이라 6.x로 올리면 그것도 함께 위반이었다 | golden hybrid(**T1 격리 CI receipt가 있는 유일한 트리**)와 같은 조합 `typescript@6.0.2` + `typescript-eslint@8.65.0`(peer `<6.1.0`)으로 맞추고, react-query는 `5.102.7`로 교체했다. 추론보다 증명이 있는 값을 골랐다. **증거 등급 구분(적대 리뷰 지적)**: `typescript@6.0.2`·`typescript-eslint@8.65.0`은 golden hybrid 트리와 일치하므로 **T1 receipt에 결속된 증명**이고, `@tanstack/react-query@5.102.7`은 golden에 없어 **등록소 조회 주장**일 뿐이다(세션 중 registry API 조회로 46개 pin 전수 실존을 확인했으나 커밋된 receipt는 없다 — 오프라인 재현 불가). 신규 프로젝트 install receipt가 이 주장의 정본이며 아직 없다. **미해결 TODO**: 템플릿 pin을 `validate-dependency-pins`가 직접 읽게 하는 배선 — 지금은 사람이 등록소를 조회해야 하고, 이 수정 자체가 그 프록시의 산물이다 |
| 통합이 남긴 테스트 경로 무소유(2026-08-27 발견 → 2026-08-28 해소) | 596c92e 삭제 전 `test-writer`는 `src/**.{test,spec}`·`src/**/__tests__/`·`e2e/**.spec.ts`를, `visual-test-writer`는 `e2e/**.visual.spec.ts`·`src/**.visual.stories.tsx`를 소유했다. 통합 후 `AGENT_OWNERSHIP`에 남은 테스트 경로는 `e2e/{fixtures,helpers}`(environment-scaffolder)**뿐**이다. `developer`의 소유는 `resolveDeveloperOwnership(spec)` = **`layerMap` ∩ 스폰 범위**이고 스팩이 없으면 `[]`(의도된 fail-closed)인데, `layerMap`은 계약상 **논리 소스 레이어**이고 실사용 확정 2호의 17개 항목도 전부 소스였다 — 즉 `e2e/`는 구조적으로 들어갈 자리가 없다 | **실측(추론 아님)**: 실제 소유권 훅에 스팩(layerMap = 소스 레이어만)을 물려 재현했다 — `src/features/cart/cart.test.ts`·`__tests__/`·`*.visual.stories.tsx`는 **통과**(소스 레이어 안이라 layerMap이 덮는다), `e2e/checkout.spec.ts`·`e2e/*.visual.spec.ts`는 **차단**. 즉 유닛 테스트는 멀쩡했고 e2e만 무소유였다. 최초 등록의 "유닛도 막힌다"는 추론은 틀렸고 실행이 교정했다. **심각도 정정**: 이것은 "경로에 소유자가 없다"가 아니라 **교착**이었다 — `web-app`의 릴리스 게이팅 검사에 `vite.browser`가 있고(`gatesRelease`는 `false`일 때만 제외되므로 기본 포함), `resolve-commands`가 그것을 프로젝트의 `test:e2e`에 결박한다. 즉 파이프라인이 **e2e 증거를 요구하면서 e2e 작성을 구조적으로 막고 있었다** | **해소(2026-08-28, 사용자 결정: 유닛은 항상·e2e는 UI가 있으면)**: 스팩에 `testLayers: {unit, e2e?}`를 신설하고 `resolveDeveloperOwnership`이 layerMap ∪ testLayers를 소유하게 했다. UI 여부는 설계자 판단이 아니라 **형태 카탈로그**(`shapes.<name>.userInterface`)에서 도출하며, 카탈로그의 누락·오타는 `validate-shape-checks`가 FAIL로 잡는다("UI 없음"으로 조용히 퇴화하지 않는다). 겹침 불신은 layerMap 안에서만 적용해 유닛 테스트를 소스 옆에 두는 정상 배치를 깨지 않는다. 반증 3호(`spec-test-layers-unit`·`spec-test-layers-e2e-ui`·`developer-owns-test-layers`)로 배선을 고정했다. **커밋된 golden 스팩은 고치지 않았다** — `schemaVersion` 2 세대로 나누고 1은 읽기 전용 이력으로 뒀다(증거를 새 규칙에 맞춰 고쳐 쓰지 않는다). **적대 리뷰가 잡은 fail-open(2026-08-28, 같은 날 수정)**: 최초 구현은 카탈로그에 없는 형태를 `userInterface !== true` → **비UI**로 퇴화시켰다. `targetShapes: ["dashboard-app"]`처럼 카탈로그 밖 이름을 적는 것만으로 e2e 요구가 조용히 증발했고, 회귀 테스트가 그 fail-open을 **의도로 고정**하고 있었다(`hasUserInterface(['unknown']) === false`). 미등록 형태를 실패로 만드는 것은 shape-routing-contract §4("하네스가 모르는 것을 실패로 만들지 않는다")와 충돌하므로, 실패시키지 않되 침묵도 허용하지 않는 쪽으로 고쳤다 — `unknown`을 돌려 스팩이 `testLayers.e2e`에 경로를 적거나 `(absent — 이유)`로 명시하게 한다(`E2E_TEST_LAYER_UNDECIDED`). 반증 19호로 고정했다. 함께 잡힌 것: `testLayers`의 미지 키가 조용히 버려져 그 경로가 무소유로 남던 것 → `TEST_LAYER_UNKNOWN_KEY`로 loud reject. **잔여**: 선언된 e2e 경로가 실제 e2e 러너의 testDir와 일치하는지 미대조 — 스펙 파일이 존재하되 실행되지 않는 green이 가능하다. 후속으로 e2e receipt가 선언 경로 아래 스펙 1개 이상을 실행했음을 대조할 것 |

| 스팩 공유 판정이 `.gitignore` **텍스트** 프록시다(2026-08-28 신설) | `checkSpecShared`는 `.gitignore` 파일의 패턴만 읽는다. **git이 실제로 추적하는지는 대조하지 않는다** — 이미 추적 중인 파일은 gitignore를 무시하고, 반대로 무시되지 않아도 커밋을 안 하면 공유되지 않는다. 미탐 경로 셋: (1) 와일드카드 패턴(`_work*`·`*.json`)은 리터럴 비교라 안 잡힌다, (2) `.git/info/exclude`·전역 `core.excludesFile` 채널은 읽지 않는다, (3) "무시되지 않는데 커밋도 안 함"은 원리적으로 정적 검사 밖이다. 전부 **fail-open 방향**이다 | 실측(2026-08-28, 실제 `git add`로 7가지 대조): 초기 구현이 `_workspace` + `!_workspace/03_dev/spec.json`을 **"공유됨"으로 통과**시켰다 — git은 상위 디렉토리가 제외되면 파일 부정으로 되살릴 수 없으므로 실제로는 무시된다. 게이트가 잡으려던 바로 그 상태의 fail-open이었고, **회귀 테스트가 그 오판을 고정하고 있었다**. 적대 리뷰가 의미론으로 지적했고 실제 git 실행이 확인했다 | 조상 디렉토리 제외를 결정적으로 판정하도록 고쳤다(`dir/*`처럼 **내용만** 제외한 경우에만 재포함 인정). 실제 git 판정과 7/7 일치를 실행으로 확인했고 테스트를 다시 썼다. 반증 20호 `spec-shared-not-gitignored`로 배선을 고정했다. **남은 프록시성은 위 미탐 3종이며 여기 등록으로 대체하지 않는다** — 실해소는 `git check-ignore`/`ls-files` 실행이지만 이 validator는 순수 파일 판정을 유지하기로 한 의식적 교환이다. **미해결 TODO**: 릴리스 판정이 공유 receipt를 전제하게 되면 `_workspace/04_qa` 무시 정책을 재검토할 것 |

## 5. 이 문서의 갱신

판단 질문·예산·프록시 등록부의 변경 자체가 "게이트/validator 변경" 클래스다 — 같은 게이트를
거친다. 등록부에서 행을 지우려면 해당 프록시가 실질 검증으로 대체됐다는 증거가 필요하다.
