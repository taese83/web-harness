# Protected Core — 하네스가 지켜야 하는 것 (판단 계층의 canonical)

형식 게이트(validate-harness)는 **필요조건**이다. 이 문서는 그 위의 판단 계층이 보호하는
불변식을 서열화하고, 변경 클래스별 판단 질문과 증거 형식을 고정한다. 소비자: 루트 `CLAUDE.md`
판단 게이트(모든 AI 세션에 로드), `harness-change-reviewer` 에이전트, `validate-contract-hygiene.mjs`
(기계 앵커). 이 문서 자체의 부재는 validate가 fail한다.

## 0. 원칙 — 판단은 기계화되지 않는다, 그러나 강제된다

"과적합인가", "2번째 서비스에도 성립하는가"를 grep으로 판정할 수 없다. 기계가 강제하는 것은
**판단이 실제로 일어났고, 반증 가능한 형태로 기록됐는가**다. 진실성 검증은 fixture·CI 증명이
따라와야 완성된다(주장≠증명 — I1).

## 1. 보호 불변식 (서열 — 충돌 시 위가 이긴다)

- **I1 · 증거의 진실성.** green은 실제 실행 근거가 있을 때만 green이다. 로컬에서 서명 증거를
  위조하지 않는다. 증명 없는 `certified`를 주장하지 않는다(tier 정직). "닫았다"와 "닫는 시늉"을
  구분해 보고한다.
- **I2 · 게이트 강도.** 통과율을 위해 검증을 약화하지 않는다. 골든/생성물이 게이트에 막히면
  그 지점이 곧 결함이다 — 게이트가 아니라 모델링을 고친다.
- **I3 · 일반화.** 계약은 부류(class) 역량이다. 특정 서비스의 이름·백엔드·고정 수치·사고모델을
  인코딩하지 않는다. 기준: **서로 다른 서비스 형태 2개 이상**에 성립함을 명시하고, 참조 서비스는
  eval fixture로만 등장한다.
- **I4 · 고정 비용 예산.** always-read·CLAUDE.md·플러그인 배포 사본 표면은 예산이다. 추가 전에
  제거를 먼저 검토한다(선례: ×3 미러는 2026-08-18 소비자 0 실증으로 제거 — M4 판정). 예산
  갱신은 의식적 행위(baseline 편집 + JUDGMENT 기록)여야 한다.
- **I5 · 프록시 ≠ 품질.** 게이트가 프록시(줄 수·개수·패턴)라면, 프록시를 우회해 "통과"만 만드는
  것은 통과가 아니라 위반이다. 알려진 프록시는 §4에 등록하고, 우회 발견 시 그 사례를 등록부에
  추가한다.
- **I6 · 안전 하한.** 접근성·보안·receipt 게이트는 fast-path에서도 생략 불가. 줄이는 것은
  세리머니뿐이다.

## 2. 변경 클래스별 판단 질문 (커밋 전 — 증거로 답한다)

**모든 변경 공통**: I1(주장/증명 구분), I2(게이트 약화 없음), I5(프록시 우회 없음).

| 클래스 | 추가 질문 | 요구 증거 |
|---|---|---|
| 계약 신설/확장 | I3: 서로 다른 서비스 형태 2개+에 성립하는가? 한 서비스를 역설계하지 않았는가? | 계약 내 `## 일반화 근거` 섹션(형태 2개+ 명명, fixture 검증 여부 명시) — 기계 강제 |
| always-read/SKILL 변경 | I4: 고정 로드가 늘었는가? 조건부로 강등 가능한가? | baseline 대비 증감. 성장 시 baseline 갱신 + JUDGMENT 사유 — 기계 강제 |
| 게이트/validator 변경 | I2: 무엇이 느슨해지는가? G2: 오탐이 정당한 기존 스킬을 잡는가? | 실제 트리에서 오탐 0 실행 로그 + 의도 회귀(seed) 탐지 로그 |
| 스킬/에이전트 신설 | I4: 고정 로드·플러그인 배포 표면 유지비 정당한가? 기존과 중복인가? | 기존 대안 검토 1줄 + README 인벤토리 갱신 |
| fast-path/세리머니 축소 | I6: 안전 하한이 남는가? | 유지되는 게이트 목록 명시 |
| tier/supportLevel 변경 | I1: 라벨이 증거와 일치하는가? | 근거 receipt/golden 링크 |

**기록 형식**: 커밋 본문에 `JUDGMENT:` 블록으로 해당 질문의 답을 1–3줄로 남긴다. 실질 변경
(계약·게이트·스킬/에이전트)은 read-only `harness-change-reviewer`를 먼저 실행하고 결과를 반영한다.

## 3. 예산 (기계 강제 — contract-hygiene)

- 루트 `CLAUDE.md` ≤ 60줄(판단 게이트 마커 `harness-judgment-gate` 필수·삭제 시 fail).
- 스킬별 always-read: `contract-hygiene-baseline.json`의 실측이 상한(ratchet — 성장만 fail).
  미등록 신규 스킬 기본 상한 8.
- baseline 밖 신규 reference 계약: `## 일반화 근거` + 형태 2개+ 필수.

## 4. 알려진 프록시 등록부 (I5 — 정직한 한계 명세)

| 프록시 | 한계 | 우회 사례(실제) | 대응 |
|---|---|---|---|
| 결정 블록 예산 제외 | `solution-design.md`의 `web-harness:solution-design` 블록은 산문 예산에서 빠진다. **유효 JSON의 문자열 필드에 산문을 넣으면 그만큼 예산을 벗어난다** — 파싱은 성공하고 스키마 강제가 없기 때문이다. 탐지 하한은 블록 자체의 15KB 상한까지이고, 그 안에서 rationale·alternatives를 부풀리는 것은 잡히지 않는다 | 실사용 우회 사례 없음(2026-09-02 **사전 등록**). 선행 제안이었던 `web-harness:*` 전면 제외는 실측 우회(45,007B → 7B)가 확인돼 거부됐고, 현재 형태는 그 우회를 세 겹으로 좁힌 것이다 | 제외 조건 3종과 상한을 반증 seed 4종으로 고정했다(`test-artifact-sharding.mjs`). 실질 해소 후보는 **필드별 길이 상한**이다 — 키 allowlist는 `rationale`·`recommended`가 허용 키라서 문자열 팽창을 전혀 막지 못한다. `spec.mjs`가 이미 그 블록을 파싱하므로 소비 지점에 붙일 수 있다. 잔여 경로가 실제로 관측된 뒤에 넣는다 |
| 대조 전용(scoped) 라벨·수행 주체 | `web-verify` 점 0의 ① 경로는 「Phase 4 아님 · tier 없음 · 수행 주체」를 **산문으로 자기신고**한다. 이를 파싱·검사하는 validator가 없고(`대조 전용`·`scoped` 소비자 0건), `_workspace`를 만들지 않으므로 **보고가 대화에만 남고 어떤 원장·파일에도 없다** — 다음 세션은 「verify가 돌았다」와 「대조만 했다」를 구별할 기록이 없다 | 실사용 우회 사례 없음(2026-09-02 **사전 등록**). 다만 이 계약을 만든 계기 자체가 오케스트레이터가 Figma를 직접 읽고 대조한 뒤 그것을 verify로 부를 뻔한 세션이다 | 적용 조건을 `_workspace` 부재로 못박아 tier 산출이 **구조적으로** 불가하게 두었다(라벨은 그 위의 표기일 뿐이다). 실질 해소 후보는 ①의 보고서를 파일로 남기게 하는 것인데, 그러면 `_workspace`를 만들지 않는다는 ①의 이점과 충돌한다 — 실사용에서 혼동이 관측된 뒤에 정한다 |
| TicketProvider 필수부 검사 | `requireTicketProvider`는 필수 4개(name·buildWorkFields·createIssue·resolveIssue — 2026-09-14 FEAT 경로 제거로 WORK 표면으로 교체, provider 해석 지점에서 호출)의 **존재만** 본다 — 존재하지만 계약과 다르게 동작하는 provider는 잡지 못한다. 탐지 하한은 "메서드가 있다"까지이고, 반환 shape·조회 의미론은 미검증이다 | 실사용 우회 사례 없음(2026-09-02 **사전 등록**). 실제로 이 검사를 통과하면서 키 없는 객체를 돌려주는 경로가 있었고(`ticketKeyOf`가 '[object Object]'를 만들어 원장 스키마를 통과), 리뷰에서 발견해 `TICKET_KEY_MISSING` loud-fail로 닫았다 — 형태 검사만으로는 못 잡는다는 실증이다 | provider별 conformance 테스트로 동작을 잰다(GitHub·Jira 양쪽에 같은 케이스를 돌린다). 실질 해소 후보는 반환 shape 검증을 인터페이스에 넣는 것인데, 그러면 순수 계약이 런타임 검사기가 된다 — provider가 셋 이상으로 늘면 재검토한다 |
| 티켓 설정의 비밀 경계 | `configure`는 **허용 키 목록**(`JIRA_QUESTIONS`)과 `baseUrl` 값의 자격증명 패턴까지만 본다. 허용된 키에 비밀을 담는 것(예: `featureField`에 토큰 문자열)은 잡지 못하고, 값 내용 검증은 `baseUrl` 하나뿐이다 | 실사용 우회 사례 없음(2026-09-02 **사전 등록**). 선행 구현이 키 이름 정규식(`token|secret|…`)이었는데 리뷰 실측에서 `jiraPat`·`pw`·URL 내장 자격증명이 전부 통과했다 — 프록시임이 드러나 allowlist로 교체했다 | 유효 키가 질문 목록으로 완전히 열거돼 있어 allowlist가 성립했다(어떤 이름의 비밀도 막고 오타도 잡는다). 잔여는 **허용 키 안의 값**이며, 실질 해소 후보는 필드별 값 형식 검증이다 — 필드가 늘면 유지비가 붙으므로 실사용에서 관측된 뒤에 넣는다 |
| `SURFACE_MODEL` 어휘(2026-09-08 신설) | `route` 또는 `overlay` 선언을 읽는 validator가 0이다. 선언 근거 규칙·Phase 1 → 2의 Phase 3 미정합 고지·`overlay` 전용 접근성 명세 3종이 모두 자기신고다. | — (2026-09-08 사전 등록, 커버 형태 프로브 0건 — 명명 수준) | Phase 3 게이트(route table·404 route)를 유지해 overlay 프로젝트는 개발 진입에서 loud하게 막힌다. 해소에는 커버 형태 프로브와 Phase 3 계약의 서피스 모델 정합이 둘 다 필요하다. 그 전에는 소비자를 늘리지 않는다. |
| `API_CONTRACT` 상태 줄(`api-schema-designer.md`, 2026-09-25 신설) | `provisional`·`confirmed (<출처>)`·`none`을 읽는 validator가 0이다(첫 기계 소비자는 배포본 평가 사례 `predev-stops-at-api-decision`의 `artifact-matches`뿐 — 평가 때만 돈다). 잠정 계약을 사용자 결정으로 올리는 것은 `system-architect`의 산문 규칙이고, 기계가 강제하는 것은 올라간 결정의 `open` 거부(`spec.mjs`)뿐이다. 상태 줄 부재와 데이터 전략과의 어긋남 줄도 자기보고이며 산문으로만 loud하다. | — (2026-09-25 사전 등록) | 해소 후보는 `spec.mjs`가 상태 줄을 읽어 `provisional`·부재면 대응 결정의 실재를 요구하는 것이다. 잠정 계약 착수가 eval에서 발화한 뒤에 넣는다. |
| 배포본 평가 환경 오류 분류(`plugin-eval-checks-lib.mjs`) | 판정에서 빼는 환경 오류는 등록된 흔적(디스패치 미스·인증 실패·디스패처 PATH 누락)뿐이다 — 새 부류의 환경 결함은 플러그인 판정에 섞인다. | 2026-09-25: 평가 세션 PATH에 배포본 bin이 없어 서브에이전트 디스패처가 exit 127이었는데, 0.47.0 receipt와 병합 전 측정이 둘 다 잡지 못했다(트레이스 실측 plan 레인 3·3·5건, ticket 1건). | 러너가 배포본 bin을 PATH 앞에 둔다(`evalSessionPath`). exit 127은 배포본에 디스패처가 있고 이름이 정확히 맞을 때만 환경 오류다. receipt schema 4부터 실행별 `dispatcherNotFound` 건수를 남긴다. schema 3 receipt는 그 뒤 수치의 기준선이 아니다. 패턴 밖 결함은 여전히 사람이 트레이스로 본다. |
| 선언 텍스트 트리거(`PUBLIC_EXPOSURE`·모드 3종, `release-report-policy.mjs`) | 기획 문서의 선언 줄(`- LOCAL_DOMAIN_STATE_MODE: true` 등)을 정규식으로 읽어 QA 요구를 더한다 — 요구를 더하는 방향이라 우회가 아니라 누락이 위험이다. `product-planner`·ingestor의 `## Modes` 형식으로 쓴 문서에서만 발화하고, 인용문 줄도 발화한다. `PUBLIC_EXPOSURE`는 `yes/no` 자리표시자를 거르지 않는다(모드 3종은 `true/false`까지 거른다). | — (2026-09-25 사전 등록) | 모드 선언은 디자인 `absent` 경로의 조용한 QA 탈락을 막는다(ingestion QA는 프로필 capability `external-ingestion`이 켜서 모드 3종만 배선했다). 해소 후보는 선언을 기계 블록(스팩의 모드 필드)으로 옮기는 것이다. |
| 개발 착수 승인 기록(`DEV_START_APPROVED`, 2026-09-25 신설) | Phase 2 → 3(또는 absent 진입) 확인 뒤 오케스트레이터가 마커에 적고, 재진입 가드가 같은 산문으로 읽는다 — 읽는 validator가 0인 자기신고다. 탐지 하한은 줄 존재와 스팩 digest 앞 8자 일치까지다. | — (2026-09-25 사전 등록) | 산출물 유무 추론(골격·기존 코드가 먼저 있으면 공허)을 대체했다. 해소 후보는 Gate 0이 이 줄과 스팩 digest를 대조하는 것이다 — Iterate(fix·change) 프로젝트에는 이 줄이 없어 소급 fail이 되므로 레인별 적용 범위를 정한 뒤 넣는다. |
| 배포본 로드 닫힘(`import-closure-lib.mjs`·빌드 게이트, 2026-09-25 신설) | 빌드는 배포본의 상대 import(줄 맨 앞 정적 import·export … from, 리터럴 동적 import)만 본다. 변수로 조립한 경로·`createRequire`·bare specifier·경로 문자열로 읽는 파일(`join(…, 'validators/…')`)은 못 본다. 빌드 배선 검사는 소스 텍스트 대조다. | 0.48.0까지 품질 실행기와 릴리스 게이트 훅 체인이 배포되지 않는 validators/를 불러 설치본에서 로드부터 실패했다 — PreToolUse 훅의 exit 1은 비차단이라 게이트가 조용히 꺼졌다. | 회귀가 실제 페이로드를 임시 폴더에 빌드해 닫힘 0건과 훅 전부의 로드를 확인한다. 경로 문자열로 validators/를 읽던 배포 스크립트 둘은 배포에서 뺐다. 해소 후보: 배포 스크립트 전부의 로드 스모크. |
| `QUESTION`·질문지 채널(`source-normalization.md`, 2026-09-08 신설) | 원문이 답하지 않은 제품 결정을 `QUESTION(Q-NNN)`으로 `00_source/author-questions.md`에 내보내게 하지만 이 마커를 읽는 기계 소비자는 0이다. 제품 결정과 표현 기본값의 분류는 판단이라 기계화 대상이 아니다. | — (2026-09-08 사전 등록, 어느 프로브에서도 실행되지 않은 명명 수준) | `plan-reviewer`가 `ASSUMPTION` 오분류·잔존 `QUESTION(Q-NNN)`을 read-only로 지적한다(게이트 아님). 해소 후보는 인계 판정에서 잔존 `QUESTION`을 세는 것이며, 먼저 eval 시나리오로 이 경로를 발화시켜야 한다(미이행). |
| 모션 역할 공백 보고(`--motion-role`, 2026-09-08 신설) | 반복 모션을 서술하면서 주기 역할 토큰을 선언하지 않았는지만 본다. 주기 토큰이 있는데 인터랙션 토큰을 인용하는 변종, 이름 규약에 의존하는 역할 판정, 닫힌 한국어·영어 어휘 밖 표현, 구현 쪽 사용은 못 잡는다. 오탐은 자연어 중의성에서 온다. | 2026-09-08 18개 프로젝트 드라이런에서 HOLE 4건 중 참 2·오탐 2였다. | 오탐 0을 못 만들어 차단이 아니라 보고(exit 0)로 둔다. 토큰 인식을 넓히면 참 양성이 PASS로 뒤집히므로 넓히지 않는다. 반증 `motion-role-period-token`이 배선을 고정한다. |
| `supplied` 공급원 자기보고 | `supplied` 선언과 `00_source/`의 스냅샷·해시·인벤토리를 어떤 validator도 내용 대조하지 않는다. 오케스트레이터 fetch 스냅샷 머리말도 무검사이며 탐지 하한은 `00_source/fetched/` 파일 존재까지다. | — (2026-09-02 사전 등록) | `absent`의 대가는 `specTier`로 기계화됐지만 `supplied`의 진실성은 아니며, 이 비대칭을 `provenance-contract.md` §2에 명시했다. 해소 후보는 정규화 산출물의 인벤토리 인용 부재를 HOLE로 올리는 것이며 미인용 사례 관측 뒤에 넣는다. |
| 공급물 소비 대조(`source-consumption`, 2026-09-08 신설) | `source-index.md`의 `소비 지점`과 산출물 `## Source Trace`의 스냅샷 파일명을 양방향 대조하므로 연결의 실재만 잡고 내용 반영은 못 본다. 파일명 문자열 포함이라 동명 문서를 구별하지 못하고, 전 행을 `없음(사유)`로 적으면 통과한다. 파일이 없으면 `SKIPPED`다. | — (2026-09-08 사전 등록, 신설 시 실제 트리 2곳에서 HOLE 발화) | 형식을 `source-artifacts.md`「인벤토리 표」로 고정하고 반증 `source-consumption-bidirectional`로 배선을 고정했다. 무문서 SKIP은 `design-binding`과 같은 미해결이다. 해소 후보는 `PLAN_SOURCE`·`DESIGN_SOURCE`가 `supplied`인데 인벤토리가 없으면 HOLE로 내는 것이다. |
| `specTier` 승격 방향성 | `spec.mjs`는 이전 확정과 비교하지 않아 `verifiable` 스팩을 `acceptanceSource: "absent"`로 재확정하면 차단 없이 `unverifiable`로 강등된다. 강등은 인계를 열지 않지만 수용 기준 계열 검사(`checkAcceptanceCoverage`·handoff `acceptance`)를 건너뛰게 한다. | — (2026-09-02 사전 등록) | 강등 금지는 `provenance-contract.md` §3의 문서 규칙이고, 기계 방어는 `spec-ledger.jsonl`의 사후 탐지뿐이다. 해소 후보는 `lockSpec`이 원장 최신 `specTier`를 읽어 강등 시 사유를 요구하는 것이며, 강등 시나리오가 2형태 이상 관측된 뒤에 넣는다. |
| 소유 커버리지 게이트(`validate-ownership-coverage`, 2026-09-08 신설) | `golden/` 트리와 `AGENT_OWNERSHIP` ∪ spec `layerMap`을 대조하므로 골든에 없는 경로는 침묵한다. spec 없는 골든(`react-vite-spa`)은 미측정이고, 이중 소유는 세지 않는다. | 신설 시(2026-09-08) hybrid 골든에서 무소유 3건(`index.html`·`.gitignore`·`.env.example`)을 찾아 닫았다. | 새 선언 표면 없이 골든·registry·spec만 쓴다. 확장 후보는 `react-vite-spa` 골든에 spec 추가와 이중 소유 검사이며, 후자는 정당한 겹침 구별이 필요해 별건이다. |
| 스크립트 400줄 제한 | 줄 수 ≠ 복잡도 | import 세미콜론-병합으로 399줄 맞춤(기존 관행 + 2026-08 세션도 동일 우회) | 병합으로 줄이지 말 것. 실질 해소는 validator 모듈 추출 — **첫 실질 추출 발생(2026-08-18)**: UI 레인 검사 추가로 한도 초과 시 병합이 아니라 섹션-리더 블록을 `validate-section-readers.mjs`로 추출해 해소. 잔여 인라인 블록의 추출은 계속 TODO |
| always-read 카운터 | `<!-- always-read -->` 앵커가 없는 스킬은 여전히 `항상…읽는다` 산문 매칭이라 영어 서술·조건부 읽기를 놓치고 앵커가 엉뚱한 줄에 걸릴 수 있다. `alwaysReadBytes`는 토큰이 아니라 바이트 근사이며 앵커 미배치 스킬은 미등록이다. | 2026-08-18 리뷰: 정규식이 web-orchestrator의 무관한 줄에 걸려 실제 목록이 미집계인 채 baseline과 우연히 일치했다. | 앵커가 산문 휴리스틱을 이기고, `alwaysReadBytes` 차원과 README `<!-- inventory:entry-cost -->` 공표 대조로 로드 비용을 잰다. 앵커 미배치 스킬 이관은 소급 fail 금지 관례로 남아 있다. |
| 상류 결정 도달(upstream-decisions) | ID 인용만 보므로 인용만 붙이고 내용을 옮기지 않아도 통과하고, 다른 낱말로 반영돼도 지적한다. ID 체계·표제 형태는 로그가 스스로 선언한 것을 쓴다. 대체 판정은 계약된 표제 한 줄로만 하며 피동·부정이면 제외하지 않는다(fail-closed). | 2026-08-30 track: 로그에 없는 D-036을 프리뷰와 출하된 `piece-path.ts`가 인용하고 있었다. | 통과 기준은 낮추지 않는다(해소 비용은 인용 한 줄). 면제는 후속 결정이 정본에 도달한 경우에만 한다. 내용 반영 판정은 design-reviewer 몫이다. |
| 테스트 실행 수(`quality-output-summary-lib`, 2026-09-25 신설) | vitest·jest·playwright 요약 줄과 istanbul·v8 표만 읽는다. 모르는 러너 형식이면 수가 null이고 `report-test-qa`는 WARN으로 낸다 — WARN은 게이트를 통과한다. 러너 include 패턴이 하네스 탐색과 달라 0개를 실행해도 요약 줄이 없으면 이 경로로 빠진다(「No test files found」·「No tests found」는 0개로 읽는다). | — | 영수증 exit code와 발견 파일 수는 그대로 막는다. 해소 후보: `runMode: all` 영수증에서 수 미상을 BLOCKED로 올린다. |
| Must 기능 인용(`report-test-qa`, 2026-09-25 신설) | cut되지 않은 Must 기능마다 발견된 테스트 파일 중 하나가 그 FEAT·TC ID 문자열을 담는지만 본다. 주석 한 줄로 통과하며 그 테스트가 기준을 실제로 검증하는지는 못 본다. 기능 표의 우선순위·범위 열 이름에 기댄다. | — | 잡는 것은 **아예 없는 것**이다. 진짜 대조는 code-reviewer와 변이 표본의 몫이다. |
| 티켓 완료 조건(TC 인용) | 텍스트에 TC ID가 있는지만 본다 | 주석만 달아도 통과하며, 그 테스트가 그 기준을 **실제로** 검증하는지는 못 본다 | 잡는 것은 **아예 없는 것**이다 — 그것만으로 "완료 주장"의 하한이 생긴다. 실질 판정은 code-reviewer의 몫 |
| 완료 조건의 유예 채널(`[유예: 사유]`) | 계획이 스스로 완료 조건을 낮출 수 있다 — 계획과 코드가 **같은 PR**에서 수정 가능하므로 경계가 soft하다 | 첫 판은 산문 표현(`검증 불가`·`실행 불가`)으로 유예를 판정해 **내용어와 충돌**했다 — `실행 불가 안내를 띄운다`는 정상 TC가 유예로 분류돼 미인용인 채 통과(fail-open, 리뷰 MEDIUM) | 산문 대신 **사유 필수 명시 마커**로 계약했다. 유예·미인용·탈출구(`--accept-incomplete`·`--accept-unverified-scope`) 사용은 전부 **원장 레코드에 남는다** — 사후에 구별되지 않으면 "의식적 인수"는 휘발성 주장이다. 계획-코드 동시 수정 우회는 여전히 열려 있고, 그 판정은 리뷰어의 몫이다 |
| 일반화 근거 섹션 | 존재·형태만 검사, **진실은 미검증** | — | fixture/2번째 서비스 검증 전까지 해당 계약은 "명명 수준" — 계약에 자기 표기 |
| 골든 5/7 로컬 green | `validate-certified-evidence`는 T1 하한만 강제하며 `t1-summary.json` 자체의 진위(위조 JSON)는 게이트 밖이다. compatible 어댑터의 스킬-내러티브 검사는 스킬 부재 시 skip된다(react-vite-spa 미해결). | 2026-08-18 M3 정찰: 라벨이 증거에 묶이지 않아 증거가 더 약한 react-vite-spa가 certified인 라벨 역전 상태였다. | certified→증거(골든·locked profile·`ISOLATED_VERIFIED`·QA 전부 PASS)를 기계 강제하고 코드별 음성 seed로 무장을 증명한다. receipt 진위는 validate-isolated-cohort와 T2 서명 attestation 몫이다. 8/8 production-grade는 별도다. |
| UI_LANE 일치 검사 | `validate-ui-lane.mjs`는 `src/`의 모듈 지정자 접두어(정적·재수출·side-effect·dynamic import·require)를 레인의 정의적 시그널과 대조할 뿐이다. 문자열 조립 지정자·import 없는 모방·의미 수준 이탈은 못 잡는다. code-reviewer의 vendored-a11y 검사는 산문 지시다. | 2026-08-18 리뷰가 재수출·dynamic import 미탐과 미지 overlay 레인의 조용한 폴백을 실측했고 같은 커밋에서 닫았다. | 미지 레인은 `UNKNOWN_LANE_DECLARATION`으로 loud 실패하고 회귀 11종이 고정한다. eval 시나리오 2종은 실행 전이라 maturity는 contract-only다. |
| 재진입 로드 맵(reentry-map) | 로드 인덱스는 원본(SKILL.md·execution-contract 등)이 바뀌어도 스스로 fail하지 않는다 — stale한 맵을 따라도 기계는 침묵한다(강제 층은 로드 독립이라 게이트는 안 뚫리지만, 안내 층이 낡은 지시를 재생산) | — | 산문 완화만 존재: SKILL.md 우선 + 발견자 수리 책임 + 애매하면 전체 진입 폴백(reentry-map.md 사용 규칙). 재진입 훅·마커 층이 얹히면 사람이 이 파일을 덜 열게 되므로, 로드 지시 대조 기계 검사는 미해결 TODO — day-2 파일럿에서 stale 발생 여부를 관찰해 우선순위 결정 |
| SessionStart 훅 주입 채널 | always-read baseline ratchet(`countAlwaysReadRefs`)은 마크다운 마커만 계측한다 — 런타임 훅의 stdout 주입(`detect-harness-project.mjs`, `_workspace/` 프로젝트에서만 851B 실측(2026-09-24) — 재진입 안내 + 진입 명령·하네스 모드 안내)은 이 채널 밖이라 **커져도 기계가 못 잡는다**(2026-08-18 리뷰 등록) | — | 현 주입은 조건부 소량이며 안내 층 한정(계약 사본 동기화 실패 시에만 1줄 더). 훅이 디스크에 쓰는 것은 `_workspace/.contracts/` 하나이고 어떤 실패도 exit 0이다. 이 채널로 주입을 늘리는 변경은 리뷰 필수 + 이 행 갱신. 훅 주입량 기계 계측(스크립트 stdout 상한 검사)은 미해결 TODO |
| 하네스 모드 안내 채널(`harness-session-mode.mjs`, UserPromptSubmit) | 사용자가 `/wh`(또는 `/team-flow`)로 켠 세션에서만 매 요청에 340B 안팎(실측, 라우팅 문서 절대 경로 포함 — 경로 길이만큼 달라진다)을 붙인다 — 모델이 따르는 안내라 평문 요청이 하네스 흐름으로 처리된다는 보장은 아니다. 표시는 홈의 세션 파일이라 세션 ID가 바뀌면(새 세션) 다시 켜야 하고, 7일 넘은 표시는 켤 때 치운다. 슬래시 원문·세션 ID 전달과 문맥 주입은 프로젝트 스킬(`disable-model-invocation: true`)로 실측했고 플러그인 네임스페이스 스킬(`/web-harness:wh`)로는 실측하지 않았다. 자동 요약 뒤 세션 ID 유지는 실측하지 않았다(`--resume`은 유지 — 2026-09-24 실측). | 실사용 우회 사례 없음(2026-09-24 사전 등록). | 켜지 않은 세션에는 0B. 주입량 기계 계측은 SessionStart 행과 같은 미해결 TODO. 회귀 `test-harness-session-mode`·반증 seed. |
| 스타일 타일 자기채점 대비 검사 | 발산 후보의 WCAG 대비 하한을 후보 디렉터리로 **복사된 사본 안의 내장 JS**가 자체 채점한다 — "사본 수정 금지"는 산문 규칙뿐이고, 사본의 검사기를 고쳐 FAIL을 PASS로 위장해도 잡는 독립 기계가 없다(선례인 wh-overlay도 존재 검사만 있음). 오케스트레이터의 렌더 판정도 사본 무결성을 재검증하지 않으면 self-report를 읽는 것이다(2026-08-18 리뷰 등록) | — | 발산 단계 안내·조기 탈락용 게이트로 한정 — 릴리스 하한의 정본은 여전히 Phase 4 접근성 QA(중복 채점이지 대체 아님). 렌더 판정 시 사본과 assets/ 원본의 digest 대조는 미해결 TODO. 실측 receipt: docs/efficacy/receipts/style-tile-selfcheck.md |
| 심볼 수준 대조(`resolveSymbols`, 2026-09-08 신설) | 파일명에서 유도한 심볼로 개명·「그 이름이 어디에도 없음」·껍데기를 가르며, 프로젝트 TypeScript가 없으면 `NOT_MEASURED`다. 이름까지 바뀐 개명은 「없음」으로 읽히고, export가 하나라도 있으면 통과하며, `export * from`·라우트 배선·props는 보지 않는다. 파서 호출(`exportedNames`)은 CI에서 검증되지 않는다. | 초안 회귀가 gitignore된 `workspace/track`에 걸려 CI에서 조용히 skip됐고(반증을 꺼도 exit 0) 같은 세션에 세 번 재발했다. | 막지 않고 보고한다 — `validate-release-gate` stderr와 릴리스 보고 문서가 `symbolBinding.state`를 옮기며 인용 필드 실재만 회귀가 대조한다. 반증 `symbol-binding-rename-vs-unbuilt`·`symbol-binding-hollow`가 배선을 고정한다. 테스트의 `workspace/` 경로 리터럴 검사는 미구현이다. |
| 설계→코드 결속(`routeBinding`, 2026-09-08 신설) | `layout-spec` 표 행의 백틱 소스 경로가 실재하는지만 보고한다(막지 않는다). 빈 파일도 통과하고, 표 밖 선언·확장자 없는 경로·행당 두 번째 경로는 세지 않으며, `src`·`api` 접두 밖은 `UNRECOGNIZED`로 센다. 비정상 펜스에서는 `NO_DECLARED_PATHS`로 떨어질 수 있다. | 신설 시(2026-09-08) 5개 프로젝트에서 선언 32경로 중 15건 부재가 전부 실제 divergence였다. | 경로만으로는 미구현과 개명을 구별할 수 없음이 실측돼 차단을 뺐고 회귀가 그 판정의 부활을 막는다. 심볼 수준은 `resolveSymbols`가 맡는다. 반증 `route-binding-reported`가 배선을 고정하고 `validate-release-gate` stderr·`release-manager`가 소비한다. |
| 시안 구현 축별 대조표(IMPLEMENTATION-VERDICT) | `designRoundSummary`는 `SELECTED_CANDIDATE`가 있을 때 대조표 부재만 막고 행 수 부족·빈 칸은 note로 남긴다. 축 이름과 대조의 진실성은 검사하지 않아 5행을 그럴듯하게 채우면 통과한다. 실제 트리 발화는 0건이고 fixture 6건으로만 고정됐다. | motor-lab v4에서 색만 바꾼 리컬러가 시안 구현으로 릴리스까지 통과했다(receipts/implementation-completeness-incident.md). | 계약이 대조 항목 최소 5종과 위치를 고정하고, 시스템-우선 왕복 규율의 탐지망으로 쓴다. computed-style 자동 대조는 미해결 TODO다. |
| SELECTED_CANDIDATE 마커(시안 선정) | 판정 기록(RENDER-VERDICT.md)의 고정 줄은 오케스트레이터 self-attestation — 소비자(콘솔 인덱서)는 후보 목록 소속만 검증하고 **마커 값이 판정 산문의 실제 결정과 일치하는지는 기계 미검증**(산문 "1순위 b"에 마커 candidate-a여도 콘솔은 a를 현재 디자인으로 노출) | — | 후보 목록 바운딩(목록 밖 값 무시) + 마커 부재 시 "선정 기록 없음" 정직 표시(추정 금지)가 코드 방어. 마커-산문 일치는 리뷰 몫. 차순위 교체 시 마커 갱신 의무는 계약 몫(2026-08-19 리뷰 등록) |
| maturity의 eval-covers 검사 | 시나리오의 명시적 `covers`만 세며 스킬 존재만 검사한다. 진입 스킬 실존과 assertion의 에이전트 실존은 대조하지만(`validate-workflows-and-evals`), 에이전트 후보는 실존 에이전트의 역할 어휘로 끝나는 이름뿐이라 지금 아무도 쓰지 않는 어휘(`-writer` 등)로 끝나는 옛 이름은 못 잡고, 반대로 그 어휘로 끝나는 일반 하이픈어(`package-manager`·`query-builder`)는 없는 에이전트로 잡는다(loud). 진입 레인에서 그 스킬에 도달하는지·실행 receipt는 미검증이라 선언이지 실행 증거가 아니다. | 2026-09-11 이관 첫 판이 feature-add를 부르지 않는 `/wh change`에 `covers: ["feature-add"]`를 적었고 적대 리뷰가 잡았다. | 우회 비용은 한 토큰이지만 의도적·diff 가시적이다. 대응 후보는 covers ⊆ 위임 그래프 대조다. 최종 진실은 시나리오 실행 결과다. |
| preview render-source 마커 스캔 | 파일 내 **문자열 포함**만 검사 — 해당 파일이 실제 로드·실행되는지 미보장(decoy 파일로 우회 가능) | — | .mjs 포함은 2026-08-28 제거된 delta 모드의 잔재였고 함께 걷었다(현재 스캔은 html·js). 실질 진실은 브라우저에서의 앵커 렌더 확인(TC 검증) 몫 |
| 스냅샷 바탕 앵커 검사(anchor-map) | 앵커가 옳은 요소에 붙었는지는 검증하지 않아(캡처는 셀렉터 유일성만 확인) 앵커 1개짜리 형식적 바탕도 통과한다. 개수 하한은 맥락 route가 정상이라 두지 않았다. | 2026-08-28 적대 리뷰: anchor-map 없이 캡처한 배지 0개 바탕 위에서 승인이 진행됐다. | 강제 지점을 캡처가 아니라 승인 판정(`readBaseSnapshot`)으로 옮겼다 — 앵커 0개 INVALID, 앵커는 meta가 아니라 HTML에서 읽고, traceability 밖 앵커·부트스트랩 부재는 loud다. 반증 4건이 고정한다. |
| 스냅샷 바탕 문자열 치환 | 치환은 텍스트 노드와 속성 5종(`value`·`title`·`alt`·`placeholder`·`aria-label`)만 덮는다. 그 밖의 속성·HTML 주석·CSS `content:`·이미지 픽셀은 미커버이고, 6자 미만 어휘는 전체 일치로만 보존된다. | 2026-08-28 적대 리뷰: 부분 일치가 문자열 전체를 보존해 이메일이 남는 등 fail-open 3건이 실측됐다. | 3건은 구간 단위 보존·title 마스킹·유니코드 속성 기반 치환으로 닫고 회귀로 고정했다. 한계는 meta.json `limits`에 산출물마다 기록된다. 근본 방어는 시드 데이터로만 캡처하는 것이다. |
| preview 소스 입력 `(.md\|/)` 그룹 필수 검사 | `existsSync` **존재-only** — 빈 디렉토리·빈 파일도 required를 충족하고 digest에 아무 것도 안 실린다. 결함 8호 정합화(2026-08-19)로 required 그룹이 3종→5종으로 확장되며 이 프록시 표면도 함께 커짐 | — | 내용의 진실은 preview 구조 검사(TC 커버리지·앵커)와 sharding validator(INDEX↔절 파일 양방향) 몫 — 이 검사는 입력 존재·digest 고정만 담당. 빈 그룹 감지(파일 0개 시 경고)는 미해결 TODO |
| ingestion ancestor 스캔의 사촌 제외 마커 | `_workspace/01_plan` **디렉토리 존재**로 "독립 하니스 release root"를 판정(결함 11호 정합화, 2026-08-19) — 이름 기반 휴리스틱이라 그 구조까지 위조한 decoy는 ancestor 스캔을 여전히 회피 가능. 기존 `SOURCE_SCAN_EXCLUDED_DIRECTORIES`(dist·test 등 이름 제외)와 같은 클래스의 한계 | 빈 `_workspace/` 위장(리뷰 실측 지적) → 실질 마커(01_plan)로 강화 + 우회 seed 회귀 고정(2026-08-19) | detector는 bounded consistency check이며 난독화 부재의 증명이 아니다(web-profile-contract 기존 명세) — 외부 수집 여부는 intake·review에서 명시 선언이 정본. 구조 위조 decoy 감지 강화는 미해결 TODO |
| 콘솔 frame-src loopback 와일드카드 | 열거식 CSP는 동적 프록시 포트가 문서 로드 후 배정되는 구조적 경합이 있어 `127.0.0.1:*`로 완화(2026-08-10) — DOM-XSS에 대한 심층방어 폭이 loopback 전 포트로 넓어짐 | — | iframe src는 서버 계산 값만, 신뢰 경계는 postMessage origin 엄격 검증 + 프록시 대상은 launch.json 포트 allowlist. script-src 'self'·frame-ancestors 'none' 유지 |
| targetless 승인의 구조화 범위 self-report | 대상 없는 CR 승인의 `affectedFeatureIds`는 apply 결과의 자기보고다. 스냅샷이 변경 전에 찍혔는지는 승인 레코드가 있을 때만 `LATE_SNAPSHOT` 바닥값으로 부분 검증되고, ID가 유지된 채 내용만 파괴되는 경우는 못 잡는다. 원장까지 지우면 초기화된다(tamper-evident). | 2026-08-11 파일럿: apply가 기존 plan을 재작성해 승인 TC를 파괴했는데 존재 검사로는 미탐이었다. | `validate-plan-delta.mjs`가 변경 전 안정 ID 인벤토리와 대조해 `UNDECLARED_REMOVAL`을, 원장 `.plan-snapshots.jsonl`로 `RESNAPSHOT`을, ID 없는 문서는 `NO_STABLE_IDS`로 잡는다. 내용 보존은 사람의 절 보존 확인 몫이고 commit-anchor는 미해결 TODO다. |
| 구현 검증 증거(implementation receipt) | 존재·형식(승인 TC 부분집합·한 줄 ≤300자)만 검사 — 자유 텍스트 self-attestation이며 테스트가 실제로 실행·통과했는지 자동 보장하지 않음 | — | 증거에 명령·통과 요약·시점 기록 의무는 계약 몫(runbook). 테스트 러너 연동 자동 수집은 미해결 TODO |
| 완결성 게이트 무산출 가드 | 무산출은 실제 신호지만 파일이 있다고 완성은 아니다. `--allow-no-output`은 오탐 회피용으로 남용 가능한 self-attestation opt-in이다. truncation 스캐너는 줄 경계 절단 recall 77~93%이며 마침 완결된 지점의 절단은 원리적으로 못 잡는다. | 초기 구현이 .ts/.js만 세어 비-code 산출물을 오탐했고, 2026-08-18 합성 리플레이가 정규식 리터럴 오탐 2건을 잡았다(둘 다 해소). | 무산출은 anyFilePresent로, truncation은 별도 스캔으로, 의미결함은 typecheck로 본다. `--allow-no-output` 남용은 리뷰에서 확인한다. 실측 오탐 원문은 `test-spawn-completion.mjs` 회귀로 고정했다. |
| tech-advisor 버전 pin | 이전엔 install 전까지 자기진술 프록시(§ 위 "일반화 근거"와 성격 유사) — "웹 리서치 교차확인" 주장이 실재 registry와 불일치 가능 | seminar-booking 실증: TS 6.0.0(존재하지 않는 버전) + typescript-eslint↔TS7 peer 비호환(install/lockfile은 WARN만) | **기계검사로 승격(2026-08-11)**: `validate-dependency-pins.mjs`가 존재성+peer 호환을 install 전 검사(Tessl 착안). 단 registry 없는 환경이면 self-attestation으로 강등(install 시점 검증에 위임), 파싱 불가 범위는 미검사 |
| 재개 매니페스트 outputs 자기선언 | `resume-manifest.mjs`는 선언된 outputs의 존재·비-truncation만 보므로 처음부터 적게 선언한 경우는 미탐이다. 원장 `.plan-locks.jsonl`과 planLock을 둘 다 지우고 재확정하면 위조된 `locked`가 성립한다(tamper-evident이지 tamper-proof 아님). | 2026-08-12 재현: 스팩을 매니페스트 안에만 두자 planLock 삭제·축소 후 재확정 두 우회가 exit 0으로 통과했다. | `validate-spawn-plan --lock`이 FITS 계획 digest를 매니페스트 밖 append-only 원장에 박고, TAMPERED는 fail-closed, 다른 digest 재확정은 거부(exit 2)한다. 원장 입출력은 `evidence-log-lib.mjs`가 정본이다. 초기 과소 선언은 계약 몫이고 commit-anchor는 미해결 TODO다. |
| 요구사항 의무 진술 검사 | `validate-requirements-notation.mjs`는 REQ 층의 **의무 진술 존재**와 AC 층의 **EARS 구조 적합**(응답 명시, 조건·트리거 분류)을 본다 — 진술·AC가 **옳은지**·요구사항을 빠뜨리지 않았는지는 여전히 미검증이다. EARS 패턴 분류는 **정규식 기반 추정**이며, 특히 `unwanted`는 한국어에서 구문이 event와 같아 **의미 키워드**(실패·오류·초과·차단 등)로 잡는다 — 어휘 목록 밖의 표현은 놓친다. Must 범위 판정(`### Must`)도 문서 구조에 의존한다 | 개발 중 실측에서 **게이트 자신이 세 번 틀렸다**: 줄끝 마커로 `$` 앵커 실패(100% 오탐) · AC 줄을 헤드라인으로 오인해 상위 REQ의 본문 강탈 · 종결 어미 뒤 여는 괄호 미허용. 셋 다 "문서가 나쁘다"가 아니라 "검사가 틀렸다"였고 회귀로 고정 | 실제 파일럿에서 **16/16 PASS·오탐 0** 확인 후 채택. REQ 블록을 하나도 못 찾으면 통과가 아니라 `NO_REQUIREMENTS`(검사 미수행)로 보고. 진술의 **품질**은 여전히 plan-reviewer와 사람 몫 |
| 산출물 언어 검사 | `validate-output-language.mjs`는 **마크다운 헤딩만** 본다 — 본문·표·코드펜스는 검사하지 않는다(샘플 데이터·고유명사로 한글이 정당하게 들어가 오탐이 크기 때문). 즉 헤딩만 영어이고 본문이 한국어여도 PASS가 난다. 검사는 **단방향**(en 선언에 한글 헤딩 금지)이라 ko 선언의 품질은 보장하지 않는다. `outputLanguage` 자체는 오케스트레이터의 자기선언이며, 요청 언어를 옳게 판별했는지는 미검증 | 실측(2026-08-12): seminar-booking을 `outputLanguage: en`으로 선언하면 한글 헤딩 **236건** 검출 — 게이트가 실제 산출물에서 작동함을 확인 | **미선언은 통과가 아니라 `UNDECLARED`(검사 미수행)로 보고**해 "영어 지원"이 조용한 통과로 성립하지 않게 한다. 헤딩 밖 본문 언어까지 보는 것은 오탐 비용이 커서 미해결 TODO — 실질 품질은 산출물을 읽는 사람 몫 |
| 계약 마커의 자연어 의존 | validator 다수가 **한국어 문자열 자체**를 마커로 썼다(`항상…읽는다`, `## 일반화 근거`, `미구현`, `대상 화면/기능` 등). 본문을 영어로 옮기면 매칭 실패 → 마커 0건 → **조용히 통과**로 게이트가 장식이 된다(번역이 게이트를 끄는데 CI는 green) | 실측(2026-08-12): 한국어는 SOV라 `항상 … 읽는다`가 always-read 목록을 감싸 조건부 읽기와 구분되지만, 영어는 SVO라 그 경계가 없다 — seed에서 lazy 매칭은 참조 0건(MARKER_LOST 오탐), 전방 윈도우는 조건부 참조까지 삼켜 과대계수(3 > baseline 2) | 한국어·영어·중립 앵커를 모두 인식하되, **baseline이 마커 존재를 전제하는데 사라지면 MARKER_LOST로 FAIL**(vacuous pass 차단). 번역 시 정확한 경계가 필요한 곳은 자연어가 아니라 중립 앵커 `<!-- always-read --> … <!-- /always-read -->`를 **필수**로 쓴다. 영어 자연어 매칭은 best-effort이며 틀릴 때 과대계수(FAIL) 쪽으로 틀리므로 저자가 앵커로 옮기게 된다 |
| A/B 암 토글의 self-attestation | `run-eval-executor.mjs --arm off`는 게이트를 코드가 아니라 프롬프트로 끈다. 게이트 미호출과 telemetry `+gatesOff` 라벨이 둘 다 실행기의 지시 순응에 의존해, 순응하지 않으면 암이 조용히 오염된다. | — (Stage 1 미착수, 실측 오염 사례 없음) | 기본값 `on`·오타 exit 2이고, `evalExecutorContract`가 서브에이전트·샌드박스 경로의 `--arm`을 차단하며, 대상은 `eval-runs/*/fixture`로 한정한다. `checkArmCompliance()`는 문자열 매칭이라 오염 탐지 하한이지 준수 증명이 아니다. Stage 1 receipt 필수 항목이다. |
| 마커 무결성 카운트 | `validate-marker-integrity.mjs`는 등록 마커의 출현 수만 세므로 decoy로 카운트를 유지하면 의미 보존을 보장하지 못한다. 디렉터리로 등록된 경로는 `.md`만 재귀해 하위 비-.md 앵커가 미집계다. 존재-류 timeseries 2종은 산문 삭제·앵커 존치 decoy에 구 인라인 검사보다 느슨하다. | 2026-08-18 `component-designer.md` 열 이름 누락과 2026-08-20 비-.md 명시 파일 누락이 baseline과 self-consistent하게 통과했다. | 생성물 쪽은 validate-artifact-sharding의 절 행 커버리지가 의미 수준으로 보고, 소스 쪽은 `consumer-read-protocol` 앵커 카운트로 좁혔다. 명시 파일은 확장자 무관 스캔으로 고쳤고 스크립트 앵커는 파일 단위 등록이 규약이다. |
| fit 게이트 임계 완화 옵션 | `--max-outputs`/`--max-read-tokens`는 게이트 자신을 느슨하게 만드는 통로다 — 계약은 "완화는 의식적 행위, 사유를 남긴다"고 요구하지만 **사유 기록을 기계로 강제하지 않는다**(산문 의무) | 초기 등록 시 상한이 없어 `--max-outputs 9999999`로 게이트를 사실상 무력화 가능했음 — 리뷰 지적으로 bash 정책에 상한 도입(2026-08-11) | bash 정책이 상한 강제(outputs ≤32, read ≤200k tokens)로 **완전 무력화는 차단**. 그 아래 구간의 완화는 여전히 자기진술 — 남용은 리뷰에서 확인. 매니페스트 `overrideReason` 필드 강제는 미해결 TODO |
| fit 게이트 readMode(injected) | `"readMode": "injected"`는 발췌를 주입했다는 자기진술이며 게이트가 확인하지 않는다. 이 한 필드로 측정치가 약 8배 작아지고, CLI가 아닌 파일 내용에 살아 명령줄에서 보이지 않는다(출력에는 `readMode=`로 노출). | 2026-08-11 seminar-booking 재구성: 좁은 선언은 4건 중 1건만, 넓은 선언은 4건 전부 REFUSE였다. | 미지정·오타는 `browse`로 fail-safe이고 browse는 상위 디렉터리 전개로 측정을 조인다. 그래도 실측 4건은 임계 미만이라 미탐이다. 발췌 근거 포인터 요구는 `overrideReason`과 묶어 미해결 TODO다. |
| 스폰 사전 fit 게이트 reads 선언 | `validate-spawn-plan.mjs`는 선언된 `reads`의 바이트만 재므로 선언 밖 읽기는 FITS를 받고도 runaway가 난다. 토큰은 바이트 기반 추정이고 임계(8 outputs·60k tokens)는 단일 서비스 실측값이다. browse 전개는 산출물 디렉터리 단위라 샤드 소비자 경계와 어긋날 수 있다. | 2026-08-11 재구성: 베이스라인 계획 6건 중 OUTPUT_FANOUT 2건만 걸리고 4건은 10.9k~23.4k로 미탐이었다. | 선언이 아티팩트로 남고 디렉터리는 하위 전체로 전개되며 `resume-manifest`와 매니페스트를 공유한다. 실측 폭주는 162k 추정으로 REFUSE하고 정당한 스폰 5건은 오탐 0이다. runaway 발생률 감소 효능은 미실증이다. |
| TC 실행 exit code 판정(콘솔 QA 탭) | `/api/qa/tc-run`은 `test:tc` 스크립트의 exit code를 그대로 기록하므로 `--passWithNoTests` 류 설정이면 매칭 0건이 통과한다. 재테스트 필요 표기는 `_workspace/04_qa`만 제외한 git 스탬프의 파생 신호이며, 비-git이거나 커밋 베이스라인이 없으면 '변경 감지 불가'로 표기한다. | 2026-08-20 정적 토큰-발견 뱃지가 프로덕션 소스 토큰을 테스트로 오인할 뻔해(TC-001-1) 실행 기반으로 교체했다. | 사전 선언 명령만 실행하고(미선언 404·동시 1개·300s) 기록은 append-only `tc-runs.jsonl`에 사실만 남긴다. no-match 성공 방지 검사와 '-t 0건 매칭' 회귀는 미해결이며 러너 기본값은 실러너 증거가 없다. |
| 티켓 식별자 원장(팀 워크플로우) | append-only 원장(`ledger.mjs`)이 재청구 1차 멱등 가드인데, 파일을 통삭제 후 재구성하면 청구 이력을 위조할 수 있다(tamper-evident, 외부 앵커 없음). 개발자별 로컬 원장의 크로스-머신 분기는 처리하지 않는다. | 2026-08-21 라이브 실증: 원장-우선 가드가 색인 지연 중복 발행을 막았다. | 쓰기는 공유 `evidence-log-lib.mjs`(O_NOFOLLOW·0o600·1MB cap)를 쓰고, `appendClaimRecord`가 `detectRebind`로 다른 해시의 재청구를 `LEDGER_REBIND_REFUSED`로 거부한다. 트래커 라벨 기반 사후 dedup은 미구현이다. |
| change-scope fenced JSON self-attestation(팀 워크플로우 link STALE 게이트) | STALE 완료 차단(2026-09-14부터 `work-link.mjs planWorkLink`)은 개발자가 편집 가능한 `change-scope.md` fenced JSON을 되읽어 대조하므로, sourceDigest를 고쳐 쓰면 우회되고 파일을 지우면 대조가 불가하다. `checks[].baseline`도 같은 클래스다. | — (2026-08-24 예방 등록) | 대조 미수행은 `staleCheck: not-performed`로 표기하고 confirm 경로를 `stale-check-unavailable`로 막으며 통과는 `--accept-unverified-scope`뿐이다. 활성 change-scope 덮어쓰기는 `--replace-scope` 없이 차단한다. 같은 작업을 다시 집으면 첫 픽업의 `baseline`을 잇는다(되돌린 작업은 잇지 않는다) — 앵커 시점이 첫 픽업까지 넓어진다. 위조는 비협상 규칙과 PR 리뷰 몫이다. |
| 브랜치 레지스트리(마커·라벨) — 팀 워크플로우 §4-1 | 정본인 이슈 본문 마커 `branch=`는 self-declaration이라 rename 후 stale해도 못 잡는다. 라벨 `branch:*`는 50자 초과 생략·구세대 무라벨로 마커의 부분집합이고, `origin/<br>` 존재 판정은 마지막 fetch 시점 스냅샷이다. | — (2026-08-24 예방 등록) | `overview.discoverBranchRegistry`가 마커·라벨을 소스 태그와 함께 union 집계하고 불일치는 `mismatches`로 노출한다(test-ticket-overview). rename-stale 마커의 기계 검증은 미해결 TODO다. |
| 픽업 시 self-assign 소유권 판정(청구≠픽업) | `computeAssignmentPlan`은 호출 시점 스냅샷만 보는 순수 판정이고 `gh issue edit --add-assignee`는 compare-and-swap이 아니다. `work-pickup-run.mjs`의 재조회~배정 사이 미시 창에서 동시 경합이 다중 배정을 남길 수 있다. | — (2026-08-21 예방 등록) | 배정 직전 재조회·재판정으로 선점 시 양보하고, 배정 뒤 남도 보이면 자기 배정을 회수한다(`unassign`, 동시에 보면 둘 다 물러난다). 회수가 없거나 실패했거나 나 말고도 배정돼 있으면 `multi-assign-detected`로 멈추고 보드도 집을 수 없다고 보인다. 회수 실 GitHub 왕복은 NOT_RUN(메모리 대역). 원장-우선 가드가 1차, 트래커 배정이 2차다. |
| 티켓 본문 인젝션 정규식 스캔(B 픽업) | `scanUntrustedBody`·`scanUntrustedIssue`는 키워드+40자 윈도우 정규식 4종이라 동의어·줄바꿈 분산·유니코드 치환은 미탐하고 정상 스펙을 오탐할 수 있다. 코멘트 오탐률은 재지 않았다. 격리 펜스는 LLM에 대한 관례이지 파서 경계가 아니다. | — (2026-08-21 예방 등록) | `pickupWorkTicket`은 본문·제목 의심 시 change-scope를 발급하지 않고 터미널 bounce하며, 의심 코멘트는 빼고 표지를 남기고, 통과 본문은 내용보다 긴 펜스의 "지시 아님" 격리 발췌로 싣는다. 회귀는 `test-ticket-untrusted`·`test-work-pickup`이다. 의미 탐지 승격은 미해결 TODO다. |
| 타일 방향 게이트의 skip 예외 | `design-approval-contract.md` 0단계는 "레퍼런스·시안 기지정 또는 기존 디자인 시스템 승계"면 후보 타일을 건너뛰게 하는데, **그 판단이 정당한지 검사하는 validator가 없다**(2026-08-20 적대 리뷰 등록). 오케스트레이터가 매 라운드 자기선언으로 스킵할 수 있고, 의무인 decision-log 기재 여부조차 기계가 확인하지 않는다. 같은 파일의 기존 "사용자가 명시 skip하면 프리뷰 루프 생략"과 동일한 honor-system 클래스 | — | 게이트 신설 취지는 비용(방향 기각을 프리뷰에서 받으면 630k~681k vs 타일 132k)이지 안전 하한이 아니므로 스킵의 blast radius는 예산 한정. decision-log 기재 존재의 기계 검사는 미해결 TODO — 스킵 사례가 실제로 관측되면 승격 검토 |
| 델타 라운드의 앵커 드리프트 | 개정 라운드 델타가 DOM 앵커를 바꾸고 `traceability.json`을 갱신하지 않으면 못 잡는다. `design-preview-status-lib.mjs`는 traceability 내부 정합성만 보고 선언 앵커의 실제 프리뷰 존재는 대조하지 않으며, `--write-source-snapshot`은 입력 digest만 고정한다. | 2026-08-20 search-portal 수동 grep에서 선언 앵커 21종이 모두 존재해 드리프트는 미발생이었다(게이트가 아니라 사람 확인). | `design-preview-builder` 규칙 12가 앵커 변경 시 갱신 의무와 기계 미대조 사실을 명시한다. 앵커↔DOM 대조 validator 승격은 미해결 TODO다. |
| T1 receipt(t1-summary.json)의 트리 결속 | `validate-certified-evidence`는 receipt의 `status`만 보고 `declaredRevision`·`sourceFingerprint`를 커밋된 골든 트리·승격 커밋과 대조하지 않는다. 첫 승격 커밋의 receipt는 설계상 stale fingerprint를 담고, 커밋 바이트의 provenance도 self-report다. | 2026-08-23 hybrid 첫 승격: fingerprint 불일치 상태로 게이트가 green이었다. | 승격 SHA로 `hybrid-t1`을 재dispatch해 receipt를 교체하는 것을 runbook 의무로 두었고 hybrid는 B-7 재실행으로 교체했다. 대조는 사람이 수행·기록한 것이며 fingerprint·revision 대조 validator 승격은 미해결 TODO다. |
| eval receipt 커밋 규약 | web 카탈로그(`scenarios.json`) receipt는 실행 중 인라인 검증(run-eval-executor의 verifyResult — runDirectory 컨텍스트 필요)에 의존한다. **커밋된 receipt만으로 사후 독립 재검증하는 표준 명령이 없어**, 커밋 후의 web receipt 내용은 self-report다(ai 카탈로그는 `run-ai-evals.mjs --verify-result`로 JSON 단독 재검증 가능 — 비대칭) | — (규약 신설 시 리뷰 지적으로 예방 등록, 2026-08-23 — receipt 0건 상태) | receipt에 실행 메타·검증 로그 요약 포함 의무는 규약 몫(evals/README). web receipt 독립 재검증 CLI 승격은 미해결 TODO — 첫 web receipt 배치 전 구현이 이상적. maturity 라벨과 receipt의 기계 결속도 §"maturity의 eval-언급 검사" 행의 승격 조건으로 연동 |
| CI 워크플로 미러 동기화 검사 | `CI_MIRROR_DRIFT`(validate-workflows-and-evals)는 `.claude/ci/*.yml` 중 `.github/workflows/<name>` **활성 미러가 존재하는 파일만** 바이트 대조한다 — 미러가 0개면(또는 `.claude/ci`가 비면) 아무 것도 검사하지 않고 pass가 나는 vacuous 조건이 있다. 제안본 자체의 내용 품질은 워크플로 보안 검사(inspectWorkflowSecurity) 몫 | — (도입 시 리뷰 지적으로 예방 등록, 2026-08-23) | 활성화 절차(ci-activation-runbook)가 미러를 배치하는 순간부터 드리프트가 기계 강제된다. pass 메시지가 검사한 미러 개수를 노출하므로(`N activated … mirror(s)`) 0개 상태가 침묵 통과로 보이지 않는다 |
| source/deployed 판별자(`detectSourceRepository`) | `.claude/deployment.json` 존재 또는 settings 2종 byte 동일이라는 위조·실수 가능한 마커가 source-only 검사 5개(I1 anti-tamper인 contract-hygiene·certified 포함)를 일괄 게이트한다. source repo에서 마커가 성립하면 5개가 침묵 속에 꺼진 채 CI가 green이 되며 카운터 불변식이 없다. | — (2026-08-25 적대 리뷰 예방 등록) | 미해결 TODO 3종: 모순 조합 fail-closed(settings 동일 && `deployment.json` 부재, deployed && `golden/`·`docs/protected-core.md` 실존), `deployment.json`의 supportLevel 스냅샷 대조, agent reachability 면제를 운영자-전용 에이전트 목록으로 한정. |
| A/B outcome 자기보고 3분류(`analyze-ab`) | `outcome`은 완결성 게이트의 판정 결과라(`execution-budget-contract.md`) **OFF 암은 계약상 outcome을 생략한다** — 주 엔드포인트가 telemetry 경로로는 ON 암에서만 측정되는 **차등 측정**이다. 미상 제외는 MAR 가정이며, crash한 스폰일수록 미기록 개연성이 높으면(MNAR) 미완율을 **과소평가**한다(게이트에 유리한 방향). `UNKNOWN_RATIO_INVALID=0.2`는 **미교정 임의 임계**이고 검사가 run 단위라 임계 이하 미상이 여러 run에 누적돼도 `interpretable: true`로 남는다 | 실측(2026-08-26): OFF run 스폰 3개 전부 `outcome: undefined` — 초판 분석기는 이를 완주로 세어 "OFF 미완율 0%"가 나올 뻔했다(fail-open) | 미상 3분류·분자분모 제외·`DATA_INVALID` 표시로 1차 차단(회귀 반증 테스트 고정). **미해결 TODO**: 실해소는 executor.log·산출물 실존에서 **암 독립 outcome 도출**(grader와 같은 관용구). 그 전까지 telemetry `outcome`을 주 엔드포인트로 쓰는 Stage 1은 착수 금지 |
| A/B 암 준수 검증(`checkArmCompliance`) | executor.log 문자열 매칭이라 게이트를 다른 경로로 호출하면 놓친다 — 오염 탐지 하한이지 준수의 증명이 아니다. 2026-08-26 적대 리뷰로 **구조적 오탐**도 발견: 로그 첫 줄 `# command:`가 프롬프트 전문을 에코하는데 OFF 지시가 3게이트 이름을 명시해, 헤더를 세면 **모든 준수 OFF run이 자동 폐기 판정**을 받았다 | 실측(2026-08-26): aborted ON run 로그 헤더에서 프롬프트 에코 확인(스폰 0 run이라 본문 출처가 아님) | 헤더 블록 제외 + 이름 언급이 아닌 **실호출 형태**(`node <경로><gate>.mjs`)만 집계로 수정. 현실적 OFF 로그 fixture로 회귀 고정(3종) |
| 에이전트 model/effort 프런트매터 계층 | 모델 티어와 effort 배정은 실측 실패 데이터가 아니라 포지셔닝 판단이다(실측 지지는 runaway 1건). `validate-harness`가 값(별칭·모델 ID, low~max)과 에이전트의 effort 명시를 검사하지만, 값이 런타임에 실제로 적용되는지는 관측하지 않는다 — 모델이 지원하지 않는 effort는 Claude Code가 조용히 낮춘다. | 2026-08-26 38종을 fable로 올렸다가 되돌려, 배정이 판단·비용 인센티브에 흔들림을 보였다. effort를 비운 에이전트는 세션 노력 수준(max)을 물려받고 있었다. | 배정 변경은 적대 리뷰 필수이고 JUDGMENT에 근거 서열을 적는다. 에이전트 effort의 실적용 관측은 미해결 TODO다. 진입 스킬(`wh`·`team-flow`)의 `effort: high`는 상한이 아니라 지정이다 — 세션이 낮으면 올리고 높으면 낮춘다(평가에서 오케스트레이터 사고량 변화로 적용을 관측했다). haiku 에이전트 산출 결함은 이 행에 추가 등록한다. |
| 프로필 감지의 요구 패키지 근거(declared ∪ workspace) | 워크스페이스 루트 선언을 요구 패키지 충족 근거로 인정한다. **멤버십 glob을 확인하지 않는다** — `pnpm-workspace.yaml`의 `packages:` 패턴에 해당 프로젝트가 실제로 속하는지 검사 없이 상위 manifest를 채택한다. `.git` 보유 디렉터리에서 탐색을 멈추지만 그 안에서는 최대 6단계를 오른다 | 실측(2026-08-26): 모노레포 패키지가 `vite`를 루트에 선언해 감지 실패 → workspace 근거로 해소. **lockfile 근거는 적대 리뷰에서 기각** — 설치 증거를 채택 증거로 오귀속해 webpack+vitest 앱이 react-vite-spa로 조용히 오탐된다(AMBIGUOUS는 2개+ 매칭에서만 발화). 기각된 설계는 음성 테스트로 고정 | evidence 문자열의 근거 출처 표기(`package:vite@workspace`)가 사후 진단 채널. forbidden도 declared ∪ workspace로 대칭 유지(호이스트 우회 차단). **미해결 TODO**: 워크스페이스 멤버십 glob 확인 |
| solution-design 자기보고 표면 | 기계 블록은 확정(`spec.mjs` 파싱)·개발 인계(`design-decisions`)·결정 반영(`checkDecisionsApplied`)이 읽고, `measured` 주장은 `validate-spec-conformance`가 실측과 대조한다. 여전히 자기신고인 것: `inferred`·`proposed` 티어의 근거, §2 경계(구현 절차를 이 파일로 옮겨도 미탐), 산문 본문 | — (2026-08-26 예방 등록 — 소비자가 생겨 2026-09-25 현행화) | 해소 후보: `source-artifact-ingestor`의 `02_design` 광역 소유권 carve-out(스팩 입력 덮어쓰기 벡터), 추론 근거의 인용 대조 |
| spec-lock (Stage 1 — 스팩 확정 아티팩트) | 수용 기준·결속 검사는 ID 문자열과 선언의 존재까지만 보므로 주석 인용으로 통과하고, 묻지 않고 `assumed`로 적는 경로는 자기보고다. `spec-ledger.jsonl`을 스팩과 함께 지우면 탐지되지 않고, `moduleBoundaries` 미선언이면 경계 검사는 미수행, 결속 이전 매니페스트는 `UNBOUND` 경고뿐이다. 스팩이 `computeSourceFingerprint` 범위 안이라 확정 자체가 receipt를 stale로 만든다. digest 입력이 고정 경로라 샤드 입력의 변형은 stale로 잡히지 않는다(fail-open 방향). `constitution.substrate`의 `measured`는 실존 대조 없이 하네스 기본값을 이기고, `.claude/substrate-defaults.json`은 `environment-scaffolder`·`validate-toolchain`·`ts-conventions`와 값을 공유하지 않는 사본이라 드리프트해도 기계가 침묵한다. 배포 상태가 `private:true`/공개 이진법이라 사내 registry 배포라는 제3 상태를 구분하지 못한다. `constitution.conventions`는 경로 포인터라 내용이 `sourceDigest` 밖이고, 필드 부재(미조사)는 자기신고라 conformance가 `CONVENTIONS_UNDECLARED`로 알릴 뿐 막지 않는다. | 2026-08-30 track: 스폰 계획이 스팩보다 76분 앞서 잠겨 경로가 어긋난 채 구현에 도달했다. | `validate-spec-conformance`·`validateReleaseGate`가 layerMap 실존·staleness·`SPEC_TAMPERED`·`SPEC_DELETED`·`INVALID_SPEC`을, `resume-manifest`가 `PLAN_LOCK_SPEC_STALE`·`SPEC_GONE`을 fail-closed로 강제한다. 결정 준수는 `code-reviewer`가 `spec.json`과 대조한다(기계 재현성 없음). runtime 형태 검사 3종·`ingestion.validate` 조건 축·어댑터 receipt 메타 대체는 미해결이다. |
| 배선 회귀 커버리지(2026-08-30 신설) | `validate-wiring-coverage.mjs`의 배선 판정은 파일 단위 근접성 프록시라 한 테스트가 A를 spawn하고 B를 import만 해도 둘 다 배선됨으로 센다(과대 방향). 위험도가 아니라 존재만 세므로 쓰기·아웃바운드가 있는 미배선(`spec.mjs`·`ticket/cli.mjs`)도 같은 무게다. | 같은 클래스 결함이 executionBinding·소유권 훅·claim-scope에서 세 번 부딪혀서야 발견됐다(2026-08-30). | `main()` 가드의 프로세스 테스트 부재와 문서 지시 명령의 bash 정책 미등록을 세어 CI에서 ratchet(증가만 실패)으로 본다. 미등록 3개(`init-workspace`·`run-golden-profile`·`ticket/cli`)는 인자 계약 설계가 선행돼야 해 문서와 정책이 어긋난 채 남는다. |
| 단계 인계 판정(2026-08-30 신설) | `validate-handoff-readiness.mjs`는 선언의 존재만 보고 `dependsOn`·`paths`가 실제와 같은지는 대조하지 않는다. 산문 순서·`…에 의존` 간선 탐지와 경계 귀속(rationale의 FEAT 표기)은 한국어 근접성 정규식 프록시이며, 선언이 하나라도 있으면 산문 순서 검사를 건너뛴다. 호출은 산문 계약이고 `--to design`은 차단이 아니라 보여주기라 분모 품질을 재지 않는 경로가 남는다. | 2026-08-30 개발 중 병렬 순서가 산문 한 줄에만 있어 11건이 착수 가능으로 보였다(실제 4건). | `--to development`가 `HOLES`면 Phase 2→3 승인을 진행하지 않고, Phase 1→2 호출은 `approval-checkpoints.md` 실행 문장과 배선 회귀로 고정했다. 판정에 원문을 싣고 병렬성 지표(간선·최장 사슬·병목)는 실패가 아닌 측정으로 낸다. 차단 승격은 실사용 통과율 관측 뒤에 정한다. |
| 사내 registry 프로젝트가 품질 러너를 못 돈다(2026-09-21 등록) | 브로커와 품질 러너가 둘 다 프로젝트·상위 디렉터리의 `.npmrc`·pnpm hook을 발견하면 exit 2 한다(`run-quality-gates.mjs`·`run-package-operation.mjs`) — 공개 registry 전제다. `audit`도 `registry.npmjs.org`로 고정돼 사설 패키지가 있으면 릴리스 필수 영수증이 성립하지 않는다. 즉 사내 registry를 쓰는 브라운필드 프로젝트는 도입 첫날 막힌다. | 실측(2026-09-21): 실제 사내 SPA에서 `.npmrc`·`pnpm.overrides`·pnpm 핀 불일치 셋이 동시에 걸렸다. 그중 핀 불일치는 이번에 해소했다. | `dependencyBinding`(lockfile↔node_modules 해시 대조)은 설치 주체와 무관하므로 설치 자체는 팀이 해도 증거가 선다. 남은 것은 러너의 `.npmrc` 차단과 공개 registry 고정 audit이며, 완화하려면 “사설 registry를 쓰는 프로젝트의 공급망 증거를 무엇으로 대체하는가”를 먼저 정해야 한다 — 그 답 없이 푸는 것은 I2 위반이다. |
| 호스트 경계 계약의 자기신고 채널(2026-09-21 신설) | `webview-host-contract.md`의 판정이 세 곳에서 자기신고다 — ① 적용 여부를 에이전트가 코드 신호로 스스로 정하므로 기계가 대상 여부를 세지 않는다(모드 플래그를 쓰면 선행 로드 예산 초과라 의식적으로 뺐다) ② §3 위임 경로의 ｢호스트가 검증한다｣ 근거는 결박 형식을 요구할 뿐 그 근거의 참을 기계가 대조하지 않는다 ③ §7 호스트 프로파일의 값·출처·버전 결박을 읽는 validator가 0이다. 그리고 **그린필드에는 트리거가 없다** — 코드 신호가 없고 스팩·프로필에 호스트 내장 필드가 없어 새 프로젝트에서는 이 계약이 서지 않는다. | 2026-09-21 계약 신설 시 예방 등록(실측 우회 없음). 근거는 실제 웹뷰 SPA 2개의 표면 대조이며 적용 결과는 미실측(명명 수준). | 안전 하한(§3 allowlist·폴백)은 `code-reviewer` 6의 FAIL 확장이라 자기신고가 아니다. `code-reviewer` 20·`browser-verifier`·`security-reviewer`가 조건부로 읽어 소비자 0은 아니다. **해소 조건**: 그린필드 트리거 필드 신설과 §7 프로파일의 기계 판독, 그리고 이 계약을 적용한 변경 1건의 실측. |
| 골든 hybrid 가드가 §7 순서 규칙과 어긋난 채 certified(2026-09-20 신설) | `golden/vite-serverless-hybrid/api/_lib/guard.ts`는 인증을 한도보다 먼저 보고 버킷 key를 클라이언트 `x-forwarded-for`에서 뽑는다 — §7이 요구하는 진입/주체 두 한도가 아니다. `api.guards` receipt는 5종 **존재**와 405만 보고 순서를 보지 않으며, `validate-certified-evidence`는 `t1-summary.status`만 읽고 `LOCK_INPUTS`는 `_workspace` 문서뿐이라 `api/` 드리프트를 어떤 기계 게이트도 잡지 않는다. 골든의 `qa-security.md`는 PASS이나 같은 트리를 지금 `security-reviewer`로 돌리면 HIGH가 난다. | 2026-09-20 §7 순서 규칙 신설 시 예방 등록(실측 우회 없음). | 순서의 정본은 SKILL.md §7의 코드 스니펫이며 골든이 아님을 SKILL(repo-only)과 이 행에 명시한다. 골든을 지금 고치지 않는 이유는 receipt↔트리 일치 유지다(T1 러너 미프로비저닝 — §4 114행의 runbook 의무). **해소 조건**: T1 러너 프로비저닝 뒤 `guard.ts`·`tests/api.unit.test.ts` 교정 + 재dispatch, 그리고 `api.guards`에 순서 회귀 추가. |
| host 실행 승인의 반복(2026-08-30 해소) | 승인은 프로젝트당 한 번 `host-execution-grant.json`(프로젝트 경로·호스트·승인 당시 `package.json`의 `scripts` digest 결박)에 남고 이후 게이트는 묻지 않는다. 프로젝트에 쓰기 권한이 있는 무엇이든 승인 파일을 위조할 수 있다(로컬 신뢰 모델의 명시적 인수 — 승인 경로는 무소유라 소유권 훅이 서브에이전트 쓰기를 막지만 훅 밖의 쓰기는 통제 밖이다). 결박 대상은 **실행될 명령**이지 그 명령이 실행하는 코드가 아니다 — script가 부르는 설정·소스가 바뀌는 것은 승인을 무효화하지 않는다(그것이 게이트가 실행하려는 대상이다). | — | 첫 승인은 여전히 묻고, 다른 머신·프로젝트로 복사하거나 승인 뒤 script 집합이 바뀌면 무효이며, 깨진 승인과 명령 집합을 담지 않은 이전 판 레코드는 부재가 아니라 거부로 읽는다. 되돌리려면 파일을 지운다. `scripts`를 소유하는 에이전트는 `environment-scaffolder` 하나뿐이다(반증 등록부 2건). |
| 하네스 제어 마커가 자기 판정의 입력이던 것(2026-08-30 실측·해소) | `unitContentHash`와 `spec.mjs` `digestInputs`는 `unit`·`refs` 두 마커 리터럴만 내용에서 제외한다 — 새 마커를 도입하고 제외 목록에 넣지 않으면 같은 자기 무효화가 재발한다(마커 등록부가 단일 소스가 아니다). 스팩 다이제스트는 utf8 문자열로 읽어 바이트 동일성 가정이 깨졌다(텍스트 산출물만이라 실질 영향 없음). | 마커 선언 커밋 하나로 track 11건이 `sync-required`가 되어 픽업 불가였다(수정 전). | 마커는 스케줄링 메타데이터라 내용에서 제외하고, 제목·TC·산문 변경은 여전히 stale을 낸다(양방향 회귀). 계획이 바뀌면 제자리 갱신이 아니라 `superseded` + 새 WORK로 보낸다. 마커 제외 목록의 단일 소스화는 미해결이다. |
| 개발 착수 관문(Gate 0, 2026-08-30 신설) | 관문은 검사 목록이지 완결성 증명이 아니다 — 목록 밖 원인으로는 개발 중 여전히 막힌다. 소유권 예행은 레이어당 대표 경로 하나만 찔러 레이어 안 경로별 판정을 못 잡는다. `checkDecisionsApplied`는 `choice`에서 패키지명 모양 토큰을 뽑는 프록시라 서술형 choice는 미검사, 패키지명 같은 서술은 오탐이다. | 2026-08-30 개발 중 블록 8건이 전부 착수 전에 알 수 있던 원인이었다. | 개발 단계 질문 없이 도출하거나 착수 전에 `BLOCKED`로 내며, 착수 전에 알 수 있던 원인만 항목으로 추가한다. 브랜치 낙후는 미항목이고 모노레포 app-root 예행은 미실증이다. Gate A·L이 `project-profile.json`을 다시 써 생기는 stale은 계약 의무일 뿐 기계 미강제다. |
| 서브에이전트 Bash 경로 탈출(2026-08-26 발견·수정) | `assertInsideProject`는 경로처럼 보이는 인자만 검사한다 — 러너가 비-경로 인자로 위험한 동작을 하면 못 잡는다. 이 정책은 서브에이전트에만 걸리고 main session Bash는 통제 밖이다(2026-07-27 승인). | 수정 전 `pnpm --dir /etc run lint`·`--reporter ../../../etc/passwd`가 통과했다. | 절대 경로·상위 탈출·홈 상대 경로를 막고 정당 명령 7종 통과를 함께 고정했다(회귀 5종, 반증 등록부 7건째). 비-경로 인자의 의미 검사는 미해결이다. |
| 서명 provenance — 설계는 SLSA L2, 실증 0(2026-08-26 조사) | `verifyQualityAttestation`은 `isolated-ci` 밖 receipt와 보호 컨텍스트 6종 부재를 거부해 fail-closed다. 그러나 `.github/workflows`가 보호 컨텍스트를 주입하지 않고 `.claude/quality-attesters.json`이 없어 서명 경로는 fixture로만 검증됐다. 격리가 `argv-only` 수준이라 SLSA L3 실행 격리는 미해결이다. | — | 로컬에서 서명 증명을 만들 수 없는 것은 설계다(로컬 서명 증거 위조 금지). 무장은 CI 작업(보호 컨텍스트 주입·attester 등록)이며 처방은 실사용 서명 증명 1호다. |
| layerMap 파일 항목(2026-08-26 발견·수정) | `normalizeLayerPath`는 파일·디렉토리를 마지막 세그먼트의 점 유무로 가른다 — `src/v1.2` 같은 디렉토리는 파일로, `Makefile` 같은 확장자 없는 파일은 디렉토리로 오인된다. | 수정 전 실사용 2호 layerMap 17개 중 파일 6개가 무소유였다. | 파일은 완전 일치(`$` 앵커), 디렉토리는 접두 일치로 판정하고 두 오판 방향을 회귀로 고정했다(반증 등록부 6건째). |
| lockfile 출처 판정 오탐(2026-08-27) | `lockfile-source-lib.mjs`는 pnpm-lock v9의 산문 필드 `deprecated` 값만 들여쓰기 규칙으로 제외한다 — 새 산문 필드는 시끄러운 오탐, 새 출처 필드는 검사된다(fail-closed). anchor/alias 거부(`rejectAmbiguousYaml`)는 호출자가 원문에 선행해야 한다. `patch:` 프로토콜은 여전히 차단이다. | 수정 전 `deprecated:` 안내 URL 오인으로 eslint 쓰는 모든 프로젝트가 install 불가였다. | `link:`는 importer 기준 대상이 루트 안인가로 판정하고 문맥을 모르면 루트 기준으로 본다. 회귀 11종·반증 13·14호, 실제 lockfile 4종 통과·탈출 시도 5종 차단. `patch:` 허용 여부는 별도 판단이다. |
| 프로젝트 밖 읽기 + ingestion 감지 게이트(2026-08-27) | `.claude/workspace-roots.json`은 수기 선언이라 선언하지 않은 형제 repo는 `DENY_PATH_OUTSIDE`로 막힌다. ingestion QA는 감지가 아니라 선언에 걸려, 실제로 수집하면서 선언하지 않으면 `ingestion.validate`가 돌지 않는다(I2 약화 방향, 명시). | 수정 전 배포 알림 `fetch(url,{method:'POST'})` 하나로 프로필 식별이 죽었다. | 연 루트도 읽기 전용이며 비밀(`.env`·`.git/config`·키)·심볼릭 링크 탈출·홈/`/`/조상 차단은 유지된다. 미선언 감지 사실은 프로필 `ingestion` 보고(evidence 포함)에 남고 intake에서 되묻는다(회귀 5종, 반증 12호). |
| 플러그인 문서 루트 읽기 + 계약 사본(`_workspace/.contracts/`) | 문서 루트(skills·agents·adapters·schemas)는 매니페스트 이름(web-harness)만으로 열린다. 사본은 세션 시작·`init-workspace` 때만 맞추므로 세션 중 업그레이드는 다음 시작까지 낡는다. 사본의 `.mjs` 3개는 소비자 `eslint .`에 잡힌다(골든 실측 오류 0, 더 엄격한 설정은 걸릴 수 있다). 교체는 rename 두 번이라 그 사이 짧게 사본이 없다. 프로젝트당 ~1.7MB. | 배포본에서 Bash 없는 에이전트 25개가 `web-harness-read` 참조를 못 따랐고, 메인 스레드도 스킬의 계약을 `DENY_PATH_OUTSIDE`로 못 읽었다. | 스크립트 폴더는 닫혀 있고 비밀·링크 탈출 검사는 연 루트 안에서도 그대로다. 사본은 payload·사본 양쪽 digest로 대조해 손으로 고친 계약을 되돌리고, 소스 지문에서 빠진다(receipt 무효화 없음). 참조한 문서가 payload에 없으면 빌드가 실패한다(회귀 8종, 반증 4호). |
| 카탈로그 키 엄격성 누락(2026-08-27 발견·수정) | `ENTRY_KEYS`는 수기 목록이다 — 새 필드를 추가하고 목록에 넣지 않으면 그 필드가 「알 수 없는 키」로 FAIL한다(fail-closed라 조용하지 않다). | 수정 전 `gatesRelease`·`needsArtifact`·`evidenceName` 오타가 기본값으로 퇴화해 조용히 통과했다. | `shape-checks.json` 미지 키 차단을 복원했다(오타 6종 주입 6/6 FAIL, 회귀 2종, 반증 등록부 11호). |
| Phase 본문 시점 로드 강등(2026-08-27) | 표면 검사(`validators/orchestration-surface.mjs`의 SKILL.md + `references/phase-*.md` glob)는 「표면에 이 문장이 있다」만 보고 오케스트레이터가 읽는지는 보지 않는다. 시점 로드 계약은 모델이 실제로 읽어야 발화한다. 절감은 고정 진입 비용이지 전체 실행 컨텍스트가 아니다. | — | 기계 게이트(receipt·release gate·훅)는 이 층에 의존하지 않아 하한은 유지된다. Phase 파일 3건은 축자 이동이라 contract-hygiene `references` allowlist에 grandfather했다(반증 등록부 10호, 회귀 2종). |
| AI 표면 전면 제거(2026-08-27) | data-access 리포트 요구는 `migrations/` 존재(루트 + 1단계 하위, `release-report-policy.mjs`)를 서버 소유 데이터의 프록시로 쓴다. migration 없이 서버 인가만 있는 형태(BaaS·외부 DB)와 2단계 이상(`apps/api/db/migrations/`)은 못 잡는다. | 최상위만 보던 첫 구현이 `client/migrations/` 관습 프로젝트를 조용히 누락했다(같은 날 수정). | 1단계 하위까지 확장하고 회귀 4건·반증 등록부 9호로 고정했다. 깊은 경로와 무migration 서버 인가는 미해결이다. |
| 반증 게이트(2026-08-26 신설) | `falsification-registry.json`은 수기다 — 새 게이트를 등록하지 않으면 반증되지 않으며 등록 강제는 미해결이다. 정본 digest는 `git ls-files -co --exclude-standard` 집합만 보므로 짝 테스트가 ignored 경로(`_workspace/`·`workspace/`·`dist/`)에 쓰면 못 잡는다. | 검증 호출을 지워도 CI가 exit 0이었다(테스트가 배선 지점을 지나가지 않음, 2026-08-26). | 최소 변형과 반드시 실패할 테스트를 짝지어 통과 시 `NOT_FALSIFIED`, 변형 지점 부재·중복은 `STALE`, 무변형 기준 실행이 빨가면 `NOT_MEASURED`로 낸다. 변형은 `os.tmpdir` 사본에서만 하고 깊이 2 이상 거부·SIGKILL 타임아웃으로 재귀를 막는다. CI 편입, 자기 회귀 4종. |
| 스팩 유래 소유권(layerMap) | 평면 `layerMap`은 carve-out을 표현하지 못해 폴백은 여전히 `AGENT_OWNERSHIP` 등록부다(`feature-mutation-builder`의 `live-mode` 제외 등) — 「FSD 기본값 제거」는 미달성이다. `layerMap`이 덮지 않는 소스 디렉토리는 아무도 못 쓰는데 판정이 FAIL이 아니라 note다. | 브라운필드 패키지 레이어 10개 중 5개가 소유자 없음으로 훅에 막혔다(2026-08-26). | 겹치는 레이어는 스팩을 신뢰하지 않고, 역할 매핑·레이어 미선언·스팩 부재는 등록부 폴백이다(fail-closed, 반증 고정). 미해결 TODO: carve-out 표현, 커버리지 공백 판정 강화. |
| 에이전트 reachability가 단방향이었다(2026-08-27 발견·수정) | `validators/agent-reachability.mjs`는 역할 어휘를 실존 에이전트 이름의 마지막 하이픈 세그먼트에서 파생해 어휘 수축을 막지 못한다 — 접미사를 유일하게 가진 에이전트를 지우면 그 죽은 지시가 침묵 통과한다. 서술과 지시를 백틱 관례로 가르므로 백틱 없는 진짜 스폰 지시는 못 잡는다(fail-open). | `visual-developer` 죽은 스폰 지시가 6곳에 남은 채 harness-ci가 8커밋 동안 green이었다. | 역방향 대조를 신설하고 회귀 11종·반증 등록부 15호로 고정했다(서술에 백틱은 loud FAIL). 미해결 TODO: 삭제 커밋에서 삭제 대상 이름의 잔존 참조 대조. |
| 메인 가드(`import.meta.url` 대조) | argv[1](호출자가 준 경로)과 import.meta.url(실경로)의 문자열 대조라, 경로에 링크가 끼면 거짓이 되어 아무것도 하지 않고 exit 0·출력 0으로 끝난다 — 호출자는 통과로 읽는다. 옛 `file://${process.argv[1]}` 관용구는 Windows에서도 같은 방식으로 빠진다. 관용구를 막는 기계 검사가 없다(`validate-environment-closure.mjs`에 옛 관용구가 남아 있다). | 2026-09-25 배포본 평가: /var/folders 링크 아래에서 디스패처가 논리 경로를 넘겨 plan-reviewer가 인계 검사의 빈 출력을 받았다. | 진입점에서 닫았다 — bin 3종 `pwd -P`, 세션 모드 훅 실경로 대조(회귀·반증 seed). 잔여: 링크가 낀 절대 경로를 `node`에 직접 넘기면 배포 진입점 22개(관용구 3종)에서 여전히 조용히 빠진다 — 문서의 호출은 디스패처와 상대 경로뿐이다. 해소 후보: 공유 isMainModule(양쪽 실경로) + 관용구 검사. |
| 스캐폴드 템플릿 pin이 게이트 밖(2026-08-27 발견·수정) | `validate-dependency-pins.mjs`는 생성된 프로젝트의 `package.json`만 검사하고 pin 원본인 `project-init/assets/templates.md` 코드펜스는 어떤 게이트도 읽지 않는다 — 죽은 pin은 신규 프로젝트 install에서야 드러난다. | `typescript@6.0.0`·`@tanstack/react-query@5.90.0`(부재)이 템플릿에 박혀 있었다. | golden hybrid(T1 receipt)와 같은 `typescript@6.0.2` + `typescript-eslint@8.65.0`으로 맞췄다. `@tanstack/react-query@5.102.7`은 등록소 조회 주장뿐이며 정본인 신규 프로젝트 install receipt는 없다. 미해결 TODO: 템플릿 pin을 `validate-dependency-pins`가 직접 읽는 배선. |
| 통합이 남긴 테스트 경로 무소유(2026-08-27 발견 → 2026-08-28 해소) | `resolveDeveloperOwnership`은 layerMap ∪ 스팩 `testLayers`를 소유하고, e2e 요구는 형태 카탈로그 `userInterface`에서 도출한다(미등록 형태는 `E2E_TEST_LAYER_UNDECIDED`). 선언된 e2e 경로가 실제 e2e 러너 testDir와 일치하는지는 미대조라, 스펙 파일이 있되 실행되지 않는 green이 가능하다. | 수정 전 파이프라인이 e2e 증거를 요구하면서 `e2e/` 작성을 구조적으로 막는 교착이었다. | 반증 3호·19호와 `TEST_LAYER_UNKNOWN_KEY` loud reject로 고정했다. 후속: e2e receipt가 선언 경로 아래 스펙 1개 이상을 실행했음을 대조한다. |
| 스팩 공유 판정이 `.gitignore` **텍스트** 프록시다(2026-08-28 신설) | `checkSpecShared`는 `.gitignore` 패턴만 읽고 git 추적 여부를 대조하지 않는다. 와일드카드 패턴(`_work*`)의 리터럴 비교, `.git/info/exclude`·전역 `core.excludesFile` 미독, 「무시되지 않는데 커밋 안 함」이 전부 fail-open 미탐이다. | 초기 구현이 `_workspace` + `!_workspace/03_dev/spec.json`을 「공유됨」으로 통과시켰다(회귀가 오판을 고정). | 조상 디렉토리 제외를 결정적으로 판정해 실제 git과 7/7 일치를 확인했고 반증 20호 `spec-shared-not-gitignored`로 고정했다. 실해소는 `git check-ignore`/`ls-files` 실행이나 순수 파일 판정 유지를 의식적으로 택했다. |
| `fix` 레인 자기검사 | 5항목 체크리스트는 **산문 self-attestation**이다 — 이를 파싱·강제하는 스크립트가 없다. ①~④는 "새" 신설만 보므로 등록 당시 리뷰가 짚은 구멍(기존 endpoint 응답 스키마 변경·기존 화면에 상태 추가·인증 가드 완화)이 미탐이었고, ⑤(기존 공개 계약 변경)를 추가해 어휘로는 덮었으나 **판정 주체는 여전히 에이전트 자신**이다. `minimal-change-contract.md`가 "root-cause상 필수면 wire schema 변경 허용"이라 fix 레인 안에서 승인 없이 공개 계약이 바뀔 여지가 남는다 | 적대 리뷰(2026-08-31) 등록 계기: 초안이 ①~④뿐이라 "새 것만 없으면 통과"였다 | ⑤ 추가로 어휘는 덮었다. **기계 강제는 미해결 TODO** — `PUBLIC_CONTRACTS_TO_PRESERVE` 대비 diff 검사(변경 파일에서 route·schema·persisted state 변화 탐지)로 승격 검토. 그 전까지 이 검사는 "승인 없는 경로의 자기 신고"이며 탐지망은 라운드 종료 게이트(승격 QA)와 리뷰다 |
| 디자인 델타 감지 대조 목록 | `DOCS_TO_UPDATE: none (대조: ...)`의 대조 목록은 **소비자가 없는 self-attestation**이다(전수 grep: 파싱 스크립트 0). 목록을 그냥 써넣으면 통과하므로 게으른 감지를 **막지 못한다** — 반증 가능한 기록을 남길 뿐이다 | 적대 리뷰(2026-08-31) 등록 계기: 초안이 "게으른 감지를 막는다"고 주장했으나 강제 층이 없었다 | 기록 의무만으로도 사후 반증은 가능해진다(무엇을 봤다고 했는지 남으므로). 기계 강제는 미해결 TODO — `02_design` 파일 목록과 대조 목록의 집합 비교로 승격 검토. change brief 스키마의 정본은 `minimal-change-contract.md`이며 형식 변경은 그 파일에서 한다 |
| 역방향 인테이크(`ticket/intake.mjs`, 2026-09-09 신설) | 사람이 쓴 기획 티켓을 격리 스냅샷 + 인벤토리 행으로 받는 입구이며 요구사항·분류를 뽑지 않는다(`bind`·`adopt`는 2026-09-14 제거). 인젝션은 막지 않고 펜스로 격리하며 스캐너는 정규식 프록시다. 중복 판정은 제목+본문 해시 앞 12자라 인테이크 뒤 코멘트 변경은 행이 알리지 않고, 링크된 티켓 내용·Jira ADF 비텍스트 노드는 싣지 않는다. | 실 Jira에서 `resolveIssue`가 `components`를 조회하지 않아 `PLAN` 티켓이 `미분류`로 나왔다(주입 stub 회귀가 못 잡음). | 반증 `intake-quarantines-ticket-body`·`intake-inventory-dedupes-by-digest`·`intake-refuses-dev-ticket`·`jira-resolve-issue-fetches-axis-fields`로 고정했다. 분류 어휘는 팀 설정(`componentAxis`)이고 매핑 밖은 추측하지 않는다. `ticket/cli.mjs` 정책은 플래그 이름까지만 보며 게이트 탈출 플래그는 열지 않는다. |
| 변이 표본(`mutation-sample-lib`, 2026-09-09 신설) | 표본이지 전수 점수가 아니며 equivalent mutant 때문에 오탐 0이 될 수 없다. `src` 밖은 후보 0으로 침묵하고, 주석·문자열 속 연산자는 보장된 생존자(하향), 테스트 명령에 `tsc` 등이 섞이면 거짓 kill(상향)이 된다. 사용자 프로젝트를 제자리에서 변이시켜 `SIGKILL`·강제 종료 시 변이가 남는다(`git status`로 확인 — 복원 실패는 `report-test-qa`가 BLOCKED로 싣는다). | 기준 실행이 없어 원래 빨간 스위트에서 점수가 항상 100%였다(2026-09-09 적대 리뷰, 수정). | 보고이며 차단이 아니다(`qa-test.md`). 무변이 기준 실행 실패 시 `NOT_MEASURED`, 첫 출현만 변이하며 반증 `mutation-sample-survivor-classification`·`mutation-sample-restores-source`·`mutation-sample-baseline-run`으로 고정했다. 임계값은 더 많은 프로젝트를 잰 뒤 정한다. |
| 티켓 되돌림의 되돌아가는 길(`readiness.mjs`, 2026-09-09 신설) | 발행 본문 체크리스트와 `content-incomplete` 차단은 2026-09-14 제거됐고 되돌림 코멘트(`bounceComment`)만 WORK 픽업 사유로 남았다. 계획 WORK의 되돌림 코멘트는 멱등이 아니라 재시도마다 쌓인다 — 코멘트에는 사람이 읽는 글만 남기고(기계 마커는 Jira에서 글자로 보인다) 사람 티켓의 요청 코멘트만 개발자 로컬 지문으로 중복을 가린다. 알림은 known 사유에만 닿고 목록 밖 되돌림은 조용하다. | `--dry-run` 미리보기가 영구 코멘트를 남기고 Jira Cloud v3 평문 코멘트가 400이던 것을 적대 리뷰가 잡았다(수정). | Jira 코멘트 형식은 API 버전으로 가른다(반증 `jira-comment-format-by-version`). 위키 서식(v2)에서는 인라인 코드를 `{{…}}`로, 대괄호·중괄호를 이스케이프한다 — 하네스 코멘트에 의도한 위키 서식은 없다(본문은 따로 다룬다). 코멘트 dedupe와 목록 밖 사유 알림은 미해결이다. |
| 디자인 근거 귀속(`design-binding`, 2026-09-03 신설) | `validate-handoff-readiness`의 `design-binding`은 미결(`unbound`·`pending`)과 기록 누락 화면을 잡지만 귀속의 옳음·`declaredBy: user` 자기신고·근거 몰아넣기·전면 `derive`·조건 어휘/receipt 미결속은 못 잡는다. 디자인 `generated`·`absent` 경로의 청구는 문서 문장 존재까지, 인수는 `ack:<ID>`의 결정 로그 표제 실재까지만 대조한다(토큰-only 칸·ID만 맞는 라벨 통과). 조건 축 식별은 이름 부분일치 프록시이고 상태 행 통째 삭제는 못 잡는다. | 적대 fixture에서 조건 열 3→1개 삭제가 READY로 통과했다(2026-09-08, 보고로 대응). | 분모(`축:값` 헤더·빈 칸=미결)를 먼저 세우고 반증 `design-binding-condition-coverage`·`plan-conditions-blank-cells`·`common-condition-axes-reported` 등으로 고정했다. 워크스페이스에서 SKIPPED 5/5라 오탐 0은 vacuous다. 미해결: `derive` 행의 Phase 2 승인 안건 배선, 분모 래칫. |
| 골든 스팩 드리프트 게이트 | `validateGoldenSpecDrift`는 `digestInputs`(lockSpec과 같은 함수)로 골든이 자기 스팩이 기록한 입력과 어긋나면 막는다. 스팩이 기록하지 않은 경로의 변경은 보지 않는다. | `ddc3314` JUDGMENT의 재-잠금 주장이 트리에 없었고 254 커밋 동안 몰랐다(2026-09-10 실측). | 골든을 v2로 이관해 게이트로 승격했으며 분모가 스팩 자신의 기록이라 오탐 0이다. 샤드 골든이 생기면 확장한다. |
| write 임대(`write-lease-lib`, 2026-09-11 신설) | developer 쓰기 스폰만 `agent_id`로 직렬화한다 — 다른 writer·메인 스레드는 대상 밖이고 중첩 프로젝트 둘도 같은 임대를 써 과직렬화한다. 자동 회수가 없어 홀더가 `SubagentStop` 없이 죽으면 사람이 임대를 지워야 하고, 해제 훅 배포 검사는 `build-plugin.mjs` 소스 텍스트 프록시다. `.git/`·임시 디렉터리 둘 다 쓸 수 없으면 fail-closed로 전부 막힌다. | — | 종단 실측(receipt `docs/audits/receipts/2026-09-11-write-lease-e2e.json`): 두 번째 스폰 차단·종료 뒤 잔존 0. `SessionEnd` 자기 `session_id` 해제와 `(sessionId, agentId)` 홀더 대조는 `session_id` 동일성·Esc 중단을 잰 뒤 넣는다. `agent_id` 없는 런타임에서는 산문 규칙만 남는다. |
| 검증 스킬 착수 승인 검사(`test-wh-lanes.mjs`, 2026-09-11 신설) | 검증 스킬이 source를 쓰는 agent(소유 레지스트리 기준 `_workspace/` 밖)를 부르면 본문에 「착수 전 승인」이 있어야 한다 — 감사 FINDING-002 후속(`visual-design-verify` 직접 진입에 쓰기 전 승인이 없었다). **프록시 세 겹**: ① 「검증 스킬」은 디렉터리 이름 `*-verify`로 고른다 — 다른 이름의 검증 흐름은 못 본다 ② 「부른다」는 백틱·목록 항목의 **이름 언급**이다 — 부르지 않고 언급만 해도 대상이 되고(과대), 이름 없이 부르면 못 본다 ③ 승인 문구가 본문 **어디든** 있으면 통과한다 — writer를 부르는 단계에 결속되지 않아 무관한 절에 한 줄 넣어도 green | 우회 관측 없음(2026-09-11). 기존 web-verify 단언이 frontmatter changelog의 같은 문구로 통과하던 공허를 이 변경이 발견해 본문만 읽게 고쳤다 — seed 2건이 두 스킬의 본문 승인 문장 삭제를 잡는다 | 실질 해소 후보: 단계 결속 검사(writer를 부르는 절 안의 승인) 또는 승인 ledger(누가·어떤 범위를·언제 승인했는지 receipt). 우회가 관측되면 그때 올린다 |

| WORK 분해 검증(`work-analysis`·`work-plan`·`claim --work`, 2026-09-11 신설, P0+P1) | 참조·상태·그래프만 검증하고 의미 품질(재사용 판정·경계·관찰의 사실성)은 보지 않으며 `scanCoverage`·`accessState`·`decisionStatus`는 작성자 자기진술이다. 완료 판정은 TC ID 인용·`targetRefs` 경로 변경 프록시라 주석 인용·의미 없는 수정도 통과한다. 트래커 편집권이 곧 범위 확장권이다(방어는 인젝션 정규식 스캔·상한·`not-automated` 표시까지). 제품 유예(`featureDisposition.deferral`)는 작성자 선언인데 부모 집계 분모에서 빠진다(뺐다고 적는다). `works-merged`는 작업 머지까지이며 통합 revision의 TC 증거는 연결되지 않는다(`closeEligible`은 늘 거짓). | 실 provider가 호출자 라벨을 버려 재개 조회가 「완전·0건」으로 중복 발행될 뻔한 것을 적대 리뷰가 잡았다. | 반증 seed 21곳·배포본 smoke와 GitHub·Jira DC 실 왕복(receipt `2026-09-15-work-github-live`·`2026-09-15-work-jira-live`)으로 결박했다. Jira Cloud(v3)·GitHub Enterprise·`--resolve`·`--aggregate`·결과 불명 재개는 NOT_RUN이다. 승격 조건: 실제 팀 프로젝트 1회 적용(첫 사용자 동작 통합까지 시간·검토 수정 횟수 측정). |
| 사람이 만든 개발 티켓 → WORK(`ticket-work.mjs`·`ticket-work-run.mjs`, 2026-09-15 신설) | CLI는 판정서의 항목·근거 문자열 존재·형식·원문 출처(정규화 문자열 포함)까지만 검증하고 판정의 참·근거 경로 실재는 보지 않는다. `--assessment <지문>` 확인은 개발자 신원·의사의 증명이 아니며(스킬 규범이 막는다) 개발 티켓 분류는 팀 선언을 믿는다. 완료 조건 충족은 수정 범위 변경 + `TT-` 인용까지이고 인젝션 스캔은 정규식 프록시다. 에이전트가 쓴 자기검사가 스팩 승인 경로까지 정한다 — 다섯 항목이 모두 「아니오」면 미리보기 확인이 1-A를 대신하고 `unverifiable` 인수 질문도 하지 않으며(`approval-checkpoints.md` ③ 예외), 「예」가 있어 1-A를 거쳐도 ①은 PC 재인수 대신 `기획 개정: ticket-acceptance` 라벨로 넘어간다 — PC 인수는 티켓 작업 어느 경로에서도 묻지 않고, 그 라벨은 change-scope(로컬)에만 남는 자기신고다(소비자 0), 티켓 작업의 `PUBLIC_CONTRACTS_TO_PRESERVE`는 비어 기존 계약 보존이 자기검사 「아니오」 한 겹뿐이다. 지문 확인은 스킬이 매개해 사용자에게 보이지 않는다. 임의 디자인(`designByImplementer`)은 티켓 출처면 원문 인용을 대조하지만 개발자 출처는 확인 지문 한 겹이다 — 사용자가 지시하지 않았는데 에이전트가 넣는 것은 스킬 규범이 막는다. 기획 미정 가정(`assumptions`)이 세부인지 정책인지는 에이전트가 가르고 CLI는 형식·착수 가능 한정만 잰다 — 권한·데이터 계약 가정은 자기검사 「예」로 스팩 승인이 다시 걸리지만, 금액·상태 전이·보존/삭제는 자기검사 밖이라 확인 미리보기와 티켓 가정 알림 코멘트뿐이다. | 기획 없이 운영하는 팀이 경로가 닫혀 하네스 동작을 문서에 틀리게 옮겨 적었다(2026-09-15). | 회귀 `test-ticket-work`·메모리 Jira e2e·반증 등록부의 `ticket-*` 항목; 로컬 등록·배정 먼저·확인 뒤 코멘트 흐름은 실 트래커 NOT_RUN. 승격 조건: 실제 팀 트래커에서 사람 개발 티켓 3건 이상을 이 경로로 판정·착수·머지. |
| 픽업 범위의 테스트 레이어(팀 워크플로우) | 픽업 범위는 `writePaths`에 소스와 겹치지 않는 `spec.testLayers`를 더한다. 테스트 레이어 안에서는 작업 사이 격리가 없고, 훅의 앱 접두 매칭 때문에 짧은 이름(`tests`)은 소스 아래의 같은 이름 디렉터리에도 맞는다. 테스트를 소스 옆에 두는 레이어는 넣지 않으므로 그때 테스트는 작업 경로 안에 둬야 한다. | 모노레포 시뮬레이션(2026-09-18): 넣기 전에는 따로 둔 테스트 디렉터리 쓰기를 훅이 막아 완료 조건인 테스트를 쓸 수 없었다. | `separateTestLayers`가 양쪽을 정규화해 소스와 겹치는 레이어를 빼고, `test-work-pickup`·`test-team-monorepo-e2e`(실제 훅 프로세스)와 반증 seed가 고정한다. |
| 작업 범위 밖 파일 알림(`scopeDrift`, 팀 워크플로우 link) | 보고 전용이며 원장에 남지 않는다. base 이후 **커밋한** 파일만 보고(커밋 안 한 편집·머지 커밋 안의 충돌 해결은 안 보인다), 훅의 앱 접두 매칭을 따라 짧은 범위 이름(`package.json`·`tests/`)은 다른 앱의 같은 이름 경로도 범위 안으로 읽는다. 확장자 없는 파일은 디렉터리로 읽어 선언해도 범위 밖으로 나올 수 있고, 개명 표시는 사용자 git 설정(`diff.renames`)에 따라 다르다. | 같은 파일 시뮬레이션(2026-09-18): 병렬 두 작업의 package.json 손 편집이 link를 조용히 통과하고 머지에서야 충돌했다. | 실제 게이트는 머지 충돌이다. 점검하지 못하면 `checked:false`와 이유를 사용자에게 옮긴다. `test-work-link`·`test-team-shared-file-e2e`와 반증 seed가 고정한다. |
| 원격 계획 개정 대조(`plan-behind-remote`, 팀 워크플로우 pickup·link) | 기준은 PR base → 계획의 `baseBranch` → 원격 HEAD 순이고, `git log HEAD..<원격> -- work-plan.json`으로 **계획 파일을 건드린 원격 커밋**만 본다(계획이 바뀌지 않은 원격 커밋은 막지 않는다). pickup·link는 먼저 fetch하지만 실패해도 막지 않고 마지막 원격 참조로 잰다. 보드는 fetch하지 않는다. git이 아니거나 원격이 없으면 재지 않는다. PR base가 개정이 없는 브랜치(예: release)면 다른 브랜치의 개정은 잡지 않는다. | 5인 개정 시뮬레이션(2026-09-18): 개정을 받지 않은 개발자의 link가 로컬 계획과 대조해 `verified`로 통과했다. | `test-work-pickup`·`test-work-link`(주입)·`test-team-revision-e2e`(실제 clone·fetch)와 반증 seed가 고정한다. |
| 완료 회수(되돌림 PR·트래커 재오픈, 팀 워크플로우) | 하네스는 되돌림을 기록하지 않는다 — 되돌림 PR 제목(`Revert "[키] …"`)이나 트래커의 재오픈(Jira: 해결 사유가 비워진 이력, GitHub: reopened 이벤트) 뒤의 머지·커밋·끝남만 완료로 센다. 되돌림 PR 없이 직접 되돌린 커밋, 해결 사유를 거치지 않고 다시 연 Jira 워크플로우, 해결되지 않은 채 열려 있던 티켓의 되돌림은 보이지 않는다(되돌림 PR을 쓰라고 안내한다). 재오픈 이력은 머지 근거가 있고 열려 있는 티켓만 읽는다. 되돌린 코드 위에서 이미 끝난 후속 작업은 거두지 않는다. 대체 알림(`work-retired`)은 한 번이며, 코멘트 뒤 원장 기록이 실패하면 다음 발행이 같은 코멘트를 다시 남긴다. | 5인 개정 시뮬레이션(2026-09-18): W1 머지를 되돌린 뒤에도 보드가 W1을 완료로, 그 위 사슬을 열린 것으로 보였다. | `test-work-events`(되돌림 PR·재오픈 시각)·`test-work-provider`(두 트래커의 재오픈 이력)·`test-team-revision-e2e`와 반증 seed가 고정한다. 실 트래커 이력 조회는 NOT_RUN. |
| 참조 문서 크기 ratchet(`validate-contract-hygiene` `referenceBytes`) | 파일 하나의 바이트로 컨텍스트 비용을 잰다 — 20KB 초과 문서를 둘로 쪼개고 둘 다 읽게 하면 파일별 검사는 통과하고 로드 총량은 그대로다(`_workspace` 분할과 달리 참조 문서에는 INDEX·소비자 읽기 규칙이 없다). `assets/*.md`·SKILL.md 본문은 대상 밖이고, 줄어든 문서의 상한은 자동으로 내려가지 않는다. | 실사용 우회 사례 없음(2026-09-24 사전 등록). | 분할의 총량은 리뷰어가 본다. 실질 해소 후보는 소비 경로별 로드 합계 계측. 회귀 `test-reference-size-ratchet`·반증 seed. |
| 검증 판정 기록 대조(`record-verdict.mjs`·`verdict-record-lib.mjs`, 릴리스 게이트) | SubagentStop이 검증 에이전트의 최종 응답에서 `## Result`만 읽어 기록하고 게이트는 상태 문자열만 대조한다 — 판정의 내용이 참인지는 보지 않는다. 스크립트 기록자(`report-test-qa`)는 보고서 digest까지 기록·대조하지만, 영수증이 참인지는 영수증 검증의 몫이다. 기록 폴더는 Write/Edit로 쓸 수 없지만 메인 세션의 Bash는 훅 밖이라 기록을 지어낼 수 있다. 기록이 한 번도 없던 프로젝트(훅 미배선·옛 판본)는 대조하지 않고 통과시킨다. | 실사용 우회 사례 없음(2026-09-24 사전 등록). | `test-verdict-record`(훅 프로세스·게이트 통합)와 반증 seed 2건. 실 세션 SubagentStop 기록은 NOT_RUN. |
| 하네스 에이전트 판별(`agent-identity.mjs`, 훅 2종·개발 착수 예행) | 플러그인 판본에서 접두 없는 이름은 프로젝트(`.claude/agents/`)·사용자(`~/.claude/agents/`)가 같은 `name:`을 정의했을 때만 하네스 밖으로 본다 — 머리말 `name:` 문자열 대조이고, 런타임이 하네스 에이전트를 접두 없이 보고하면서 같은 이름의 프로젝트 정의가 있으면 하네스 제한이 풀린다 — 공식 훅 문서는 플러그인 에이전트를 `plugin:agent`로 보고한다고 적는다(문서 확인, 실측 아님). 판본은 빌드 산출물의 `.claude-plugin/plugin.json` 실존으로 가른다. | 실사용 우회 사례 없음(2026-09-24 사전 등록). | `test-agent-identity`(플러그인 배치 복사로 훅·예행 실행)와 반증 seed 2건. 런타임 `agent_type` 접두 실측은 NOT_RUN. |
| 연결 전 리뷰(`link` `review`, 팀 워크플로우) | CLI는 부를 리뷰어(하네스 `code-reviewer` + 트래커 설정 `reviewAgents` + 개발자 로컬 `~/.claude/web-harness/local.json`의 리뷰어·참고 문서)만 돌려준다 — 로컬 설정은 그 개발자에게만 적용되고 팀은 모른다. 로컬 리뷰어 이름의 실존은 대조하지 않고, 없는 참고 문서·형식 오류는 보고할 뿐 막지 않으며, 로컬 파일은 등록부·ratchet 밖의 표면이다 — 리뷰가 실제로 돌았는지, 결함을 고쳤는지는 기록도 대조도 하지 않는 스킬 규범이다. 프로젝트 리뷰어 이름은 팀 선언을 믿는다. | 실사용 우회 사례 없음(2026-09-24 사전 등록). | `test-work-link` 리뷰 계획 회귀; 실제 리뷰 수행은 NOT_RUN. 승격 조건: 리뷰 결과를 head 지문에 묶어 `link`가 대조. |
| 개발 티켓 생성(`create`, 팀 워크플로우) | 양식(네 절·빈 선택 절 없음·완료 조건 목록·FEAT/TC 없음)과 **제목** 중복만 기계로 잰다 — 제목을 바꾼 같은 작업은 막지 못한다. 끝난 티켓과의 겹침은 기계로 막지 않는다 — 초안 에이전트의 코드·결정 검색과 미리보기 `recentDone`(최근 끝난 개발 티켓 한 페이지)을 사람이 보는 두 겹이다. 제목 대조는 팀 접두어(`titlePrefix`)를 붙인 제목으로 하므로 접두어를 나중에 정하면 그 전에 만든 접두어 없는 열린 티켓과는 대조되지 않고, 접두어 판정은 대소문자·공백을 그대로 본다. 하네스가 쓴 본문을 하네스 판정이 `source: ticket`으로 인용하므로 자기 산출물로 자기를 재는 구조이고, 독립 검증은 생성 미리보기의 사람 확인 한 겹이다 — 확인 지문이 미리본 판본에 묶이지만 스킬이 매개하므로 사람이 실제로 봤다는 증명은 아니다. 초안 인젝션 스캔은 정규식 프록시다. Jira Cloud(v3) 평문 렌더는 실 트래커 NOT_RUN이다. 응답이 끊긴 생성은 원장에 남기지 않고 다시 실행의 같은 제목 검사로 찾는다(열린 개발 티켓을 못 읽으면 만들지 않는다). | 기획 없이 공통 기반을 손 티켓으로 먼저 진행하는 팀이 하네스로 티켓을 만들 수 없었다(`claim`은 기획 필수). | 만든 티켓은 WORK 마커 없이 개발 티켓 분류만 붙어 손 티켓과 같은 판정·등록을 탄다. 회귀 `test-ticket-create`(메모리 Jira: 미리보기 쓰기 0·분류·마커 없음·재실행 건너뜀·pickup 판정 요구)와 반증 seed(`ticket-create-*`). |
| 사람 티켓의 겹침은 보여 주고 확인받는다(팀 워크플로우 pickup) | 판정·등록이 개발자 로컬이라 남이 다른 클론에서 등록한 티켓의 수정 범위는 보이지 않는다 — 동료 진행 티켓은 배정·상태로만 미리보기에 보이고, 다른 사람과는 **배정 먼저**로 갈린다. 내 클론 안의 겹침(발행한 계획 작업·내 등록 중 머지 전)도 막지 않고 보여 준 뒤 확인하면 겹친 채 등록한다 — 확인 지문이 겹침 목록을 묶고(틀린 확인에 기대 지문을 돌려주지 않는다) 등록 기록에 `acceptedOverlaps`가 남는다 — 읽는 소비자는 없고 사후 대조용이다. 지문은 스킬이 매개하므로 사람이 실제로 미리보기를 봤다는 증명은 아니다. 같은 파일 충돌은 머지할 때 정리한다(사용자가 받아들인 한계). 배정은 비교·교체가 아니라 배정 직전·직후 다시 읽어 양보한다. | 트래커 정본 이관 5단계(2026-09-19): 티켓 원본을 고치지 않기로 하면서 다른 클론의 수정 범위를 읽을 곳이 없어졌다. | `test-team-human-tickets-e2e`(동시 픽업·머지 뒤 겹침 해제)·`test-ticket-work`와 반증 seed가 고정한다. |
| 트래커 끝남을 완료로 읽기(팀 워크플로우 board·pickup) | 사람이 머지 없이 완료로 바꿔도 완료로 센다(상태·해결 사유 변경은 트래커 이력에 남는다). 머지된 PR(제목의 티켓 키)·머지된 커밋·트래커 끝남 중 하나면 완료이고, 트래커 취소는 그 뒤의 머지만 이긴다. 트래커를 못 읽으면 계획·원장만으로 판정하고 그 사실을 결과에 적는다. 해결 사유 이름은 인스턴스마다 달라 설정이 틀리면 완료를 취소로(또는 반대로) 읽고, 해결 사유 없이 끝난 티켓은 세지 않는다. GitHub 종단(목록→겹침)은 메모리 대역 NOT_RUN. | 트래커 정본 검토(2026-09-18): 하네스가 Jira를 완료로 전이하지 않아 원장과 트래커가 따로 놀았고, Won't Fix도 상태 범주가 done이다(사내 Jira 실측). | `test-work-provider`·`test-work-board`·`test-work-pickup`과 반증 seed가 고정한다. |
| 머지된 커밋을 완료로 읽기(Jira Git Integration, 팀 워크플로우 board·pickup) | GitHub 스쿼시 머지 커밋만 센다 — 기대 base에 있고, 제목이 티켓 키로 시작하고 `(#PR번호)`로 끝나는, 머지 커밋이 아닌 커밋. base 직접 푸시·체리픽·다른 호스트의 병합 방식은 세지 않는다(I3: GitHub 스쿼시 기본 제목에 기댄다). 머지 커밋 방식(feature 커밋이 base에도 보이는지)과 되돌림 커밋의 연결은 실측하지 않아 쓰지 않는다 — 되돌림은 되돌림 PR 제목과 트래커 재오픈으로 거둔다. 애드온 색인이 늦으면 머지 직후 잠시 안 보인다. 키를 제목에 잘못 단 남의 커밋과, 같은 이름의 다른 저장소(포크)의 base 커밋은 구별하지 못한다(애드온 응답의 저장소는 id·이름뿐이다). 트래커 취소보다 뒤의 커밋만 취소를 이긴다. 애드온이 없으면(경로 404 — 없는 키는 400이라 구별된다) 근거 없이 가고 픽업 결과 `trackerRead.mergeEvidence`에만 남는다. 저장소 기본 브랜치를 모르면 보드가 알린다. | 트래커 정본 검토(2026-09-18, 사내 Jira 실측): 애드온이 GHE 저장소 27,478개를 색인하고, AOA-19의 스쿼시 커밋 `[AOA-19] … (#17)`이 `develop`에 있다고 돌려줬다. 11건 병렬 0.18초. | `test-work-provider`(근거 규칙·애드온 유무)·`test-work-board`와 반증 seed가 고정한다. |
| 사람 티켓의 등록 기록 = 개발자 로컬(팀 워크플로우 pickup·board·link) | 확인한 판정은 그 개발자의 로컬(`ticket-assessments/<키>.registered.json`, git 제외)에만 있다 — 티켓 본문·속성·라벨은 쓰지 않는다. 로컬 파일을 잃으면 다시 판정한다. 확인한 뒤 사람이 원문을 고치면 원문 지문으로 `link`가 STALE로 막고(`--accept-unverified-scope`로 넘기면 연결 기록·PR 본문 문단에 남는다), 원문을 다시 읽어 대조하므로 트래커를 못 읽으면 `--accept-unverified-scope` 없이는 막는다. 착수 불가 판정은 로컬이라 다른 클론 보드에는 보이지 않는다(배정과 확인한 요청 코멘트로 안다). | 트래커 정본 이관 5단계(2026-09-19): 사용자 결정 — 티켓 원본은 그대로 두고 개발자 기록은 커밋되지 않는 로컬에 둔다. | `test-ticket-work`(로컬 등록·재확인·취소)·`test-team-human-tickets-e2e`(원본 불변·커밋 제외)·`test-work-routes-e2e`(원문 변경 STALE)와 반증 seed가 고정한다. |
| 완료 = 기대 base에 머지된 PR의 제목·브랜치 키(팀 워크플로우 link·board·pickup·자동 닫기) | 완료의 근거는 origin 저장소에서 기대 base로 머지된 PR 목록 중 **제목이 티켓 키로 시작하거나 브랜치 이름에 키가 한 토큰으로 있는** 것이다(브랜치는 문자가 있는 키만 — 숫자뿐인 이슈 번호는 제목으로만) — 제목·브랜치만 맞으면 `link`의 게이트(STALE·완료 조건)를 거치지 않은 PR도 완료로 읽히고, GitHub 자동 닫기(v5)는 제목의 이슈 번호로만 닫는다(신뢰 경계는 PR 리뷰·머지 승인). `link`는 제목·브랜치 모두에 키가 없으면 막지만(`--base`여도 PR을 읽는다) 제목을 못 읽으면 알리기만 하고, `link`를 거치지 않은 PR은 막지 못한다. 되돌림을 되돌린 PR(`Revert "Revert "…""`)은 재착륙으로 센다. 목록은 최근 300건까지만 보고 넘으면 알린다. 저장소 원격을 모르거나(로컬 경로·ssh/https가 아닌 원격) gh를 못 부르면 근거 없이 간다. 한 PR은 한 티켓이다 — 제목이 트래커 키로 시작하면 제목이 정하고 브랜치는 보지 않는다. 되돌림은 제목의 `Revert "…"` 겹과 브랜치의 `revert-N-` 접두 중 큰 겹으로 센다(둘 다 고치면 완료로 읽힌다). 연결 기록은 개발자 로컬이라 다른 클론의 멱등·표시에는 쓰이지 않는다. | 트래커 정본 이관 5단계(2026-09-19): 티켓 원본을 고치지 않기로 하면서 연결 코멘트를 쓸 수 없어졌다. AOA 실측(aoa-web): 머지된 PR 18건 중 제목 키 1건, 브랜치 키도 같은 1건 — 브랜치 근거는 관습(`feature/KEY-…`)을 기대하고 더했고 효과는 아직 실증되지 않았다. | `test-work-link`(제목 키·로컬 기록)·`test-work-close`(v5)·`test-team-e2e`·`test-team-github-e2e`와 반증 seed가 고정한다. 실 GitHub PR 목록 조회는 NOT_RUN. |
| 팀 공유 설정(`team-sharing`, 개발 준비 검사) | `.gitattributes`·`.gitignore`에 정해진 **줄이 있는지만** 본다 — 같은 뜻의 다른 패턴(`_workspace/**` 등)은 없다고 읽고(오탐, 덧붙이면 해소), 전역 git 설정·`.git/info/attributes`는 보지 않는다. 사람이 파일을 지우거나 `merge=union`을 무시하는 병합 도구를 쓰면 막지 못한다. 이미 커밋된 로컬 작업 파일은 `git ls-files`로 잡지만 git이 아닌 트리에서는 줄만 본다. 배정 전 두 사람이 같은 티켓을 판정하면 병합 순서에 따라 한쪽 판정이 남는다. | 3인 시뮬레이션(2026-09-18): 설정이 없으면 PR 셋 중 둘이 원장·작업 범위에서 충돌하고, 받은 사람의 픽업이 남의 작업 범위에 막혔다. | `test-team-e2e.mjs`가 설정 뒤 PR 셋의 무충돌 머지와 다음 픽업을, `test-development-readiness`가 원장에 덧붙인 두 브랜치의 무충돌 병합을 고정하고, 반증 seed가 규칙 줄 삭제를 잡는다. 실제 팀 저장소에서 NOT_RUN. |
| 재사용 목록의 사용처·중복(`reuse-inventory.mjs`, developer 스폰 전후) | 사용처는 **import 구문에 같은 이름이 나오는 비테스트 소스 파일 수**다 — 모듈을 해석하지 않아 외부 라이브러리의 동명 import(`Button`)가 로컬 export의 사용처로 섞이고(과소 경고), `import * as ns`로만 쓰이면 0이다(과대 경고). 중복은 **이름이 같은가**만 본다 — 이름이 다른 같은 책임은 잡지 못한다. default·라우트 규약 이름(`GET`·`fetch`·`metadata` 등)은 `entry`로 두 신호에서 뺀다 — 목록에 없는 규약 이름은 오탐이 된다 | 실사용 우회 사례 없음(**사전 등록**) | 보고 전용(exit 0)이며 게이트로 올리지 않는다. 같은 책임인지는 developer·code-reviewer가 코드를 열어 판정한다. 규약 이름 오탐이 관측되면 `ENTRY_NAMES`에 더한다 |
| 개발 중 스팩 변경의 사전 질의(phase-3 「개발 중 스팩 변경」) | 기계가 잡는 것은 developer의 스팩 직접 수정(소유권 훅)과 재확정 흔적(원장)뿐이다. **개발자에게 묻고 나서 재확정했는가**는 대조하지 않는다 — 오케스트레이터가 묻지 않고 `spec.mjs`를 돌려도 원장에는 정상 재확정으로 남는다. `SPAWN_RESULT: blocked` + `SPEC_CHANGE_REQUEST`는 완결성 검사에서 다른 blocked와 구분되지 않는다 | 실사용 우회 사례 없음(**사전 등록**) | 규율로 둔다. 실질 해소 후보는 재확정 시 `decision-log` 결정 ID를 원장 줄에 요구하는 것 — 무질의 재확정이 관측된 뒤에 넣는다 |
| 레이어 방향(`validate-layer-boundaries.mjs`, Gate B·C) | 정규식으로 읽은 정적 import 문자열을 상대경로·루트 tsconfig `paths`로 해석해 레이어 경로에 대본다. 번들러 전용 별칭(vite `resolve.alias`)·`require()`·템플릿 문자열 동적 import·재노출 체인 너머의 실제 소비는 보지 않는다 — 해석 못 한 `@`·`~/`·`#` 별칭은 `unresolved`로 따로 내고 통과로 세지 않는다. 해석은 `paths`의 첫 대상만, 루트의 tsconfig만 읽고(`extends`로 루트 밖 설정을 가리키면 못 본다), `#`(Node subpath imports)와 호이스트된 `@scope/*`는 해석 못 한 별칭으로 셀 수 있다. 슬라이스는 레이어 경로 바로 아래 **디렉터리**라는 가정이다. 방향 자체의 옳음은 스팩이 정한다 — 스팩이 넓게 허용하면 검사도 넓다. 결과는 Gate C가 evidence 파일로 남기지만 **릴리스 게이트는 읽지 않는다** | 실사용 우회 사례 없음(**사전 등록**) | 방향 선언이 없으면 `NOT_DECLARED`, 해석 못 한 별칭이 있으면 `INCOMPLETE`(둘 다 exit 3, 통과가 아니다). FSD·헥사고날 두 어휘와 별칭·상대경로 동등성을 `test-layer-boundaries.mjs`가 고정한다 |
| 미사용 코드 진단(`run-quality-gates --check deadcode`, knip) | receipt는 출력을 저장하지 않아(비밀 유출 방지) **있음/없음 1비트**만 남는다 — 목록은 사람이 `pnpm deadcode`로 본다. 설정 제외(`_workspace/**`·`.claude/**`) 밖의 생성물·프레임워크 진입점을 knip이 모르면 항상 `FAIL`이 되어 신호가 무의미해진다. `--all`·릴리스 증거에 들지 않는다 | 실사용 우회 사례 없음(**사전 등록**). 제외 없이 배포 형태(`.claude/` 포함)를 돌리면 하네스 스크립트가 미사용 파일로 잡혀 exit 1 — 제외 후 exit 0을 스크래치 프로젝트로 실측했다 | 진단 전용으로 두고 게이트로 올리지 않는다. `knip --fix` 계열은 스크립트 의미 검사가 거부한다(`validate-quality-policy`) |
| React Compiler 선언(`tech-stack.md`의 `REACT_COMPILER`) | 선언을 읽는 validator가 없다 — environment-scaffolder가 설정을 넣고 developer가 새 수동 memo를 쓰지 않는 것은 산문 준수다. 설정은 vite 프로필에서만 실측했고(빌드는 preset, vitest는 플러그인 직접 — preset은 client 환경에만 걸린다) Next는 미검증이다 | 실사용 우회 사례 없음(**사전 등록**) | 켰는데 빌드 산출물에 compiler-runtime이 없거나 vitest 설정에 플러그인이 없으면 잡는 검사가 후보다 — 실사용이 생긴 뒤에 넣는다 |
| CSP Report-Only 측정(`security-headers.md`, 템플릿 preview + e2e smoke) | 위반은 smoke가 **방문한 화면에서만** 수집된다 — 방문하지 않은 라우트·상호작용 뒤에 로드되는 스크립트·스타일은 측정 밖이다. preview와 배포 헤더가 같은 함수·같은 env로 만들어졌는지는 security-reviewer 산문 대조이고, 함수 출력을 배포 설정(`vercel.json`·`_headers`)으로 옮기는 수단도 아직 없다. 템플릿 가드는 헤더 문자열 존재만 본다. XSS lint 셀렉터는 spread props(`{...{dangerouslySetInnerHTML}}`)·`createElement` 인자·템플릿 리터럴·문자열 조립으로 만든 `javascript:`를 잡지 못하고, 출구 파일 안의 설정 약화는 리뷰 판단이다 | 실사용 우회 사례 없음(**사전 등록**) — 실측 `docs/audits/receipts/2026-09-20-csp-report-only.json` | 알림으로 두고, 강제 전환은 사용자가 고른다. 배포 헤더와 preview 정책의 기계 대조는 실사용이 생긴 뒤 넣는다 |
| 스킬 프리로드 검사(`validate-agent-boundaries` `droppedSkillPreloads`) | 런타임 동작을 전제로 한 검사다 — Claude Code가 `disable-model-invocation` 스킬의 `skills:` 프리로드를 조용히 뺀다는 사실(공식 문서 + 프로브 receipt `docs/audits/receipts/2026-09-25-skill-preload-probe.json`, 2.1.281)에 묶여 있어, 판본이 그 동작을 바꾸면 정당한 설정을 막는다(loud). | 분석 에이전트 2개가 1.1KB 본문만으로 돌았다 — 도메인 계약이 프리로드로 실린다고 믿었다. | Claude Code를 올릴 때 프로브를 다시 돌린다. 프리로드 대신 참조 경로를 본문에 적는 쪽은 판본과 무관하게 맞다. |
| 분할 설계 산출물의 보고서 요구(`release-report-policy` `hasDesignArtifact`) | 파일 또는 같은 이름의 디렉터리 **존재-only**로 요구를 켠다 — 빈 잔존 디렉터리도 요구를 켜고(막는 방향), 분할 축·읽기 프로토콜이 계약에 없는 산출물(`performance-budget/`)은 요구만 켜지고 검증자가 `.md` 부재로 BLOCKED를 낸다. | 분할하면 `qa-state`·`qa-perf` 요구가 조용히 사라지던 것을 막으면서 생긴 표면이다. | 내용 진실은 각 검증자와 sharding validator 몫. 분할 산출물을 읽는 검증자 정비는 도메인 검증자 통합에서 한다. |
| 배포본 플러그인 평가(`run-plugin-evals`, 릴리스 회귀) | 사례 3개·k=3이라 드문 회귀는 놓친다. 비대화 규약이 첫 ✋에서 멈추므로 승인 이후 흐름은 보지 않는다. 채점은 러너 채점기(메인 스레드 도구 호출·정규식)와 사후 검사(src 해시·배포본 초안 검사기)라 초안 문장의 품질은 판정하지 않는다 — src 보장은 해시 사후 검사에서 오고, 메인 스레드 `tool_used`는 서브에이전트를 못 보며 그 `file_path` 패턴은 입력 JSON 직렬화 형식을 전제한다. ✋ 표시는 토큰 프록시(어디든 찍으면 통과)이고, fix-lane은 결정 지점만 보는 사례라 기획 실행의 회귀는 change-lane의 스폰 채점기만 본다. 환경 오류 판정은 디스패처 메시지에 묶여 있어 러너의 샌드박스·도구 합성이 바뀌면 다시 확인해야 한다. | 첫 기준 측정이 부모 세션 PATH의 설치본 디스패처를 불러 하네스 스크립트가 하나도 돌지 않았는데 레인 사례가 통과했다(음성 채점기만 있었다). change 레인 사례는 시드에 없는 개념을 가정한 요청이라 기획 전에 묻는 실행(계약상 정당)을 양성 채점기가 떨어뜨렸고, 거꾸로 analyst가 blocked를 돌려준 뒤 같은 질문으로 멈춘 실행은 스폰 이름·✋ 글자로 통과했다(기준 측정 2/3, 실질 1/3). | 러너가 PATH를 거르고 환경 오류를 판정에서 빼며, 회귀 사례마다 한 일을 보는 채점기·사후 검사를 요구한다. 승인 이후 사례는 ✋를 이미 인수한 시드로 후속한다. 레인 사례의 요청은 interaction-contract의 질문 조건에 걸리지 않게 쓴다(evals README 「사례 작성」). change 레인 사례는 기획 산출물(`01_plan/feature-plan.md`, 시드에 없다)의 존재를 사후 검사로 보고, 시드에 이미 있는 파일을 보는 `file-exists`는 사례 validator가 공허하다고 잡는다. receipt는 schema 3부터 사례·시드 digest로 잰 판본을 묶는다. |
| 슬래시 메뉴 숨김(`validate-entry-points` `invocationMismatches`·배포 문서의 슬래시 표기) | frontmatter 선언과 문서 표기만 본다 — 메뉴에서 실제로 빠지는지는 런타임 동작이라 프로브 receipt(`docs/audits/receipts/2026-09-25-user-invocable-probe.json`, 2.1.281)에 묶여 있다. 슬래시 없이 이름만 적은 모델 지시는 막지 않으므로, 읽을 경로가 없는 지시도 통과한다. | 숨기기 전에 사용자에게 보이는 안내(HANDOFF 템플릿·검증 보고의 「다음 단계」·프로젝트 체크리스트)가 내부 명령을 가리켰다 — 숨기면 막다른 길이고, 숨긴 명령을 치면 아무것도 돌지 않는다(`-p` 실측 0턴·$0 — 대화형 입력은 미실측). | Claude Code를 올릴 때 프로브를 다시 돌린다. 모델 지시는 `.claude/skills/<이름>/SKILL.md` 경로로 적는다(배포본에서 계약 사본 경로로 바뀐다). |
| 시점 로드 분리(`change-lane-checkpoint.md`·`spawn-decomposition-contract.md`) | 분리한 계약을 언제 읽는지는 산문 포인터(핵심 계약·Phase 3 3단계·reentry-map 행)로만 보장된다 — 빌더 스폰 전에 `spawn-decomposition-contract.md`를 읽지 않으면 fit-gate·계획 잠금이 불리지 않고, 그것을 잡는 기계 검사는 없다(잠금 없는 매니페스트는 자기선언으로 보고될 뿐이다). 배포본 평가는 ✋ 이전만 보므로 빌더 스폰 경로의 로드는 측정 밖이다. | 실사용 우회 사례 없음(**사전 등록**) | 부르는 자리를 셋으로 둔다. 빌더 스폰 경로를 보는 평가 사례가 생기면 트레이스로 로드 여부를 잰다. |
| 퇴역 에이전트 이름 검사(`agent-registry` `RETIRED_AGENTS` — validate-harness 옛 에이전트 목록·평가 시나리오 단언·분할 INDEX 후임 안내) | 이름 문자열만 본다 — 옛 역할을 이름 없이 서술한 문장은 잡지 못한다. 합친 에이전트가 옛 산출물 형식을 계속 내는지는 산출물 검사(인계 판정·design-binding·배포본 평가 사후 검사)가 본다. 진행 중 프로젝트의 분할 INDEX에 남은 옛 이름은 분할 검사가 막고 후임을 알린다. | 실사용 우회 사례 없음(**사전 등록**) — 접미사 추론만으로는 같은 접미사의 마지막 에이전트가 사라지면 그 이름을 놓친다는 것을 통합 설계에서 발견했다. | 에이전트를 합치거나 없앨 때 퇴역 이름과 후임을 이 목록에 먼저 적는다. |

## 5. 이 문서의 갱신

판단 질문·예산·프록시 등록부의 변경 자체가 "게이트/validator 변경" 클래스다 — 같은 게이트를
거친다. 등록부에서 행을 지우려면 해당 프록시가 실질 검증으로 대체됐다는 증거가 필요하다.
행에는 **현재** 한계·우회 사례 한 문장·대응만 적는다 — 경위와 수정 이력은 커밋 메시지가 정본이다.
해소된 사고(남은 한계 없음)는 행을 만들지 않는다.
