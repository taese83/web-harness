# Day-2 파일럿 프로토콜 — 하네스 vs 대화+push (반증 실험)

> 배경: 저자가 자기 하네스를 우회하고 실제 반복 개발을 대화+직접 push로 수행한다는
> 스모킹 건(2026-08-18 합의, 메모리 `harness-dogfood-gap`). 원인 진단은 "모든 가치가
> 서명 릴리스 단일 출구에 결박"이었고, 재진입 기계화 3층(reentry-map `5c0e157` ·
> SessionStart 훅 `34a8db9` · CLAUDE.md 마커 `c5a8343`)이 그 처방의 1단계였다.
> 이 파일럿은 그 처방이 실제로 작동하는지를 **저자의 실제 변경 건**으로 심판한다.
> 심판대에 서는 것은 게이트가 아니라 하네스다.

## 반증 가능 기준 (사전 고정 — 사후 변경 금지)

- 대상: tamiya 계열 실서비스(tamiya-motor-lab / tamiya-race-app / minicar-laptime)의
  **실제 변경 약 3건**을 재진입 경로(reentry-map 상황 A/B)로 수행한다.
- 합격: 3건 완료 후 저자가 자기 기준으로 "다음 변경도 이 경로로 하겠다"고 판단하면
  합격이다. **우회의 재발 여부 자체가 판정이다** — 파일럿 후 저자가 다시 대화+push로
  돌아가면 불합격.
- 불합격 시 조치(사전 합의): day-2 제품 재설계, 또는 하네스 정체성 주장을
  "0→1 그린필드·대형 변경"으로 축소(I1 — 실측이 주장을 이긴다).
- 이 파일은 라운드마다 실측을 append한다. 좋게 나온 라운드만 기록하는 것은 I1 위반이다.

## 측정 항목 (라운드당)

| 항목 | 출처 |
|---|---|
| 스폰 수·토큰(스폰 합계) | 프로젝트 `_workspace/04_qa/execution-telemetry.json` (계약: null 허용, 위조 금지) |
| 벽시계 시간(요청→검증 완료) | 세션 기록 |
| 실행 게이트·검증 목록 | 라운드 기록에 열거(생략은 생략이라 적는다) |
| 산출 receipt | change-scope 항목·구현 검증 증거·(승인 표면 썼으면) 승인 레코드 |
| 대화+push 대비 저자 체감 | 라운드 종료 시 저자 한 줄 판정(더 나음/비슷/더 나쁨) |

비교 기준선: 같은 규모 변경을 대화+push로 할 때의 관행 — 정밀 토큰 대조는 불가능하므로
(과거 대화의 토큰은 미계측) **저자 체감 + receipt 유무의 질적 차이**가 1차 판정이고,
토큰·시간은 참고 수치다. 이 한계는 정직하게 남긴다.

## 온보딩 비용의 분리

1회차는 브라운필드 온보딩(CLAUDE.md 마커 append 등 — brownfield-adoption.md 도입
사다리)을 포함하므로 총비용을 **온보딩분·변경분으로 분리 기록**한다. 2·3회차가 순수
재진입 단가다. 재진입 스택 사전 실측(2026-08-18): `detect-harness-project.mjs`가
tamiya-motor-lab·tamiya-race-app 두 실제 repo에서 발화·reentry-map 절대경로 주입 확인.

## 라운드 기록

### R1 — tamiya-motor-lab N1 완화(R68 초기 잠금 검증) [2026-08-19]

- 요청: repo 문서화 백로그 1순위(pano-tuner-comparison.md §4.1) — 신규 획득 무hint
  프레임의 comb식 무이력 하강 검사. DSP 엔진 로직 변경(feature/bug-fix급).
- 상황 분류: **A (Iterate 라운드)** — buildable 기존 프로젝트 + 범위 좁힌 변경.
- 로드한 계약: 상황 A core 6종 = **37.4KB**(SKILL 전체 재로드 없이). 훅·마커 신호 정상.
- 스폰·토큰: **스폰 0** — 오케스트레이터 직접 편집(엔진 ~20줄 + 테스트 1파일).
  main-loop 토큰은 미계측(계약상 null — 지어내지 않음).
- 벽시계: **09:59:34 → 10:06:40 (~7분)** — 온보딩(마커 append) 포함. 순수 변경분은
  브리프→재현→수정→전체 게이트까지 ~6분.
- 게이트·검증: 재현 테스트(변경 전 FAIL — 배값 고착 113프레임 재현) → 수정 → 신규
  테스트 2/2 + 기존 전체 스위트 318/318 green + typecheck·lint·build green(toolchain
  pin 명시). 실기기 레짐은 DEPLOY_ONLY — 사용자 위임 명시.
- receipt: change brief 전 필드 + 라운드 결과가 change-scope.md에 기록.
  **QA evidence: STALE (재발급 불가)** — run-quality-gates가 locked profile 불일치(M3
  어댑터 변경)로 거부, 재잠금은 INGESTION_CONTRACT_MISSING(api/ 하이브리드 모델링
  갭)으로 BLOCKED. **도그푸드 갭 진단("서명 출구가 하이브리드를 못 모델링")이 R1에서
  그대로 실측됨** — 출구 분리(범위 서명·무서명 receipt) 필요성의 실증 데이터.
- 저자 판정(2026-08-19): **더 나음** — 대화+push 대비.

### R2 — tamiya-motor-lab 디자인 리프레시(v4 Pit-Wall Amber) [2026-08-19]

- 요청: 사용자 — "더 스타일리쉬·깔끔·트렌디하게"(ui-change). 발산 기계화 경로의
  **실사용 프로젝트 첫 적용**(잔여 미실증 슬롯 해소).
- 상황 분류: A(Iterate) + 발산 프로토콜. 진행: 발산 3후보(타일) → 렌더 판정 →
  단일 시안 승인(AskUserQuestion) → v4 개정 → 소스 동기화 → 라이브 양모드 검증 →
  작업 커밋 3건(release는 사용자).
- 스폰·토큰: 2회(발산 154.5k + v4 개정 197.6k) = **352.1k**. 동기화·판정은
  오케스트레이터 직접.
- 핵심 실측: ① 진단 — 기존 v3가 상투 목록 "near-black+애시드"와 문자 그대로 일치
  ② 에이전트 자기검증 — README 라이트 값 3:1 미달을 v4 개정 중 자체 교정
  ③ export 심볼 보존 리네임 전략으로 typecheck 파손 0 ④ 시안 아카이브 규약
  (라운드 디렉터리) 첫 이행.
- receipt: change-scope 2단계 기록 + RENDER-VERDICT + v4 근거 절. QA evidence:
  STALE(R1과 동일 사유 — 하이브리드 모델링 갭).
- 저자 판정(2026-08-19): **더 나음** — R1·R2 공통, 결함·보수 왕복을 포함한 판정.

### R2 부록 — release 마찰 실측 (2026-08-19)

- **pnpm 상향 탐색이 release 차단**: motor-lab에 pnpm-workspace.yaml이 없어 pnpm이
  부모(하네스 루트)를 workspace 루트로 인식 — `pnpm version`의 clean-tree 검사가 부모
  repo를 향해, 부모가 더티면(병행 세션 WIP) 자식이 클린해도 release 불가.
  telemetry-viewer lockfile 사건과 같은 클래스의 2번째 실측 — 경계 파일 선언으로 해소.
  브라운필드 온보딩 체크리스트에 "pnpm workspace 경계 확인" 추가 후보.
- **R-번호 충돌**: 다른 머신의 미푸시 커밋(R68 근접 음량, 8/10 작성)이 릴리스 직전
  합류 — 리베이스(충돌 0) 후 이번 세션의 R68을 R69로 리넘버, 결합 상태로 전체 스위트
  재검증(320/320) 후 v2.42.0 릴리스. 멀티 체크아웃 환경에서 리비전 번호는 푸시 시점
  선점 — 넘버링 규율의 알려진 한계로 기록.

### R2 부록 2 — 결함·보수 사가 실측 (2026-08-19, I1: 결함도 기록한다)

- **사용자 발견 결함 3건**: ① v4가 색상만 적용(5축 중 1축 — "리컬러" 스코핑+색만 본
  검증) ② 레이스 셀렉션·인풋 미적용(§8.2 주석↔§10 표 불일치 + ToggleButton 과도기
  기각) ③ 기록 버튼 비활성 라벨 저대비(폐기된 ::before 계약의 고아 소비자).
- **결함이 만든 하네스 자산**: 시안 적용 완결성 절(5축 스코핑·대조표·색의 위장 효과) →
  시스템-우선 왕복 규칙 → mui 레인 임의값 가드(리뷰가 공백 발견) — 결함 1건당 계약
  1개꼴로 환류됨. 완결성 규칙 2(축별 대조)는 도입 직후 v4.1 이식 결손 2건을 실제로
  잡아 재발을 막음(자기 실증).
- **비용**: R2 누적 스폰 6회 ~1.12M tokens(발산 1·개정 1·재개 4 — architect 중단 3회는
  대형 문서 개정의 알려진 패턴, 범위 재개로 회수). 릴리스 3회(v2.42.0/1/2).
- **검증 한계의 정직성이 작동**: 대조표의 "요소 실측 미도달(로그인 게이트) — 사용자
  위임" 표기 지점에서 사용자가 결함을 발견 — 위임된 검증이 실제 수행된 구조. fixture
  기반 인증 후 검증(auth-verification)의 부재가 이 왕복 비용의 원인 — 개선 후보.
- 저자 판정(2026-08-19): **더 나음** — R1·R2 공통, 결함·보수 왕복을 포함한 판정.

### R3 — tamiya-motor-lab 라이브 델타 킷 온보딩 [2026-08-19]

- 요청: 사용자 — 델타 킷 온보딩(infrastructure). 전환 규칙의 "첫 델타 표면"을 as-is로
  생성 — 콘솔 라이브 카드 + 이후 변경의 승인 표면 확보.
- 상황 분류: 인프라 온보딩(계약상 오케스트레이터 직접 — 생성 루프가 브라우저 필수,
  design-preview-builder는 이 모드 생성 불가). **스폰 0**.
- 산출: 킷 5파일 + feature-plan FEAT 매핑 부록. validator live-delta/UNAPPROVED/에러 0.
  라이브 실측: 프록시 생성·identity 대조 통과(신원 검증 계약 첫 실사용)·앵커 3종
  스탬핑·오버레이 배지 가동·정착 후 경고 0.
- 왕복 2건: 오버레이 명시 초기화 시그니처, 하이드레이션 경합의 경고 시점 정교화
  (침묵 금지의 "실패" 판정 = 정착 후).
- 하네스 발견 2건: ① 구형(pre-FEAT) 플랜은 FEAT 매핑 부록 없이 온보딩 구조 불가
  (traceability 양방향 리터럴 대조) — brownfield-adoption 체크리스트 후보 ② 부록
  산문의 FEAT-토큰 언급도 grep 대조에 걸림(리터럴 스캔 성격 자기실측).
- 저자 판정(2026-08-19): **더 나음** — 대화+push 대비.

## 종합 판정 (2026-08-19) — 합격

사전 고정 기준의 완주: 실제 변경 3건(R1 엔진 수정 · R2 디자인 리프레시(결함·보수 왕복
포함) · R3 델타 킷 온보딩) 모두 **저자 판정 "대화+push보다 더 나음"**, 최종 질문("다음
변경도 이 경로로 하겠는가")에 **"합격 — 이 경로로 계속"** 응답. 반증 실험의 귀무가설
(하네스가 대화+push를 못 이긴다)은 기각됐다.

정직 한계: 합격의 궁극 판정 기준인 "우회의 재발 여부"는 선언이 아니라 행동으로만
확인된다 — 이후 실제 변경들이 이 경로로 오는지가 지속 조건이며, 우회가 재발하면 이
판정을 재개봉한다.

파일럿이 남긴 미해소 항목(합격과 별개의 다음 단계):
- **서명 출구 갭**(R1·R2에서 재현): locked profile 불일치 + 하이브리드 모델링 갭으로
  QA evidence 재발급 불가 — 출구 분리 설계(무서명 receipt·범위 서명)의 실측 근거 축적됨.
- 구형 플랜 온보딩 체크리스트(R3 발견), auth-verification fixture(R2 부록 2 발견),
  brownfield-adoption에 pnpm workspace 경계 확인 추가(R2 부록).
