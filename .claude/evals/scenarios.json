[
  {
    "id": "public-commerce-rendering",
    "entrySkill": "/web-plan",
    "prompt": "검색 유입과 상품 공유 미리보기가 중요한 공개 커머스 사이트를 설계해줘.",
    "assertions": [
      "tech-stack.md selects and justifies an SSR/SSG-capable rendering profile instead of forcing CSR",
      "requirements include canonical metadata, structured data, sitemap, status codes, and Core Web Vitals",
      "the compatibility matrix cites primary release and engine constraints"
    ]
  },
  {
    "id": "secure-admin-auth",
    "entrySkill": "/auth-setup",
    "prompt": "관리자와 조회자 역할이 있는 프로덕션 대시보드 인증을 추가해줘.",
    "assertions": [
      "BFF HttpOnly session cookie or OIDC Authorization Code with PKCE is selected explicitly",
      "no token, JWT, session ID, or refresh token is persisted in browser storage",
      "CSRF, CORS, single-flight refresh, server authorization, 401, and 403 tests are specified"
    ]
  },
  {
    "id": "complete-harness-packaging",
    "entrySkill": "/project-init",
    "prompt": "새 단일 React 웹앱을 만들고 Claude harness도 함께 배포해줘.",
    "assertions": [
      "all skills, agents, scripts, evals, and settings are copied",
      "validate-harness.mjs runs successfully in the generated project",
      "the generated orchestrators have no missing subagent dependency"
    ]
  },
  {
    "id": "api-contract-drift",
    "entrySkill": "/web-verify",
    "prompt": "OpenAPI에는 nullable 필드와 422 envelope가 있지만 MSW와 query 타입이 다른 프로젝트를 검증해줘.",
    "assertions": [
      "api-contract-verifier reports producer and consumer locations",
      "runtime schema, nullable fields, error envelope, and MSW drift are checked",
      "the verifier returns findings without editing source files"
    ]
  },
  {
    "id": "browser-accessibility",
    "entrySkill": "/web-verify",
    "prompt": "모바일 overflow와 키보드 focus 문제가 의심되는 앱을 브라우저에서 검증해줘.",
    "assertions": [
      "Playwright runs Chromium desktop and mobile projects",
      "axe, keyboard navigation, focus visibility, console errors, and failed requests are checked",
      "missing Playwright infrastructure results in BLOCKED rather than PASS"
    ]
  },
  {
    "id": "production-aws-deploy",
    "entrySkill": "/dev-orchestrator",
    "prompt": "S3와 CloudFront로 승인 기반 프로덕션 배포 파이프라인까지 만들어줘.",
    "assertions": [
      "GitHub Actions are pinned to full commit SHAs",
      "AWS authentication uses OIDC role assumption without long-lived access keys",
      "production environment approval, least permissions, artifact digest, smoke check, and rollback are documented"
    ]
  },
  {
    "id": "trusted-npm-publish",
    "entrySkill": "/dev-orchestrator",
    "prompt": "공개 TypeScript 라이브러리와 안전한 npm publish workflow를 만들어줘.",
    "assertions": [
      "npm trusted publishing and provenance are used without a long-lived npm token",
      "publint, attw, pack dry-run, and consumer fixtures validate package exports",
      "publish permissions are isolated to a protected environment"
    ]
  },
  {
    "id": "qa-immutability",
    "entrySkill": "/web-verify",
    "prompt": "테스트 실패가 있는 기존 프로젝트를 검증하고 수정 담당자를 알려줘.",
    "assertions": [
      "verifier agents have no Write or Edit tools",
      "the orchestrator persists returned reports",
      "fixes are routed to the smallest owner agent set and no snapshot update or auto-fix runs"
    ]
  },
  {
    "id": "typed-error-recovery",
    "entrySkill": "/web-orchestrator",
    "prompt": "401, 422, 500과 offline 복구가 중요한 업무 앱을 만들어줘.",
    "assertions": [
      "AppError preserves status, code, details, cause, and request ID",
      "4xx errors render inline while unrecoverable 5xx errors use QueryErrorResetBoundary",
      "retry decisions do not parse human-readable error messages"
    ]
  },
  {
    "id": "grafana-timeseries-dashboard",
    "entrySkill": "/web-orchestrator",
    "prompt": "그라파나와 같은 빅데이터 그래프 사이트를 만들어줘. 날짜별 조회 대시보드와 실시간 시계열 데이터가 필요하고 성능 문제가 없어야 해. API는 Mock으로 만들고 실제 연결 가이드도 제공해줘. 부족한 정보는 질문해줘.",
    "assertions": [
      "TIMESERIES_MODE asks for series, point rate, visible points, historical range, transport, latency, recovery, timezone, and browser budgets",
      "timeseries-architect produces a bounded snapshot plus stream architecture before API and component design",
      "the implementation includes historical range queries, deterministic realtime Mock transport, bounded buffer, batched updates, live pause and return-to-live behavior",
      "HANDOFF documents real snapshot and stream endpoints, authentication, cursor, heartbeat, aggregation, performance fixtures, and known backend limitations"
    ]
  },
  {
    "id": "timeseries-stream-recovery",
    "entrySkill": "/timeseries-dashboard",
    "prompt": "WebSocket이 끊기거나 데이터가 중복·역순·누락되는 경우에도 복구되는 실시간 메트릭 계층을 기존 앱에 추가해줘.",
    "assertions": [
      "the stream contract defines protocol version, sequence, cursor, heartbeat, duplicate, out-of-order, gap, reset, and error behavior",
      "realtime-data-builder uses bounded backoff, bounded ring buffers, idempotent cleanup, resume cursors, and deterministic ordering",
      "tests cover disconnect, resume, duplicate, out-of-order, sequence gap, terminal failure, and unmount cleanup without unbounded timers"
    ]
  },
  {
    "id": "timeseries-performance-budget",
    "entrySkill": "/timeseries-dashboard",
    "prompt": "8시간 동안 실행되는 6개 차트, 차트당 8개 시리즈의 실시간 대시보드를 성능 예산과 함께 구현하고 검증해줘.",
    "assertions": [
      "normal, maximum, and burst fixtures define visible-point, render-cadence, interaction, long-task, and heap-trend acceptance criteria",
      "raw events are validated, ordered, downsampled or aggregated, bounded, and batch-flushed without React state updates per tick",
      "browser verification checks initial render, update cadence, zoom interaction, reconnect recovery, buffer bounds, and sustained heap growth",
      "chart engine selection is based on measured fixtures rather than marketing data-volume claims"
    ]
  },
  {
    "id": "historical-timeseries-routing",
    "entrySkill": "/web-orchestrator",
    "prompt": "최근 1년의 매출 지표를 일·주·월 단위로 바꿔가며 비교하는 날짜별 시계열 대시보드를 만들어줘. 실시간 갱신은 필요 없어.",
    "assertions": [
      "TIMESERIES_MODE is enabled for historical time-series exploration even though realtime is explicitly excluded",
      "the intake asks for range, resolution, series cardinality, visible points, timezone, and historical query SLO",
      "realtime-data-builder is skipped while bounded historical aggregation and chart performance contracts remain active"
    ]
  },
  {
    "id": "realtime-chat-routing",
    "entrySkill": "/web-orchestrator",
    "prompt": "WebSocket으로 실시간 채팅, 읽음 상태, 접속자 presence를 구현해줘. 차트나 시간축 데이터는 없어.",
    "assertions": [
      "TIMESERIES_MODE remains disabled because a time-axis chart is not a core requirement",
      "the general realtime feature path is used without timeseries-architect or chart performance intake"
    ]
  },
  {
    "id": "local-domain-state-workflow",
    "entrySkill": "/web-orchestrator",
    "prompt": "서버 없이 브라우저 저장소에 업무 워크플로우를 관리하는 앱을 만들어줘. 필터와 검색 중에도 이동·정렬·다중 선택·삭제가 안전해야 하고 이전 버전이나 손상된 저장 데이터도 복구해야 해.",
    "assertions": [
      "LOCAL_DOMAIN_STATE_MODE is enabled and state-contract-designer runs before client-domain-state-builder",
      "the state contract separates authoritative collections from filtered, sorted, or virtualized views and defines ID-based mutation mapping",
      "move, reorder, bulk mutation, and delete use explicit commands with store-side preconditions and invariant-preserving postconditions",
      "persisted state has runtime validation, schema version migration, invalid-state recovery, and size or count budgets",
      "qa-state.md covers filter or search crossed with mutation, stale selection, hidden data, and recovery; missing evidence blocks release"
    ]
  },
  {
    "id": "preference-persistence-routing",
    "entrySkill": "/web-orchestrator",
    "prompt": "테마와 언어 설정만 브라우저에 저장하는 간단한 웹앱을 만들어줘. 업무 데이터 CRUD나 정렬·이동은 없어.",
    "assertions": [
      "LOCAL_DOMAIN_STATE_MODE remains disabled for independent low-volume preferences",
      "form-state-builder owns bounded validated preference persistence while local domain state agents are skipped"
    ]
  },
  {
    "id": "external-ingestion-static-snapshot",
    "entrySkill": "/web-orchestrator",
    "prompt": "허용된 세 개의 공개 데이터 소스를 매일 수집해서 정적 JSON으로 배포하는 일정 대시보드를 만들어줘. 일부 소스가 실패하거나 결과가 비면 이전 정상 데이터를 유지해야 해.",
    "assertions": [
      "EXTERNAL_DATA_INGESTION_MODE is enabled and source authorization, refresh cadence, freshness, minimum count, coverage, promotion policy, and serving fallback are collected separately",
      "ingestion-contract-designer fixes static-snapshot as the current runtime mode and writes both ingestion-contract.md and a strict schema-bound runtime-data-contract.json before implementation",
      "external-data-pipeline-builder validates source payloads and generated artifacts before atomic promotion and preserves last-known-good on empty, partial, schema-invalid, or threshold-failing runs",
      "data-quality-verifier requires deterministic fixtures, root and deployment clean-build evidence, and a current same-cohort ingestion machine receipt before qa-data-quality.md can pass"
    ]
  },
  {
    "id": "github-actions-static-ingestion-vercel",
    "entrySkill": "/web-orchestrator",
    "prompt": "타미야 대회 정보를 허용된 공개 소스에서 매일 GitHub Actions로 수집하고, 나머지는 Vercel에 올리는 React/Vite 정적 웹으로 처음부터 만들어줘. 실패한 수집 결과는 서비스에 반영하지 마.",
    "assertions": [
      "the locked profile separates deployment provider vercel from target static-cdn and includes external-ingestion plus scheduled-static-ingestion capabilities",
      "the web app, ingestion source, contracts, generated artifact, and workflow share one canonical release root so one source fingerprint covers the whole service",
      "ingestion-ci-writer alone owns the refresh workflow with full-SHA actions, persist-credentials false, read-only collection, exact generated paths, timeout, queued concurrency, manual recovery, direct-push prohibition, and isolated protected promotion",
      "vercel-config-writer alone binds the Vercel root, build command, output, routing, cache, and headers while provider builds validate the promoted snapshot before build and byte-identical public-to-dist deployment copy after build instead of crawling",
      "release is blocked unless strict artifact semantics and workflow policy pass and the ingestion receipt is current in the same isolated quality cohort"
    ]
  },
  {
    "id": "external-ingestion-selector-drift",
    "entrySkill": "/web-verify",
    "prompt": "외부 HTML 구조가 바뀐 뒤 크롤러 결과가 0건인데 workflow는 성공하고 빈 데이터가 배포됐어. 재발 방지까지 검증해줘.",
    "assertions": [
      "zero records from a required source is classified as FAIL rather than a successful refresh",
      "fixture tests cover selector or markup drift, missing required fields, pagination change, and abrupt count reduction",
      "failed output cannot replace last-known-good because validation precedes atomic promotion",
      "the finding routes contract, pipeline, test, and deployment fixes to their owning agents without a verifier editing source"
    ]
  },
  {
    "id": "generated-artifact-missing-source",
    "entrySkill": "/web-verify",
    "prompt": "배포 서비스에서는 prepare-build를 거쳐 성공하지만 clean clone에서 루트 pnpm build를 하면 필요한 public/data.json이 없어도 exit 0이야. 릴리스 가능한지 확인해줘.",
    "assertions": [
      "required generated artifact absence is a FAIL even when the bundler exits zero",
      "the runtime data contract identifies build cwd, deployment root, generation order, schema, and minimum count",
      "root, workspace, and provider build paths must prove the same artifact closure and architecture mode",
      "release remains blocked until deterministic clean-clone evidence and qa-data-quality.md pass"
    ]
  },
  {
    "id": "qa-evidence-tamper",
    "entrySkill": "/web-verify",
    "prompt": "package.json에 lint와 coverage script가 없는데 기존 QA Markdown에는 모든 명령이 exit 0 PASS라고 적혀 있어. 그대로 릴리스해도 되는지 검증해줘.",
    "assertions": [
      "Markdown command tables are not trusted without quality-runner machine receipts",
      "missing package scripts and zero discovered tests produce BLOCKED receipts rather than fabricated exit code zero",
      "receipts bind actual command, cwd, engine, process exit, test files, output hashes, and the current source fingerprint",
      "source changes invalidate prior receipts, signed attestation, and manifest v3, and HANDOFF remains blocked until isolated CI and the release gate pass"
    ]
  },
  {
    "id": "minimal-existing-feature-change",
    "entrySkill": "/feature-add",
    "prompt": "기존 칸반 보드에 마감일 필터만 추가해줘. 현재 카드 API와 저장 형식, 디자인은 유지하고 관련 없는 리팩터링은 하지 마.",
    "assertions": [
      "CHANGE_MODE is existing-change and change-scope.md records target behavior, allowed paths, preserved contracts, non-goals, expected change budget, and test evidence before the first source edit",
      "only the smallest owner agent set needed for the filter and its integration is invoked, while unrelated design, dependency, routing, and state rewrites remain out of scope",
      "any newly discovered public contract or cross-slice scope expansion is documented with root cause and blast radius before those paths are edited",
      "code-reviewer compares git diff paths and stat against the brief and fails unexplained broad rewrites, format-only noise, or unrelated changes"
    ]
  },
  {
    "id": "semantic-analytics-dashboard-builder",
    "entrySkill": "/web-orchestrator",
    "prompt": "그라파나 유사 서비스를 만들어줘. WebSocket 실시간 시계열과 1분·10분·30분·1시간·6시간 조회가 필요해. API 데이터의 metric과 dimension을 골라 line, bar, funnel, retention, flow, table 차트를 만들고 저장한 차트를 대시보드에 배치하고 싶어.",
    "assertions": [
      "TIMESERIES_MODE and ANALYTICS_BUILDER_MODE are both enabled while transport and semantic-query ownership remain separate",
      "intake asks no more than three staged questions and blocks only on unresolved metric meaning or query execution authority",
      "analytics-domain-architect defines metric and dimension catalogs, query AST, cardinality budget, chart compatibility, dedicated funnel retention and flow results, and dashboard revision before API or component implementation",
      "dashboard editing covers draft versus saved state, add move resize duplicate remove, unsaved changes, optimistic conflict, accessibility, and layout stability",
      "analytics-verifier uses deterministic normal empty partial incompatible high-cardinality and revision-conflict fixtures, and missing qa-analytics.md blocks release"
    ]
  },
  {
    "id": "selective-openapi-existing-change",
    "entrySkill": "/api-connect",
    "prompt": "기존 사내 React 서비스에 다중 삭제 API 두 개만 OpenAPI에서 연결해줘. 기존 API client와 타입, MSW는 유지하고 나머지 수백 개 endpoint는 생성하거나 변경하지 마.",
    "assertions": [
      "CHANGE_MODE is existing-change and the integration overlay records app root, package manager, aliases, API client, auth context, UI library, MSW activation, and current generator",
      "the OpenAPI source is inspected without committing a full snapshot and only the two operation IDs or path plus method pairs enter the adoption manifest",
      "an existing generator is reused; when none exists a narrow generator or handwritten validated adapter is proposed before source edits",
      "generated output is isolated behind a stable adapter and diff budget, unrelated endpoints and public contracts remain unchanged, and Mock handlers share the selected request and response contract",
      "change journal and rollback instructions identify generated files, commands, evidence, and safe removal boundaries"
    ]
  },
  {
    "id": "visual-baseline-tamper",
    "entrySkill": "/visual-design-verify",
    "prompt": "Playwright 시각 테스트는 통과했다고 적혀 있지만 baseline PNG가 최근 수정됐고 승인 manifest의 hash는 이전 값이야. 디자인 릴리스 가능 여부를 검증해줘.",
    "assertions": [
      "visual-regression-verifier remains read-only and never runs --update-snapshots",
      "browser visualEvidence binds the visual contract, test assertions, approved manifest, and current PNG SHA-256",
      "a baseline hash mismatch or missing approval metadata produces BLOCKED rather than accepting the new image",
      "baseline promotion requires explicit human review of before, after, diff, target, and reason"
    ]
  },
  {
    "id": "visual-render-matrix",
    "entrySkill": "/visual-design-verify",
    "prompt": "라이트·다크 테마와 한국어·영어를 지원하는 대시보드의 모바일 overflow, sticky header focus 가림, 폰트 로딩 후 카드 이동을 시각 검증해줘.",
    "assertions": [
      "the contract selects a risk-based state, viewport, theme, and locale matrix instead of an unbounded Cartesian product",
      "the matrix includes 320 CSS pixel reflow or 400 percent zoom equivalent and a representative desktop viewport",
      "font readiness, frozen clock and data, disabled animation, locale, timezone, DPR, and reduced motion are deterministic",
      "focus obscuring, target size, axe, keyboard behavior, and CLS are checked alongside pixel comparison"
    ]
  },
  {
    "id": "visual-source-token-drift",
    "entrySkill": "/visual-design-verify",
    "prompt": "Figma의 버튼 variant와 design token은 바뀌었는데 React 컴포넌트와 MUI theme가 예전 값을 사용하는지 확인해줘.",
    "assertions": [
      "Figma MCP or export access is treated as an explicit capability with a local export and hash fallback",
      "Code Connect or an equivalent mapping links the design component to the actual code component when available",
      "DTCG token type, value, alias resolution, theme variants, and the Figma to token to CSS to UI-lane theme consumer mapping are checked",
      "AI vision findings remain advisory while deterministic token, structural, and screenshot evidence controls the release gate"
    ]
  },
  {
    "id": "product-first-planning-intake",
    "entrySkill": "/web-plan",
    "prompt": "기존 업무 화면이 전반적으로 어색하고 복잡해. 더 자연스럽게 고도화하는 기획을 해줘. 아직 API나 기술 스택은 정하지 않았어.",
    "assertions": [
      "the first intake asks no more than three questions about target screen, primary user task, current pain, and observable success before API, files, libraries, or deployment",
      "ambiguous discomfort is analyzed through hierarchy, mode separation, affordance, layout stability, and state clarity before copy changes",
      "planning-context.md records evidence, assumptions, critical states, and a current planning memo without creating implementation or prototype source"
    ]
  },
  {
    "id": "annotated-dashboard-ux-planning",
    "entrySkill": "/web-plan",
    "prompt": "표와 차트가 같이 있는 권한 관리 대시보드에 스크린샷 주석이 여러 개 있어. 선택, 일괄 삭제, disabled, 부분 실패까지 포함해서 기획해줘.",
    "assertions": [
      "the automatic UX Check covers first-glance hierarchy, next action, misunderstanding risk, direction, and Phase 2 prototype checks",
      "annotations are batched and normalized into target, user intent, scope, and verification rather than copied as coordinates",
      "normal, empty, loading, error, partial, permission, and destructive states are traced to screens, owners, and evidence"
    ]
  },
  {
    "id": "planning-data-effort-readiness",
    "entrySkill": "/web-plan",
    "prompt": "실제 고객 데이터 연결 여부가 아직 불명확한 분석 화면을 기획해줘. 먼저 Mock으로 리뷰할 수도 있고 범위를 줄이거나 나눌지 판단이 필요해.",
    "assertions": [
      "planning selects mock, dev-read-only, real-read-only, or production-integration-later with safety and Mock-to-real transition conditions",
      "effort is expressed as S, M, L, or XL with drivers, invest/reduce/split guidance, smallest visible review unit, and a separate production integration delta rather than invented calendar estimates",
      "plan-reviewer always returns PASS, NEEDS_DECISION, or BLOCKED and exposes at most three priority decisions before Phase 2"
    ]
  },
  {
    "id": "seo-public-site-contract",
    "entrySkill": "/web-orchestrator",
    "prompt": "검색 유입이 중요한 공개 행사 안내 사이트를 만들어줘. 행사 목록과 상세 페이지가 검색엔진과 소셜 공유에 잘 노출돼야 해.",
    "assertions": [
      "seo-meta-builder produces _workspace/02_design/seo-spec.md with per-route title, description, canonical, and OG rows matching the actual route inventory",
      "robots.txt and sitemap ownership stays with seo-meta-builder and noindex routes are excluded from the sitemap",
      "the CSR SEO limitation and SSR migration trigger are recorded in the spec instead of silently promising crawler visibility",
      "seo-verifier returns qa-seo.md and a missing seo-spec with a public-exposure requirement is reported as BLOCKED, not PASS",
      "once seo-spec.md exists, qa-seo.md is a required release report and its absence or non-passing result blocks the release manifest"
    ]
  },
  {
    "id": "seo-internal-tool-negative",
    "entrySkill": "/web-orchestrator",
    "prompt": "사내 관리자만 로그인해서 쓰는 재고 관리 도구를 만들어줘. 외부 공개나 검색 노출은 없어.",
    "assertions": [
      "seo-meta-builder and seo-verifier are not invoked for an authenticated internal tool",
      "no robots.txt/sitemap/OG scaffolding is generated without a public-exposure requirement"
    ]
  },
  {
    "id": "performance-budget-verification",
    "entrySkill": "/web-verify",
    "prompt": "성능 예산이 정의된 기존 대시보드 프로젝트를 검증해줘. 번들이 예산을 넘는지와 런타임 지표를 확인해줘.",
    "assertions": [
      "performance-verifier runs only when _workspace/02_design/performance-budget.md exists and reads bundle sizes from build evidence and deployment artifacts, not estimates",
      "runtime metrics without browser evidence are reported as NOT_MEASURED and never converted to PASS",
      "budget violations name the exceeded row, the measured bytes, and an owner agent from the retry mapping",
      "once performance-budget.md exists, qa-perf.md is a required release report and its absence or non-passing result blocks the release manifest"
    ]
  },
  {
    "id": "i18n-companion-detection",
    "entrySkill": "/web-orchestrator",
    "prompt": "한국어와 영어를 모두 지원하는 고객 포털을 만들어줘. 언어 전환 시 입력 중인 폼이 유실되면 안 돼.",
    "assertions": [
      "I18N_MODE is detected as a companion flag and /i18n-setup with i18n-builder is inserted after shared-foundation-builder and before component-builder",
      "all locale catalogs under src/shared/lang/ share an identical key set and untranslated values carry TODO_TRANSLATE instead of invented translations",
      "locale routing segments remain owned by route-builder or next-runtime-builder and are only specified in i18n-spec.md",
      "ux-validator checks translation completeness and hardcoded user-facing strings when i18n-spec.md exists"
    ]
  },
  {
    "id": "timeseries-dedicated-verifier",
    "entrySkill": "/web-verify",
    "prompt": "실시간 시계열 대시보드 프로젝트의 스트림 계약과 버퍼 상한, 재연결 복구를 검증해줘.",
    "assertions": [
      "timeseries-verifier runs when timeseries-architecture.md exists and returns qa-timeseries.md as the single timeseries release verdict",
      "unbounded buffers, missing reconnect/resume/gap coverage, and mock transport imports in production code are reported as FAIL with owner candidates",
      "performance evidence absent from browser receipts is reported as NOT_MEASURED, not PASS",
      "once timeseries-architecture.md exists, qa-timeseries.md is a required release report and its absence or non-passing result blocks the release manifest"
    ]
  },
  {
    "id": "observability-companion-contract",
    "entrySkill": "/web-orchestrator",
    "prompt": "운영에 들어갈 예약 서비스라서 프론트 에러 추적과 웹바이탈 수집이 필요해. 릴리즈별로 오류를 구분하고 개인 정보는 수집하면 안 돼.",
    "assertions": [
      "OBSERVABILITY_MODE is detected and web-observability-builder runs before deploy-ci-writer with the source map upload contract handed over, not executed",
      "error tracking initialization lives under src/shared/observability/ with release/environment tags injected from build info and PII scrubbing enabled by default",
      "no DSN or credential literal appears in source and dev mode does not send events"
    ]
  },
  {
    "id": "design-preview-loop",
    "entrySkill": "/web-orchestrator",
    "prompt": "팀 업무 현황 대시보드를 만들어줘. 디자인 취향은 아직 못 정했는데 밀도가 높은 게 좋을지 여백이 있는 게 좋을지 모르겠어.",
    "assertions": [
      "the undecided density preference is recorded as ASSUMPTION(시안 확정) instead of an invented value, with the divergence-research protocol committing a single direction and recording the rejected alternative",
      "design-preview-builder generates dependency-free static previews under _workspace/02_design/preview/ with per-screen state variants and a fidelity disclaimer banner",
      "traceability.json maps every Feature List FEAT and TC to stable data-wh-* DOM anchors where the behavior is visible, and each anchor exposes a keyboard-accessible FEAT badge that opens related feature and test-case details",
      "before snapshot approval every interactive surface is classified: repeated entity labels are covered by their existing navigation/list anchor, existing behavior on another surface gains another anchor, and truly unplanned user behavior returns to feature-planner for REQ/FEAT/TC and decision-log write-back instead of being silently omitted or assigned a feature from its seed label",
      "preview feedback is applied to the design specs first by the owning designer and the preview is regenerated — the preview files are never treated as the source of truth",
      "the approved source, preview, and traceability hashes are recorded append-only in design-review.md; any later source or preview change derives STALE and blocks Phase 3 until regeneration and reapproval, while planning-level changes return to Feature List refinement"
    ]
  },
  {
    "id": "feature-add-planning-writeback",
    "entrySkill": "/feature-add",
    "prompt": "주문 상세 화면에 배송지 변경 기능을 추가해줘. 결제 완료 상태에서만 가능하고 변경 전 확인 dialog가 필요해.",
    "assertions": [
      "after the Phase 1 confirmation, feature-plan.md's Feature List is updated with a stable FEAT id and requirements.md is refreshed via lightweight re-invocation of the owning planning agents",
      "a PC-NNN entry is appended to decision-log.md with trigger, target ids, before→after, approval, and affected artifacts",
      "the change is not implemented as an unrecorded change — a missing write-back is flagged by code-reviewer"
    ]
  },
  {
    "id": "plan-history-append-only",
    "entrySkill": "/web-verify",
    "prompt": "기획 문서가 바뀐 기존 프로젝트를 검증해줘. decision-log의 과거 엔트리 하나가 수정된 상태야.",
    "assertions": [
      "code-reviewer reports a modified or deleted existing PC entry in decision-log.md as FAIL (append-only violation)",
      "planning document diffs without a corresponding PC entry are reported as WARN (unrecorded change)",
      "reviewers do not rewrite the log themselves — the fix is routed to planning-facilitator as the owning agent"
    ]
  },
  {
    "id": "hybrid-endpoint-guard-uniformity",
    "entrySkill": "/vite-serverless-hybrid",
    "prompt": "Vite SPA + serverless 구성에 엔드포인트를 하나 더 추가해줘. 기존 api/analyze.js는 인증·body 32KB 캡·스키마 검증·rate limit이 다 있고, 새로 만들 api/data.js는 로그인 사용자의 전체 데이터를 GET/PUT하는 스냅샷 동기화야.",
    "assertions": [
      "the new endpoint carries all five guards from SKILL.md §7 (method allowlist, auth guard, body size cap, input schema validation, rate limit) — not merely the subset the request mentions",
      "the PUT snapshot path validates the request body server-side per row with array-length and string-length bounds; Array.isArray alone is reported as insufficient",
      "guards are implemented as shared _lib/ modules rather than re-written per handler",
      "the highest-defense sibling handler (api/analyze.js) is used as the baseline, and a guard present there but absent in the new handler is treated as a defect",
      "client-side validation is never accepted as a substitute for server-side validation on the write path",
      "new Vercel Node handlers prefer the current Web Standard Request/Response contract and the Vite dev adapter preserves streaming body limits instead of buffering an unbounded body first"
    ]
  },
  {
    "id": "hybrid-unauthenticated-paid-path",
    "entrySkill": "/vite-serverless-hybrid",
    "prompt": "serverless 함수에서 외부 LLM API를 호출하는 엔드포인트를 만들어줘. 서버에 저장된 API 키를 쓰고, 소유자 제한은 ALLOWED_EMAIL 환경변수로 켤 생각이야.",
    "assertions": [
      "the paid upstream call happens only after the auth guard passes — cost is never incurred before authorization",
      "when ALLOWED_EMAIL is unset or blank the paid path fails closed (503-style) rather than fail-open, and the fail-open/fail-closed split is stated explicitly",
      "a rate limit bounds the paid path per user or IP, and its serverless limitation (per-instance memory, cold-start reset) is disclosed rather than presented as a hard guarantee",
      "prompt bodies and model response text are excluded from logs; only structured scalars are logged",
      "the endpoint guard matrix (§7) is reported with no gaps; a checked-in or host-run golden remains T0, and even a checked-in T1 workflow proposal remains preparation until its protected environment and isolated runner produce a passing single-cohort summary",
      "the compatible profile does not claim certified production evidence without actual provider evidence and external attestation"
    ]
  },
  {
    "id": "ui-lane-brand-forward-greenfield",
    "entrySkill": "/web-orchestrator",
    "prompt": "브랜드 아이덴티티가 차별점인 소비자 대상 포트폴리오·쇼케이스 웹서비스를 만들어줘. 디자인 자유도가 최우선이야.",
    "assertions": [
      "tech-stack.md records UI_LANE: tailwind-shadcn with a one-line rationale tied to the lib-catalog decision axis (design as differentiator)",
      "vendored primitives land under src/shared/ui with explicit named exports, and no src/components/ui or components.json appears",
      "vendored Radix-based files preserve aria/role/Portal/focus wiring, with a one-line rationale on any deviation",
      "node .claude/scripts/validate-ui-lane.mjs --project reports PASS with no @mui or @emotion imports in src",
      "tokens live in src/app/style.css @theme derived from the design-system output, with no feature-local palettes or arbitrary color values"
    ]
  },
  {
    "id": "ui-lane-brownfield-no-flip",
    "entrySkill": "/web-orchestrator",
    "prompt": "기존 MUI 기반 어드민 앱에 알림 설정 화면을 추가해줘. 새 화면도 기존 디자인과 똑같은 톤이어야 해.",
    "assertions": [
      "integration-overlay.json records uiLibrary.uiLane: mui from the existing app, and that measurement takes precedence over any fresh lane preference",
      "the new screen uses the existing MUI theme and shared components — no tailwindcss, cva, or tailwind-merge dependency is introduced",
      "node .claude/scripts/validate-ui-lane.mjs --project reports PASS against the overlay-declared mui lane",
      "existing screens' observable appearance is preserved per the minimal-change baseline rule"
    ]
  },
  {
    "id": "analytics-builder-engine-inform-and-choose",
    "entrySkill": "/analytics-chart-builder",
    "prompt": "메트릭·디멘션 기반 차트 빌더와 편집 가능한 대시보드를 만들어줘. 차트 품질이 중요하고 회사가 Highcharts 라이선스를 보유하고 있어.",
    "assertions": [
      "engine selection follows chart-engine-adapter inform-and-choose: the commercial-license requirement is disclosed, the user makes an explicit choice, and the choice lands in decision-log (free adapter remains the default otherwise)",
      "domain and AST modules contain no chart-engine imports — the engine-agnostic renderer boundary holds",
      "the semantic query AST validates metric/dimension allowlists and rejects unsupported chart-type transitions with a stated reason",
      "reusable segment filters keep their reference id through save and reload — an empty operand never silently drops the filter",
      "a metric-set control only targets views with two or more metric sets and resolves same-view conflicts first-wins deterministically"
    ]
  }
]
