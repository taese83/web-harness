// 목록 표시와 행 선택을 지원하는 공용 표. 공개 props: rows, columns, selectedIds, onSelect, empty, error.
export function DataTable(props: {rows: unknown[]; columns: string[]; selectedIds?: string[]; onSelect?: (id: string) => void; empty?: string; error?: string}) {
  return null
}
