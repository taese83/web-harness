# seed: brownfield-ticket-project

기획·디자인 없이 스팩만 확정된(`spec.mjs`로 잠근 `unverifiable`) 작은 브라운필드 앱. 티켓 초안·레인 판정 같은 **행동** 회귀 시나리오가 빈 fixture가 아니라
진행 중인 프로젝트에서 돌도록 fixture에 복사된다(`run-eval-executor.mjs`의 `seed`).
