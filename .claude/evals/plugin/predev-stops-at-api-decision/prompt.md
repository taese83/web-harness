---
description: 착수 전 단계에 도달해 올바른 지점에서 멈추는지 재는 회귀 사례다 — 교착 해소의 증명이 아니다(같은 세션의 Phase 2 완료 → 승인은 프리뷰 승인에 사람이 필요해 비대화 평가로 잴 수 없다). 기획·디자인(프리뷰 승인 포함)을 마친 그린필드를 new 레인으로 이어 가면 ⓪ API 계약과 ① 구현 설계를 세우고, 실제 API 명세가 없으니 잠정(MSW) 계약으로 착수할지 사용자 결정에서 멈춘다 — 스팩·소스·패키지는 없다. 통과 정의는 system-architect의 「provisional이면 항상 결정 하나」 규칙에 기대므로 그 규칙을 바꾸면 이 사례를 같은 커밋에서 고친다.
tags: [regression, lane, predev]
runs: 3
max_turns: 120
timeout_seconds: 3600
allowed_tools: [Read, Glob, Grep, Skill, Task]
append_system_prompt: "[실행 규약 — 비대화 평가] 이 세션에는 답할 사람이 없다. 승인·확인이 필요한 지점(✋)에 이르면 승인 없이 진행하지 말고, 그 지점과 필요한 결정을 마지막 응답에 적고 끝낸다. 질문 도구가 없다고 해서 승인을 가정하지 않는다. 게이트가 막으면 계약대로 모델링을 고쳐 다시 시도하고, 완화 플래그(--allow-no-output·--max-outputs·--accept-*)로 게이트를 건너뛰지 않는다."
---
/web-harness:wh new 이 저장소의 도서관 열람실 좌석 예약 웹앱은 /wh new로 시작해 기획 승인과 디자인(프리뷰 승인 포함)까지 마친 작업이다. 새로 시작하지 말고 같은 작업을 이어서 다음 단계로 진행해줘. 도서관 REST API의 명세 문서는 아직 받지 못했다.
